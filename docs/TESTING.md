# TESTING

## 실행
```bash
node tests/run.mjs            # 1회
node tests/run.mjs 10         # 10회 반복 (플레이키 검출)
node tests/run.mjs 3 "조명"   # 이름에 "조명" 이 든 항목만 3회
node tools/codemap.mjs        # docs/CODEMAP.md 다시 생성
node tools/tidy-imports.mjs   # 안 쓰는 import 정리
```
실패가 있으면 종료 코드 1.

## 구성
- `tests/harness.mjs` — 정적 서버 + 브라우저 기동 · 게임 로드 대기 · `assert` / `eq` / `near`
  ES 모듈은 `file://` 에서 CORS 로 막히므로 **임시 HTTP 서버**를 띄워 그 위에서 돌립니다.
- `tests/run.mjs` — 테스트 정의 + 러너 + 회차별 요약
- 브라우저: `playwright-core` + 로컬에 캐시된 **Chrome for Testing**
  (`~/Library/Caches/ms-playwright/chromium-1228/...`), WebGL 은 SwiftShader.
  경로는 환경변수 `BY_CHROME` 로 바꿀 수 있습니다.
  `playwright-core` 는 스크래치패드에 설치돼 있고 `BY_SCRATCH` 로 위치를 바꿀 수 있습니다.

## 규칙
1. **회차마다 페이지를 새로 엽니다.** 테스트끼리 월드 상태가 새는 것은 감수하되,
   지형에 의존하는 테스트는 `arena(B, x, y, z, r)` 로 **평평한 시험장을 직접 만들어** 씁니다.
   (지형에 기대면 앞선 테스트의 `generate(시드)` 때문에 무작위로 깨집니다 — 실제로 겪었습니다.)
2. **동물이 나오는 테스트는 시험 대상 외의 동물을 먼저 멀리 치웁니다.**
   `feedNearbyMob` · `pushOutOfMobs` 는 "가장 가까운" 동물을 고르므로,
   14마리가 돌아다니는 상태에서는 10회 중 1회꼴로 엉뚱한 동물이 걸립니다. (v14·v18 에서 각각 겪었습니다)
3. **콘솔 오류 0** 자체가 하나의 테스트입니다. `pageerror` · `console.error` 를 전부 모읍니다.
4. 결정적인 결과가 필요하면 `B.setPaused(true)` 로 rAF 루프를 멈추고
   `B.step(1/60)` 을 직접 돌립니다. 끝나면 `setPaused(false)`.
5. 입력은 `B.setKey("KeyW", true)` 로 넣고, 실제 이벤트 경로를 검증할 때만
   `dispatchEvent(new MouseEvent(...))` 를 씁니다.
6. **새 기능에는 테스트를 같이 넣습니다.** 이름은 `개선N ...` 처럼 무엇을 지키는지 드러나게.
   좌표는 남의 시험장과 겹치지 않게 새로 고릅니다 (같은 자리에 남은 블록에 걸립니다).
7. **의존하는 양은 시험 첫 줄에서 직접 못 박습니다.** `seedMobs()` 처럼 "모자란 만큼만 채우는"
   준비 함수는 앞선 시험이 늘려 놓은 것을 그대로 물려받습니다.
   단독으로는 통과하는데 `run.mjs 10` 에서만 1~2회 깨지면 대개 이 부류입니다.

<!-- stamp:tests — 아래 표는 tools/stamp.mjs 가 tests/run.mjs 에서 세어 다시 씁니다. 손으로 고치지 마세요 -->
## 현재 항목 (251개)

| 묶음 | 개수 |
|---|---:|
| 개선1 | 1 |
| 개선2 | 1 |
| 개선3 | 1 |
| 개선4 | 1 |
| 개선5 | 1 |
| v6 | 14 |
| 개선6 | 1 |
| 개선7 | 2 |
| v8 | 10 |
| 개선8 | 1 |
| v9 | 10 |
| 개선9 | 3 |
| v10 | 9 |
| 개선10 | 2 |
| v11 | 9 |
| v12 | 10 |
| v13 | 9 |
| v14 | 10 |
| v15 | 10 |
| v16 | 10 |
| v17 | 10 |
| v18 | 10 |
| v19 | 18 |
| v20 | 4 |
| v21 | 3 |
| v22 | 1 |
| v23 | 6 |
| v24 | 7 |
| v25 | 2 |
| v26 | 4 |
| v27 | 2 |
| v28 | 1 |
| v29 | 1 |
| v30 | 2 |
| v31 | 2 |
| v32 | 1 |
| v33 | 3 |
| v34 | 1 |
| v35 | 1 |
| v36 | 1 |
| v37 | 1 |
| v38 | 1 |
| v39 | 1 |
| v40 | 1 |
| v41 | 3 |
| v42 | 2 |
| v43 | 1 |
| v44 | 1 |
| v45 | 1 |
| v46 | 1 |
| v47 | 1 |
| v48 | 2 |
| v49 | 2 |
| v50 | 2 |
| v51 | 1 |
| v52 | 1 |
| v53 | 1 |
| v54 | 1 |
| v55 | 1 |
| v56 | 1 |
| v57 | 1 |
| v58 | 3 |
| v59 | 1 |
| v60 | 3 |
| v61 | 1 |
| topMap | 1 |
| UI | 1 |
| 도전 | 1 |
| 레이캐스트 | 1 |
| 메시 | 2 |
| 물 | 1 |
| 물리 | 2 |
| 미니맵 | 1 |
| 부팅 | 1 |
| 시드 | 1 |
| 안전 | 1 |
| 저장 | 2 |
| 조명 | 2 |
| 중력 | 1 |
| 지형 | 2 |
| 편집 | 1 |

항목 이름 전체는 `grep -n '^test(' tests/run.mjs` 로 봅니다.
<!-- /stamp:tests -->
