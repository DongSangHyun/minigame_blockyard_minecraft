# BACKLOG — 아직 안 한 것

> v5~v24 에 걸쳐 **자문 5회 · 개선 120건 이상**이 반영됐습니다.
> 지난 목록(B·C 계열)은 전부 처리되어 아래로 갈아 끼웠습니다. 자세한 내역은 `memory.md`.

## 다음에 볼 만한 것
| 항목 | 내용 | 난이도 |
|---|---|:--:|
| **무한 월드 / 청크 스트리밍** | `WX·WZ` 가 96 고정이라 끝에 닿으면 격자벽을 만난다. 배열 재할당과 저장 포맷이 함께 움직여야 한다 | 상 |
| **면 병합(greedy meshing)** | 지금도 여유가 크지만(전체 메싱 34ms · 삼각형 69k), 세계를 키우려면 필요하다 | 중 |
| **서바이벌 모드** | 체력·낙하 피해·용암 피해·밤 위협. 지금은 완전한 크리에이티브다 | 상 |
| **인벤토리·채집·크래프팅** | 서바이벌과 한 묶음 | 상 |
| **용암 흐르기** | v24 에서 "불 붙이기" 만 넣었다. 흐르기는 `waterTick` 복제 + `MAXFLOW` 2 · 물의 4배 느리게 | 중 |
| **진짜 문(2칸)** | `GATE`(울타리 문)뿐인데 `docs/GAMEPLAY.md` 는 "문" 이라고 적어 두었다 | 중 |
| **원목 축 3방향** | 지금은 X·Z 만. Y(세로)는 기본값이라 별도 표시가 없다 | 하 |
| **명령창 확장** | `clone` · `mob` 등 (`undo <n>` 은 v23 에서 넣었다) | 중 |

| **지붕 아래 동물 보행** | `groundAt` 이 `topMap`(기둥 최고점=지붕)을 땅으로 봐 헛간 안에서 얼어붙는다. v26 에서 고쳐 봤으나 얕은 물 회피와 충돌해 되돌렸다 — **물 회피와 한 판정으로 묶어야 한다** | 중 |

## 건드리지 말 것 (자문 의견 — 마크보다 나은 부분)
- 놓을 자리 고스트 미리보기 · `Ctrl+Z` 되돌리기(대량 편집도 한 번에)
- 생성 시점에 모래/자갈 침강을 미리 처리
- `G` 하나로 풀블록/반블록/계단 전환 · 지하에서 미니맵이 단면도로 전환
- 영역 도구(채우기·복사·붙여넣기·청사진) — 마크 바닐라에는 없는 건축 도구
