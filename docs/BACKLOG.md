# BACKLOG — 아직 안 한 것

> v5~v24 에 걸쳐 **자문 5회 · 개선 120건 이상**이 반영됐습니다.
> 지난 목록(B·C 계열)은 전부 처리되어 아래로 갈아 끼웠습니다. 자세한 내역은 `memory.md`.

## 다음에 볼 만한 것
| 항목 | 내용 | 난이도 |
|---|---|:--:|
| **무한 월드 / 청크 스트리밍** | `WX·WZ` 가 96 고정이라 끝에 닿으면 격자벽을 만난다. 배열 재할당과 저장 포맷이 함께 움직여야 한다 | 상 |
| ~~**면 병합(greedy meshing)**~~ (v102 완료 — 자연 지형 −12% · 건물 포함 −19%) | 지금도 여유가 크지만(전체 메싱 **52~65ms** — v79 로 지하가 두 배가 되며 34~45ms 에서 올랐다), 세계를 키우려면 필요하다. 전체 메싱은 세계를 갈아탈 때만 도는 일회성이고 `buildBudget` 이 프레임마다 나눠 굽는다 | 중 |
| **서바이벌 모드** | 체력·낙하 피해·용암 피해·밤 위협. 지금은 완전한 크리에이티브다 | 상 |
| **인벤토리·채집·크래프팅** | 서바이벌과 한 묶음 | 상 |



## 자문 5~7차가 남긴 것
**전부 처리됐습니다.** 마지막으로 닫힌 것 —
5차 청사진(용량 v56 · 목록 UI v63) · 7차 묘목(v62). 자세한 내역은 `memory.md`.

## 자문 8차가 남긴 것 (2026-09-07 · 유저가 이번에는 넘긴 것)
| 항목 | 내용 | 난이도 |
|---|---|:--:|
| ~~**물·용암 지우개(양동이)**~~ (v86 담기 · **v90 붓기** 완료) | 물·용암은 **놓기는 되는데 좌클릭으로 지울 수가 없는 유일한 블록**이다(레이캐스트가 액체를 건너뛴다 — 물속에서 바닥을 캐야 하므로 그 자체는 맞다). 지우려면 `/fill 공기` 나 영역 비우기까지 가야 해서 결국 물을 안 쓰게 된다. 부싯돌이 이미 "놓는 블록이 아닌 손에 쥐는 물건"(`ITEMS`)의 선례다 — `src/blocks.js` 의 `ITEMS`, `src/mine.js` 의 `tryInteract` | 중 |
| ~~**핫바 칸마다 모양 기억**~~ (v82 완료) | `G` 로 고른 모양(`S.shapeMode`)이 **전역 하나**라, 3번 칸(계단)에서 2번 칸(유리)으로 바꾸면 유리도 계단으로 나온다. 지붕 한 채에 `G` 를 스무 번 친다. 길이 10짜리 배열로 바꾸고 `selectSlot` 에서 읽는다. **핫바 2쪽(`S.barAlt`)과 저장(`bar`/`bar2`)에 같이 실어야 하고, 세계 전환 두 경로도 함께** (v61 곁가지의 그 함정) | 소~중 |

## 자문 10차가 남긴 것 (2026-09-07 · 실측 근거 있음)
> 전부 처리됐습니다 — 재굽는 범위 v70(바닥 한 장 144→8청크 · 힙 54.7→31.2MB) ·
> 시작 화면 v71("이어서 짓던 곳") · 불을 되돌리기에 담기 v72.

| 항목 | 내용 | 난이도 |
|---|---|:--:|

## 자문 11차가 남긴 것 (2026-09-08 · 실측 근거 있음)
> **바다 장식**(v75)과 **오두막 다양화**(v76)는 처리했습니다.
> 둘 다 **별도 난수 줄기**를 써서 기존 시드의 땅이 안 밀립니다 — 시험이 그것을 못 박습니다.

| 항목 | 내용 | 난이도 |
|---|---|:--:|
| ~~**지하 구조물**~~ (v81 완료 — 버려진 갱도) | (v79 로 동굴 공기가 **34,000~58,000칸**이 되어 넣을 자리가 넉넉해졌습니다) 예전 진단: 동굴 공기가 **8,586~24,846칸**인데 구조물이 **아예 없습니다**. 지상은 v76 에서 오두막을 다양하게 했지만, 굴을 파고 내려가면 볼 것이 광맥뿐입니다. 버려진 갱도(횃불 줄·나무 지지대·조약돌 바닥)나 작은 방이면 `src/world.js` 의 `buildHuts` 와 같은 틀을 쓸 수 있습니다 — **별도 난수 줄기**로 (v75·v76 처럼) | 중 |

| 항목 | 내용 | 난이도 |
|---|---|:--:|
| ~~**실내 소품 더**~~ (v84 완료 — 색 카펫 16 · 화분 · 액자) | 책장·카펫은 v74 에서 들어갔습니다. 다음은 **색 카펫**(양털 16색과 짝) · 화분 · 액자. 간판은 글자 렌더가 붙어 큽니다 | 소~중 |

## 자문 12차가 남긴 것 (2026-09-08 · 실측 근거 있음)
> 열 건 중 **아홉**을 v78 에서 처리했습니다 — 물·용암 되돌리기 · 스텝업 눈높이 ·
> 폰 미니맵 겹침 · 번식 하트 · 폰 픽블록/핫바 2쪽 · 폰 도움말 · 놓기 반복 간격 ·
> 동물 무리 스폰 · 눈 쌓임. 남은 한 건은 아래입니다.

> **지하를 깊게는 v79 에서 처리했습니다** (아래는 그때의 진단입니다).

| 항목 | 내용 | 난이도 |
|---|---|:--:|
| ~~**지하를 깊게**~~ (v79 완료) | 세계는 64칸인데 **지표에서 기반암까지 13.5~15.3칸뿐**입니다(최고점 위로는 30~38칸이 늘 빈 하늘). 석탄 중앙값 y=4, 철·금·다이아 y=3~4 — **"내려갈수록 좋은 것"** 이라는 사다리가 없습니다. `WY` 도 저장 포맷도 안 건드리고 **지형 기준 높이만 내려서** 같은 64칸 안에서 지하를 두 배로 만들 수 있습니다. 다만 **모든 시드의 땅이 달라지므로** v75·v76 의 난수 줄기 규칙으로는 못 덮습니다 — 새 세계에만 적용하고 예전 저장은 그대로 열리는 갈래가 필요합니다. **유저 승인이 필요한 항목입니다.** 백로그의 "무한 월드" 는 가로(96 고정) 이야기라 이것과 별개입니다. 지하 구조물을 넣더라도 **넣을 자리가 14칸뿐이라 이게 먼저입니다** | 중 |

> **v81 에서 지하 지도 층 나누기와 버려진 갱도를, v82 에서 굴 어귀 · 핫바 칸별 모양 ·
> `high` 과제를 처리했습니다.** (양동이는 v86·v90 · 소품은 v84 에서 끝났습니다.) 남은 것은
> 그리고 큰 것 셋(면 병합 · 무한 월드 · 서바이벌·인벤토리)입니다.

## 자문 13차가 남긴 것 (2026-09-08 · 실측 근거 있음)
> 열 건 중 **아홉**을 v80 에서 처리했습니다 — 바깥 바다 판 · 발밑 지하 컬링 · 갱도 과제 ·
> 미리보기 산 비율 · 동굴 울림 · 용암 띠 · 구름 높이 · 시작 화면 카메라 · 광맥 분포.
> 열째(지하에서 길을 잃는다)는 **절반**만 했습니다 — 굴 어귀 표시와 표식 층 표시는 넣었고,
> 아래가 남았습니다.

| 항목 | 내용 | 난이도 |
|---|---|:--:|
| ~~**지하 지도를 층으로 나누기**~~ (v81 완료) | `seenMap` 의 지하 비트가 `SEEN_UNDER` **한 장**이라, 판 1 에서 13층이 나눠 쓰던 것을 판 2 에서는 **26층**이 나눠 씁니다. 위층 굴을 밝혀 놓고 아래층으로 내려가면 그 칸들이 "밝혀짐" 인 채 **전부 통돌 회색**으로 그려집니다(실측: y20 에서 633칸을 밝힌 뒤 y4 에서 보면 돌 82% · 공기 0%). 지하 밝힘 반경도 6칸(지상 14칸)이라 두 배로 걸어야 합니다. 층 묶음 두세 장으로 쪼개면 됩니다 — `mm` 은 선택 필드라 **저장 버전은 안 올려도 되지만**, 칸당 값의 뜻이 바뀌므로 예전 저장을 읽는 갈래가 필요합니다 | 중 |
| ~~**굴 어귀를 더 뚫기**~~ (v82 완료) | 굴 1,000칸당 어귀가 판 1 의 14~31개에서 판 2 에서 2.4~5.2개로 떨어졌습니다(뭍 기둥 대비로는 9~13% → 5~9.5%). 지표 카브 문턱을 0.66 → 0.638 로 낮춰 봤지만 어귀가 178→189 로 거의 안 늘었습니다 — **병목은 문턱이 아니라 `carvedBelow`(바로 아래 칸이 이미 뚫려 있어야 한다)** 입니다. 세로 협곡을 지표까지 잇거나, 어귀 전용 굴착을 따로 두어야 합니다. 지형이 달라지므로 별도 난수 줄기로 | 중 |
| ~~**`high` 과제가 산으로는 안 열린다**~~ (v82 완료) | `y > 50` 인데 최고봉이 32~44 라 반드시 날거나 쌓아야 합니다. 판 1 에서도 그랬으니 v79 회귀는 아닙니다. 해수면 기준으로 바꾸면 "산꼭대기에 서다" 가 됩니다 | 소 |

## 자문 14차가 남긴 것 (2026-09-08 · 실측 근거 있음)
> 열 건을 **전부** v83 에서 처리했습니다 — 지하 지도 깜빡임 · 지하 색조 · 폰 지도 조작(확대·표식) ·
> 지하 단면의 굴 어귀 · 시야 손잡이 · 찾기 과제 · 지나온 빈 자리 색 · 새 높이 · 천장 안내.

| 항목 | 내용 | 난이도 |
|---|---|:--:|
| **`WY` 를 96 으로** | v79 가 지형을 12칸 올려 **지을 하늘이 45칸에서 33칸(−27%)** 이 됐습니다. v83 이 "천장입니다" 라고 말하게는 했지만 여유 자체는 그대로입니다. `WY` 를 늘리려면 **저장 포맷과 청크 격자(`CY`)가 함께** 움직여야 합니다 — `liftLegacy` 가 48→64 를 이미 한 번 해냈으니 틀은 있습니다 | 대 |

## 자문 15차가 남긴 것 (2026-09-09 · 실측 근거 있음)
> 열 건을 **전부** v85 에서 처리했습니다 — 나무 밑 지도 오작동 · 지하 조명(v83 에서 이미) ·
> 폰 지도 드래그 · 폰 도움말 · 갱도 지도 자국 · 어귀 점 덩어리화 · 배율 저장 ·
> 지도 기호 안내 · 핫바 모양 글리프 · 갱도 꺾기.
> 남은 것은 아래 큰 것들뿐입니다.

## 자문 16차가 남긴 것 (2026-09-09 · 실측 근거 있음)
> v86 에서 **넷**을 처리했습니다 — 내가 판 굴에서 지도·소리가 안 켜지던 것(#1) ·
> 갱도 지도 자국이 세계마다 13~45%로 딴판이던 것(#6) · 어귀가 수직 우물이던 것(#5) ·
> 어귀의 82~84%가 지도에 안 찍히던 것(#3). 아래가 남았습니다.

| 항목 | 내용 | 난이도 |
|---|---|:--:|
| ~~**폰 — 지도·핫바·단추가 시점 제스처를 먹는다**~~ (v87 완료) | 시점 영역(x≥354)의 **38%가 캔버스가 아니다** — 단추 147×276 · 지도 105×123(화면 위 한가운데) · 핫바 402×46. `S.lookId` 는 **캔버스의 `touchstart`** 에서만 잡히므로 그 위에서 시작한 쓸기는 시점에 영영 안 간다. v85 가 지도에서 "움직이면 탭을 접는다" 까지는 했지만 **넘겨주지는 못했다**(`preventDefault` 와 이벤트 라우팅은 무관하다). `window` 의 `touchmove` 가 lookId 없는 터치를 시점으로 승격시키게 한다 | 소 |
| ~~**지하 지도 배율이 지상과 한 값**~~ (v87 완료) | 갱도를 통째로 걸어도 ×1 에서는 잉크가 **9~11%** (×4 면 35~65%). 굴에서 ×4 로 올리면 지상이 24칸만 보이고, 오르내릴 때마다 다시 맞춰야 한다. `mmzoom`/`mmzoomUnder` 두 칸으로 나누고 지하 기본을 ×2~4 로 | 소~중 |
| ~~**갱도가 17~32초**~~ (v87 완료) | 줄기 다섯이 **따로따로**고 갈래가 없다. 통로 총연장 79~146칸. 시드 333 에서는 5개 중 3개(214칸)가 **어떤 공기와도 안 이어진 밀봉 주머니**다. 토막마다 확률로 갈래를 하나 더 뻗고(재귀 1단), 마지막 토막은 가까운 동굴 공기로 뚫고 나가 자연 동굴과 잇는다 | 중 |
| ~~**끝방이 0~2개**~~ (v87 완료) | 문서상 5줄기 × 60% = 3개인데 실제 평균 1.25개. ① 5×5 상자가 통로보다 커서 자주 떨어지고 ② `lastLeg` 를 **계획한** 토막 수로 정하는데 `mineFits` 실패로 줄기가 끊기면 마지막 토막이 영영 안 온다. `laid` 로 실제 마지막 토막을 알고 있으니 거기에 방을 시도하고, 5×5 가 안 되면 3×3 으로 물러선다 | 소~중 |
| ~~**폰에서 표식에 이름을 못 붙인다**~~ (v87 완료) | 데스크톱은 `Shift+B`, 폰은 `toggleMark(false)` **고정**이라 이름 프롬프트로 가는 길이 없다. 열두 개가 전부 같은 금색 점이 된다 — **코드가 스스로 그 문제를 적어 뒀다**(`hud.js`) | 소 |
| ~~**핫바 모양 글리프가 둘 다 "작은 주황 네모"**~~ (v87 완료) | `▄`(채운 네모)와 `◱`(빈 네모)가 폰 9px 에서 구별이 안 되고, **고른 칸에는 아예 안 그린다.** 아이콘을 그 모양으로 그리거나(`drawIcon` 에 shape 를 넘긴다) 최소한 색을 가른다 | 소 |

## 자문 18차가 남긴 것 (2026-09-09 · 실측 근거 있음)
> **열 건 전부 처리됐습니다.** 일곱은 v91 에서 (폰 첫 화면의 「플레이」 단추 · 스폰 방향 ·
> 물속 무한 부양 · 폰 다시하기 · 폰 화면 표시 끄기 · 폰 시간/날씨 · 동물 따라오기),
> 남은 셋은 **v92** 에서 — 아래 표의 취소선 셋입니다.

| 항목 | 내용 | 난이도 |
|---|---|:--:|
| ~~**3인칭에 몸이 없다**~~ (v92 완료 — `src/body.js`) | `F5` 를 두 번 눌러 앞에서 보면 **거기에 아무것도 없습니다** — 조준선만 허공에 뜹니다. 씬에 플레이어 몸 메시가 없습니다. 게다가 `F6` 사진 모드는 첫 줄에서 `S.thirdPerson = 0` 을 박아 **사진 모드에서는 3인칭이 아예 불가능**합니다. 사진 모드·화면 담기·공유 링크가 다 있는데 **찍을 주인공만 없습니다.** 동물 몸통(`mobs.js` `makeMob` — 박스 3개 + 다리 4개)이 그대로 틀입니다 (1인칭에서는 숨기고 3인칭에서만) | 중 |
| ~~**비마다 천둥이 치고, 번개는 하늘 번쩍임뿐**~~ (v92 완료 — 비 셋 중 하나만 뇌우 · 볼트) | `updateStorm` 이 비면 **예외 없이** 7~23초마다 번쩍임+천둥을 냅니다 — 비 한 판(60~150초)에 평균 7번입니다. 그리고 번쩍임은 **하늘과 지형을 통째로 흰색으로 물들이는 것뿐**이라 어디 떨어졌는지 알 수 없습니다(볼트 지오메트리가 없습니다). 비가 곧 "10초마다 화면이 하얗게 튀는 것" 이라 짓는 데 방해가 됩니다. ① 비 시작 때 30% 확률로만 뇌우 ② `THREE.Line` 몇 마디로 볼트를 0.15초 그리고 `opts.firespread` 면 `ignite` | 소~중 |
| ~~**묘목이 한 종류뿐**~~ (v92 완료 — 묘목 세 종) | 종은 `saplingKind(x,z)` 가 정합니다 — 설원이면 가문비, 그 밖에서는 **자작 34% / 참나무 66% 무작위.** 초원 평지에 30개를 심어 기른 실측: 참나무 11 · 자작 2. **초원·사막에서 가문비를 심을 방법이 아예 없습니다.** 색은 열여섯 가지로 고르는데(v84) **나무는 주사위**라, 자작나무 가로수·가문비 산장 같은 건축 의도가 무너집니다. 「블록 추가 체크리스트」 13줄을 밟으면 됩니다(수집가 분모 73 → 76 확인) | 소~중 |

> **2026-09-10 현재 — "소" 도 "중" 도 남은 것이 없습니다.** 자문 12~18차가 낸 70건을 다 처리했습니다.
> 남은 것은 넷뿐입니다: **`WY` 를 96 으로**(대 · 저장 포맷+청크 격자) ·
> **면 병합**(중) · **무한 월드**(상) · **서바이벌·인벤토리**(상). 전부 유저 승인이 필요합니다.
> (권하는 순서는 **면 병합 → `WY` 96** 입니다 — 삼각형을 줄여 놓아야 세로로 키울 여유가 생깁니다.)

## 자문 19차가 남긴 것 (2026-09-10 · 실측 근거 있음)
> **열 건 중 아홉을 v93 에서 처리했습니다** — 폰 터치 단추 잘림 · 청크/동물 GPU 버퍼 누수 ·
> 잡음 버퍼 재사용과 빗소리 루프와 탭 숨김 · 새 세계의 횃불 카운터 · 좁은 창 화각 ·
> 계기판의 조준 좌표 · 불 9칸 라벨.
> 하나는 **틀린 계측**이었습니다(반쪽 문 — 실제 캐기 경로는 두 칸을 묶어 지웁니다).
> 아래 하나가 남았습니다.

| 항목 | 내용 | 난이도 |
|---|---|:--:|
| ~~**동물을 없앨 방법이 세상에 없다**~~ (v95 완료 — 유저가 「좌클릭으로 잡기」를 골랐습니다) | `src/mobs.js` 전체에 `splice`/`removeMob`/`kill` 이 하나도 없습니다. 좌클릭은 블록만 캐고, TNT 도 안 듣고, 명령창에도 없습니다. `canPlaceAt` 은 **동물이 선 칸에도 `true`** 라(플레이어 몸만 봅니다) 돌 두 칸으로 **산 채로 묻을 수 있고**, `strandedAt` 은 물·용암만 보므로 30초 뒤에도 그대로입니다. 96×96 섬에 24마리가 영구히 삽니다 — 거실 한가운데 양이 한 마리 서면 그 집은 평생 그렇습니다. 되돌리기도 못 씁니다(동물은 편집이 아닙니다). **「되돌릴 수 없는 것은 없습니다」(v72)의 유일한 예외가 동물입니다.** 마크는 크리에이티브에서 좌클릭 한 번이면 죽고, 엔티티가 있는 칸에는 블록이 안 놓입니다. **조작·파괴 규칙을 건드리므로 유저 승인이 필요합니다** | 중 |

## 자문 20차가 남긴 것 (2026-09-10 · 실측 근거 있음)
> **v92·v93 이 갓 넣은 코드를 실제로 오래 써 본 결과입니다. 열 건 전부 처리됐습니다** —
> 다섯은 v95 에서(3인칭 몸 조명 · 비행 자세 · 사진 모드의 토스트와 과제 팝업 ·
> `/give` · `/tp`), **남은 일곱은 v96 에서**. 아래 표는 취소선으로 남겨 둡니다.

| 항목 | 내용 | 난이도 |
|---|---|:--:|
| ~~**폰에 3인칭·사진 모드로 가는 길이 없다**~~ (v96 — 설정의 시점 알약 · 뽑기 길게 · 사진 모드 미니 바) | `F5`·`F6`·`F2` 가 `keydown` 안에만 있습니다 — 터치 단추 10개에도 메뉴에도 롱프레스에도 없습니다. **v92 가 만든 몸을 폰 사용자는 한 번도 못 봅니다.** 그리고 과제 40개 중 「사진사」는 폰에서 **영영 못 땁니다**(39/40 이 상한). v91 이 만든 롱프레스 틀(`bindHold("tb-menu", …)`)을 그대로 쓰면 됩니다 — 예: 비행 단추 길게 = 시점 전환, 메뉴에 「사진 모드」 한 줄 | 소~중 |
| ~~**가문비에 가문비 원목이 없다**~~ (v96 — `SPRUCE_LOG(80)`) | `growTick` 의 `logB = (kind === 1) ? BIRCH_LOG : LOG` — 가문비는 **잎만 다르고 줄기는 참나무**입니다(세계 생성기도 같습니다). v92 가 묘목 세 종을 넣은 명분이 "자작 가로수·가문비 산장" 이었는데 **산장 벽에 쓸 나무가 안 나옵니다.** 「블록 추가 체크리스트」 13줄 (`isLog`·`isFlammable`·아틀라스 타일 · 수집가 분모 확인) | 중 |
| ~~**자란 나무를 되돌리면 묘목이 안 돌아온다**~~ (v96) | `growTick` 이 `applyEdit(x,y,z,AIR,false,…)` 로 **묶음 밖에서** 묘목을 지웁니다. `Ctrl+Z` 하면 나무는 사라지는데 묘목도 같이 사라집니다 — GAMEPLAY 의 "되돌릴 수 없는 것은 없습니다" 표에 「나무가 자람」이 올라 있는데 그 약속이 묘목 한 칸만큼 샙니다 | 소 |
| ~~**번개 볼트가 1px 실 세 가닥이다**~~ (v96 — 어긋남을 카메라 거리에 비례) | WebGL 의 `linewidth` 가 1px 고정이라 `LineSegments` 로는 굵기가 안 나오고, 가닥 어긋남이 0.22·0.16 블록 **고정**이라 멀수록 셋이 겹쳐 한 줄이 됩니다. 실측 낙뢰 거리 18.7~55.5칸이니 **대부분은 실오라기 하나**로 보입니다. 볼트를 넣은 이유("어디 떨어졌는지 알게")가 반만 섰습니다. 오프셋을 카메라 거리에 비례시키거나 카메라를 향해 세운 얇은 쿼드 띠로. 낙뢰점 `boltAt` 은 이미 있습니다 | 소~중 |
| ~~**「화면 표시 크기」를 올리면 HUD 끼리 겹친다**~~ (v96 — 배율을 화면 높이로 자르고 힌트를 핫바 위로) | 힌트 띠가 핫바를 덮는 건 데스크톱 110%·폰 120% 부터고(**폰 기본이 이미 110%**), 폰 160% 부터는 **미니맵이 조준선을 삼킵니다**(844×390 에서 지도 336~508 × 14~215, 조준선 (413,186)). 180% 면 HUD 가 화면의 48.9%. v93 이 터치 단추에서 고친 "슬라이더의 위쪽 절반이 게임을 못 하게 만든다" 와 **똑같은 모양**입니다 — `applyTbtn()` 처럼 `--ui` 도 화면 높이로 자르면 됩니다 | 소 |
| ~~**폰 도움말이 v91·v92 를 못 따라왔다**~~ (v96) | 「되돌리기 길게 = 다시하기」·「메뉴 길게 = 화면 표시 끄기」가 **한 줄도 없습니다**(롱프레스는 우연히 발견될 수 없는 조작입니다). 「아직 키보드에서만 … 시간·날씨」는 v91 이 설정에 넣었으므로 **도움말이 자기가 넣은 기능을 "안 된다"고 말합니다.** 「블록 55종」은 실제 75, 「위가 6칸」은 실제 7칸, **묘목이 세 종이 됐다는 말이 없습니다** | 소 |
| ~~**곁가지 셋**~~ (v96 — 도구는 납작한 카드 · 훅에 BUCKET·SPRUCE_LOG · fillBar 초기화) | ① 1인칭 손의 양동이가 아직 회색 6면 큐브입니다(`hud.js drawIcon` 만 고쳐졌고 `hand.js makeBlockGeometry` 는 `isCross` 만 봅니다) — 3인칭 손도 같은 함수라 반블록·계단도 통짜로 나옵니다 ② 훅 `B` 에 `BUCKET` 이 없습니다 ③ **슬롯을 불러오기로 갈아탈 때 `S.fillBar`(담긴 양동이)가 안 비워집니다** — `newWorld` 경로에만 있고 `afterWorldSwap` 에는 없어서 세계 A 에서 뜬 용암이 세계 B 로 따라옵니다 | 소 |

## 자문 21차가 남긴 것 (2026-09-11 · 실측 근거 있음)
> **"이번엔 실제로 큰 건물을 지어 보라"** 고 이른 차수입니다.
> 열 건 중 아홉을 v97 에서 처리했습니다 — **내가 놓은 계단을 못 올라가던 것**(스무 차례 자문 동안
> 아무도 안 밟아 봤습니다) · 되돌리기가 144청크를 다시 굽던 것 · 「내 집」 600칸 상한 ·
> **영역 도구로 지은 것이 통계·과제에 안 잡히던 것** · 「다리」가 수면 바로 위에서 안 열리던 것 ·
> 붙여넣기가 빈칸을 건너뛰던 것 · 붙여넣기 상자를 못 떼던 것 · 목록 열린 채 `Tab` ·
> 청사진 덮어쓰기와 `/clone` 횟수. **나머지 하나와 곁가지 셋도 v98 에서 비웠습니다.**

| 항목 | 내용 | 난이도 |
|---|---|:--:|
| ~~**폰에서는 영역을 찍을 수는 있는데 그 영역으로 할 수 있는 일이 없다**~~ (v98 — 모양 단추 길게 = 영역 도구 바) | `bindTouchRegion()` 이 **두 손가락 탭으로 모서리를 찍게** 해 두어 초록 선택 상자도 뜨고 "영역 3×2×4 · 24칸" 토스트도 뜹니다. 그런데 **채우기·비우기·복사·붙여넣기·회전·거울·해제가 터치 단추 10개에도, 메뉴에도, 롱프레스에도 없습니다.** 명령창(`/`)도 `keydown` 안에만 있어 `/expand`·`/fill`·`/bp` 를 못 씁니다. **도움말은 "영역 도구는 키보드에서만" 이라는데 코드는 터치로 찍게 해 놓았습니다 — 둘 중 하나가 거짓말입니다.** 사거리도 데스크톱 `aimCell(64,true)` 대 터치 `raycast(6)` 라, 폰에서는 20×20 집터를 잡으려면 모서리마다 날아가야 합니다. v91 의 롱프레스 틀을 그대로 쓰면 됩니다 — 예: **모양 단추 길게 = 영역 메뉴** | 중 |
| ~~**곁가지 셋**~~ (v98 — 셋 다) | ① `Ctrl+F` 로 **제 발밑을 채우면 산 채로 묻힙니다**(120프레임을 걸어도 좌표가 그대로) — v95 가 `/tp` 에서 고친 "몸이 들어갈 두 칸을 위로 훑는다" 를 `fillSelection`·`pasteClip` 끝에도 부르면 됩니다 ② **고른 영역의 크기가 토스트 2.5초 말고는 어디에도 안 남습니다** — `selectionText()` 가 이미 있으니 F3 계기판에 한 줄 ③ 거울(`Ctrl+Shift+R`)이 **언제나 월드 X 축**이라, Z 로 뻗은 건물의 반대쪽 날개를 만들려면 `Ctrl+R` 두 번 + 거울인데 그 조합이 어디에도 안 적혀 있습니다 | 소 |

## 자문 22차가 남긴 것 (2026-09-11 · 실측 근거 있음)
> **"한 세계에서 한 시간을 논다"** 고 이른 차수입니다. 문제가 전부 **성능이 아니라 지속성** 쪽이었습니다
> (자동 플레이 140초 동안 힙 30.2→31.1MB · 청크 288 고정 · 오류 0 — 긴 판에서 새는 곳은 없습니다).
> 넷을 v99 에서 처리했습니다 — 마지막 슬롯 기억 · 탐험도 저장 대상 · **ESC 가 세계를 멈춘다** ·
> 튜토리얼 힌트 접기. **나머지는 v100·v101 에서 — 자문 22차는 열 건 전부 처리됐습니다.**

| 항목 | 내용 | 난이도 |
|---|---|:--:|
| ~~**되돌려도 「사람이 손댄 칸」이 안 돌아온다**~~ (v100 — 묶음에 `fromT` 한 칸) | `applyEdit` 이 `markTouched()` 를 찍는데 `applyCell`/`applyCellAt` 이 그걸 **안 되돌립니다.** `touched` 는 저장(`tc`)에 실리니 **영구**입니다. 결과 — 되돌린 자리에 **눈이 영영 안 쌓이고**(`loop.js` 눈 쌓임이 `isTouched` 로 거릅니다) **흙이 영영 풀이 안 되며**(`fluids.js` 잔디 번짐), `findMine`/`findHut`·`roofDepth` 도 어긋납니다. 설원에 탑 자리를 세 번 잡아 봤다 지우면 눈이 와도 **그 세 자국만 회색**으로 남고 원인을 알 길이 없습니다. **v72 「되돌릴 수 없는 것은 없습니다」에 뚫린 유일한 구멍**입니다 — 문서가 표까지 그려 놓고 이 한 비트만 빠졌습니다. `makeBatch` 에 `fromT: Uint8Array` 한 칸 + `world.js` 에 `clearTouched()` | 소~중 |
| ~~**같은 세계를 두 탭에서 열 수 있고, 아무 말도 없이 하나가 지워진다**~~ (v101 — 탭 잠금 + 카드 경고) | 세션 잠금이 없습니다. 탭 A(500칸 건축) → 탭 B 에서 새 세계 → 탭 A 가 자동 저장하면 **탭 B 의 세계는 `.bak` 한 벌만 남고 다음 저장에 덮입니다.** 경고 토스트는 `""` 입니다. 한 시간 놀다 탭을 안 닫고 **내일 새 탭에서 또 여는 것**이 정확히 이 모양이고, 백그라운드 탭도 `S.active` 가 살아 있어 20초 자동 저장이 그대로 돕니다. 세이브는 localStorage 한 벌뿐이라 복구가 없습니다. `blockyard.lock.<slot>` 에 5초 심장박동 + 부팅 때 확인 | 중 |
| ~~**클라우드가 슬롯 셋을 「main」 한 세계로 뭉갠다**~~ (v100 — 슬롯마다 이름) | 세계 이름이 `blockyard.cloud.world` **전역 한 칸**이라 슬롯과 무관합니다. 슬롯 1 올리고 슬롯 2 올리면 같은 `main.json` 판 2 가 되고 **conflict 도 안 뜹니다**(같은 기기라 baseRev 가 따라 올라갑니다). 내려받기는 "지금 슬롯" 을 덮으므로 슬롯 1 세계가 슬롯 3에 앉습니다. 「기기 사이 이어하기」가 조용히 하나만 남깁니다. `cloud.js worldName()` 기본값을 `"slot" + S.slot` 으로 (가장 작은 고침은 `pushWorld` 에서 시드가 다르면 되묻기) | 중 |
| ~~**「수집가」 76종인데 무엇이 남았는지 알 길이 없다**~~ (v100 — 과제에 진행도) | 과제 줄에 숫자가 없습니다(`• 수집가 · 모든 종류의 블록을 놓아본다`). 개수는 `S.placedKinds` 로 이미 세고 ESC › 통계에 `0 / 76` 이 있는데 **과제 쪽과 블록 목록(E) 쪽 어디에도** 안 나옵니다. 76종 중 **32종이 색만 다른 것**(양털 16 + 색 카펫 16)이라 73번째를 찾으려면 목록을 76번 훑어야 합니다. `refreshAchList` 에 진행도 한 줄 + `pickBtns` 에 놓아 본 표시 점 | 소 |
| ~~**저장이 안 되기 시작해도 경고는 한 판에 1.6초 한 번**~~ (v100 — 계기판에 「⚠ 저장 안 됨」) | `QuotaExceededError` 를 던지게 하고 자동 저장 120회(40분치)를 돌리니 **120번 다 실패**했는데 경고는 **한 번 · 1.6초**였고(`S.saveWarned` 가 계속 true) 그 뒤 화면 어디에도 표시가 없습니다. 청사진이 세계 저장과 같은 localStorage 를 나눠 쓰므로 실제로 일어납니다. `saveWarned` 를 "한 번만 말한다" 가 아니라 **상태**로 쓰고 계기판에 빨간 「저장 안 됨」 + 「파일로 내보내기」로 가는 길 | 소 |
| ~~**표식 12개 상한인데 멀리 있는 표식을 지울 길이 없다**~~ (v100 — `/marks del`) | `toggleMark()` 는 지울 때 `markHere()`(내 자리 3칸 안)만 봅니다. 12개가 차면 토스트뿐이고 `/marks` 는 목록만 보여 줍니다 — **명령 15개 중 표식을 지우는 것이 하나도 없습니다.** 13번째를 찍으려면 쓸모없어진 표식까지 날아가야 합니다. 한 시간이면 굴 어귀·갱도·집·광맥으로 12개는 금방 찹니다. `/marks del <번호>` 한 줄 + 폰은 지도 롱프레스에 「지우기」 | 소 |
| ~~**「지도장이」 설명이 거짓말이다**~~ (v100 — 문구를 사실에 맞춤) | `"섬의 8할을 **걸어서** 지도에 밝힌다"` 인데 `markSeen` 은 비행 여부를 안 봅니다 — 12m 상공을 한 바퀴 날면 열립니다. 문구를 고치거나 비행 중 반경을 줄입니다 | 소 |

## 자문 23차가 남긴 것 (2026-09-11 · 실측 근거 있음 · 스크린샷 40여 장)
> **"남의 마크 사진과 견줘 무엇이 빠져 보이는지"** 를 물은 차수입니다.
> 다섯을 v103 에서 처리했습니다 — 동물이 밤에도 정오 색이던 것 · 높은 구름층이 시간을
> 무시하던 것 · 사진 모드에 손이 남던 것 · 고스트가 조명을 안 받던 것 ·
> 노을에 별이 먼저 뜨고 해가 흐려지던 것 · 반딧불이가 바다 위에 뜨던 것.
> 아래가 남았습니다.

| 항목 | 내용 | 난이도 |
|---|---|:--:|
| ~~**세계 물과 바깥 바다가 한 줄에서 색이 갈린다**~~ (v104 — vox 와 같은 식으로) | 세계 안에서 바다를 내려다보면 **한 픽셀 줄**에서 밝기가 129 → 74 (−43%) 로 꺾입니다. 노을에는 색상까지 갈립니다(세계 물 보랏빛 (52,52,67) · 바깥 바다 차가운 (24,49,75)). 세계 물은 vox 셰이더(하늘빛 × 면 그늘 × `uNight` × 안개)를 타는데 바깥 바다는 `vec3(0.13,0.30,0.47) * (0.35+0.65*uDay)` 뿐이라 그렇습니다. **이 판은 "세계 끝에서 물이 일직선으로 잘리면 디오라마가 된다" 는 이유로 넣은 것인데, 지금은 그 판 자체가 일직선을 하나 더 긋고 있습니다** — 섬 사진은 거의 다 이 줄이 화면을 가로지릅니다. `src/scene.js` 바깥 바다 셰이더에 `litness`·`uNight`·안개를 vox 와 같은 식으로 | 소~중 |
| ~~**물속에서 하늘·구름·별이 그대로 보인다**~~ (v104 — setSkyHidden) | 지형 안개는 물속에서 `near 0.1 · far 22` 로 제대로 걸리는데 **지형만** 그렇습니다 — 하늘 구·구름 두 층·해·달·별이 전부 `fog:false` 이고 하늘 셰이더에는 안개 항이 없습니다. 수심 3칸에서 구름 슬래브가 또렷하고, 밤이면 별밭이 물 위로 비칩니다. 마크는 물속에서 하늘이 완전히 사라져 **"물속은 다른 세계"** 라는 감각이 거기서 나옵니다. `src/loop.js` 의 물속 갈래에서 하늘 재질들의 가시성을 같이 끄는 것이 가장 작은 고침입니다 | 중 |
| ~~**세계 끝 격자가 안개를 안 받는다**~~ (v104 — 작은 셰이더로) | `edgeMat` 이 `fog:false` 라, 물속에서 지형은 22칸에서 다 지워지는데 **격자만 제 불투명도를 다 들고** 남아 화면 오른쪽 절반이 **라임색 삼각 격자**로 덮입니다(디버그 와이어프레임처럼 보입니다). `PlaneGeometry(96,64,12,12)` 라 대각선도 그대로 보입니다. 게다가 격자는 8칸 안에서만 뜨므로 **바닷가로 헤엄쳐 나갈 때만** 나타나는데 그때가 가장 어둡습니다. 마크의 월드 보더는 세로 줄무늬가 흐르는 청록 벽입니다 | 소 |
| **섬에 세로가 없다** | 일곱 시드의 최고봉이 **해발 6~14칸**, 뭍 평균 해발 3~5칸입니다 — 가장 높은 자연 지형이 3층 건물 높이고, 어느 시드로 찍어도 **동심원 계단 모양 모래 팬케이크**가 나옵니다. 실루엣에 절벽도 봉우리도 없습니다. 앞 차수들이 빛·물·하늘·UI 를 다 다듬었는데 **찍을 대상 자체가 평평합니다.** 두 갈래 — ① **바위 노두**(3~7칸 돌덩이 열댓 개)를 기존 지표 위에 얹기: `heightMap` 을 안 건드리므로 공유한 시드 링크가 그대로 열립니다(`buildHuts` 틀 재사용 · **권함**) ② 기복 자체를 키우기: 모든 시드의 땅이 달라지므로 **판 3** 이 필요하고 유저 승인이 듭니다 | 중 / 대 |
| **곁가지 셋** | ① 과제 팝업(`#achpop`)이 화면 위 한가운데라 `E` 를 연 순간 「블록」 제목과 필터 줄을 덮습니다(마크의 토스트는 늘 오른쪽 위) ② 잎이 빛은 완전히 막는데 타일에 구멍이 있어 하늘이 파란 네모로 비치고, **타일마다 같은 구멍 무늬**라 캐노피 전체에 같은 점이 반복됩니다 ③ 시작 화면 카드가 1024폭에서 화면의 67% 를 덮어 첫 인상이 "어두운 카드" 입니다 | 소 |

## 건드리지 말 것 (자문 의견 — 마크보다 나은 부분)
- 놓을 자리 고스트 미리보기 · `Ctrl+Z` 되돌리기(대량 편집도 한 번에)
- 생성 시점에 모래/자갈 침강을 미리 처리
- `G` 하나로 풀블록/반블록/계단 전환 · 지하에서 미니맵이 단면도로 전환
- 영역 도구(채우기·복사·붙여넣기·청사진) — 마크 바닐라에는 없는 건축 도구
