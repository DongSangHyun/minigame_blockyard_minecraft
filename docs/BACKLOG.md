# BACKLOG — 아직 안 한 것

> v5~v24 에 걸쳐 **자문 5회 · 개선 120건 이상**이 반영됐습니다.
> 지난 목록(B·C 계열)은 전부 처리되어 아래로 갈아 끼웠습니다. 자세한 내역은 `memory.md`.

## 다음에 볼 만한 것
| 항목 | 내용 | 난이도 |
|---|---|:--:|
| **무한 월드 / 청크 스트리밍** | `WX·WZ` 가 96 고정이라 끝에 닿으면 격자벽을 만난다. 배열 재할당과 저장 포맷이 함께 움직여야 한다 | 상 |
| **면 병합(greedy meshing)** | 지금도 여유가 크지만(전체 메싱 34ms · 삼각형 69k), 세계를 키우려면 필요하다 | 중 |
| **서바이벌 모드** | 체력·낙하 피해·용암 피해·밤 위협. 지금은 완전한 크리에이티브다 | 상 |
| **인벤토리·채집·크래프팅** | 서바이벌과 한 묶음 | 상 |



## 자문 5차가 남긴 것 (2026-09-06 · 실측 근거 있음)
| 항목 | 내용 | 난이도 |
|---|---|:--:|
| **건축 과제 5개** | 과제 30개가 전부 "기능을 만져 봤나" 다 — **지은 것을 보는 과제가 0개**. 방(`touched` 로 둘러싸인 27칸 밀폐 + 문) · 탑(20칸 기둥) · 다리(물 위 20칸) · 갱도(y≤8 에 200칸 + 횃불 10) · 색칠(양털 8색). 도움말 과제 탭에 "다음에 해 볼 것 3개" 도 | 중 |
| **붙여넣기 미리보기·회전·거울** | `Ctrl+V` 가 눈을 감고 박힌다. 고스트·선택상자 재료는 이미 있다. `Ctrl+R` 90° 회전(계단·벽·문 방향도 함께), `Ctrl+Shift+R` 거울, `/clone` 에 반복 횟수 | 중 |
| **청사진 UI·용량** | `bp` 가 명령창에만 있고 목록이 없다. `JSON.stringify(Array)` 라 15,376칸이 63KB — `encodeArrB64` 를 쓰면 몇 KB | 소 |
| **`/sel <±dx> <±dy> <±dz>`** | 고른 상자를 늘리고 줄인다. 6칸 사거리라 30칸 영역은 두 모서리로 날아가야 한다 | 소 |

## 건드리지 말 것 (자문 의견 — 마크보다 나은 부분)
- 놓을 자리 고스트 미리보기 · `Ctrl+Z` 되돌리기(대량 편집도 한 번에)
- 생성 시점에 모래/자갈 침강을 미리 처리
- `G` 하나로 풀블록/반블록/계단 전환 · 지하에서 미니맵이 단면도로 전환
- 영역 도구(채우기·복사·붙여넣기·청사진) — 마크 바닐라에는 없는 건축 도구
