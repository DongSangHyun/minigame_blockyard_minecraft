# BACKLOG — 아직 안 한 것

고인물 자문 24건 중 **1차 10건(v5) · 2차·3차 8건(v6)** 은 반영 완료입니다 (`memory.md` 참고).
아래는 남은 것과, 작업하며 새로 드러난 것들입니다.

## 남은 자문 항목
| # | 항목 | 지금 | 난이도 |
|---:|---|---|:--:|
| B2 | **원목 축 회전** | `TILES[LOG]` 고정 — 어떻게 놓아도 나이테가 위. 기둥·보 건축의 기본인데 빠져 있다 | 중 |
| B9 | **무한 월드 / 보이지 않는 벽** | `WY` 는 64로 올렸지만 `WX·WZ` 는 96 고정. 끝에 닿으면 좌표 클램프로 투명 벽 | 상 (청크 스트리밍) |
| B12 | **크리에이티브 관용구** | 스페이스 더블탭 비행 · `F1` HUD 숨기기 · `F5` 3인칭이 없다 | 하 (F5만 중) |
| B11 | **물속 산소** | 무한히 숨 쉰다. (수면 부력은 v6 에서 처리) | 중 (HUD 필요) |
| B13 | **연속 배치 드래그** | 우클릭 유지 시 0.2초 간격. 마크는 드래그로 줄을 긋는다 | 하 |
| B10b | **각진 해와 달 · 달 위상** | 방사형 그라디언트 원반. 나머지 텍스처는 전부 16×16 도트인데 여기만 매끈하다 | 하 (위상은 중) |

## 작업하며 새로 드러난 것
| 항목 | 내용 | 난이도 |
|---|---|:--:|
| **바이옴 균형** | 바이옴 노이즈 임계값(`t < 0.36`)이 설원을 너무 많이 만든다. 시드 5개 중 3개가 육지의 70%가 설원 — 풀·꽃을 넣어도 안 보이는 세계가 나온다 | 하 (임계값 조정 + 시드별 실측 필요) |
| **지표 구멍** | 동굴 노이즈가 지표를 뚫어 해수면 위 공기 칸의 0.9%가 빛이 안 닿는 주머니가 된다. 멀리서 보면 검은 반점처럼 보인다 | 중 |
| **물 소스 제거 동선** | 물은 조준되지 않아(조준 편의) 근원을 없애려면 그 자리에 블록을 놓거나 `Ctrl+Z` 해야 한다. 직관적이지 않다 | 하 |
| **벽 횃불** | 횃불이 바닥에만 놓인다. 마크는 벽에 붙인다 | 중 |
| **모듈 분리** | 게임 코드를 ES 모듈로 나누는 작업 (사용자 승인) | 진행 중 |

## 건드리지 말 것 (자문 의견 — 마크보다 나은 부분)
- 놓을 자리 고스트 미리보기 · `Ctrl+Z` 240단계 되돌리기
- 생성 시점에 모래/자갈 침강을 미리 처리 (동굴 천장에 뜬 모래가 없다)
- `G` 하나로 풀블록/반블록/계단 전환
- 지하에서 미니맵이 자동으로 단면도로 전환
