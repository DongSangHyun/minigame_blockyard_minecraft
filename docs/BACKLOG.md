# BACKLOG — 아직 안 한 것

> v5~v24 에 걸쳐 **자문 5회 · 개선 120건 이상**이 반영됐습니다.
> 지난 목록(B·C 계열)은 전부 처리되어 아래로 갈아 끼웠습니다. 자세한 내역은 `memory.md`.

## 다음에 볼 만한 것
| 항목 | 내용 | 난이도 |
|---|---|:--:|
| **무한 월드 / 청크 스트리밍** | `WX·WZ` 가 96 고정이라 끝에 닿으면 격자벽을 만난다. 배열 재할당과 저장 포맷이 함께 움직여야 한다 | 상 |
| **면 병합(greedy meshing)** | 지금도 여유가 크지만(전체 메싱 34ms · 삼각형 69k), 세계를 키우려면 필요하다 | 중 |
| **서바이벌 모드** | 체력·낙하 피해·용암 피해·밤 위협. 지금은 완전한 크리에이티브다 | 상 |
| **인벤토리·채집·크래프팅** | 서바이벌과 한 묶음 | 상 |



## 자문 5~7차가 남긴 것
**전부 처리됐습니다.** 마지막으로 닫힌 것 —
5차 청사진(용량 v56 · 목록 UI v63) · 7차 묘목(v62). 자세한 내역은 `memory.md`.

## 자문 8차가 남긴 것 (2026-09-07 · 유저가 이번에는 넘긴 것)
| 항목 | 내용 | 난이도 |
|---|---|:--:|
| **물·용암 지우개(양동이)** | 물·용암은 **놓기는 되는데 좌클릭으로 지울 수가 없는 유일한 블록**이다(레이캐스트가 액체를 건너뛴다 — 물속에서 바닥을 캐야 하므로 그 자체는 맞다). 지우려면 `/fill 공기` 나 영역 비우기까지 가야 해서 결국 물을 안 쓰게 된다. 부싯돌이 이미 "놓는 블록이 아닌 손에 쥐는 물건"(`ITEMS`)의 선례다 — `src/blocks.js` 의 `ITEMS`, `src/mine.js` 의 `tryInteract` | 중 |
| **핫바 칸마다 모양 기억** | `G` 로 고른 모양(`S.shapeMode`)이 **전역 하나**라, 3번 칸(계단)에서 2번 칸(유리)으로 바꾸면 유리도 계단으로 나온다. 지붕 한 채에 `G` 를 스무 번 친다. 길이 10짜리 배열로 바꾸고 `selectSlot` 에서 읽는다. **핫바 2쪽(`S.barAlt`)과 저장(`bar`/`bar2`)에 같이 실어야 하고, 세계 전환 두 경로도 함께** (v61 곁가지의 그 함정) | 소~중 |

## 자문 10차가 남긴 것 (2026-09-07 · 실측 근거 있음)
> 전부 처리됐습니다 — 재굽는 범위 v70(바닥 한 장 144→8청크 · 힙 54.7→31.2MB) ·
> 시작 화면 v71("이어서 짓던 곳") · 불을 되돌리기에 담기 v72.

| 항목 | 내용 | 난이도 |
|---|---|:--:|

## 자문 11차가 남긴 것 (2026-09-08 · 실측 근거 있음)
> **바다 장식**(v75)과 **오두막 다양화**(v76)는 처리했습니다.
> 둘 다 **별도 난수 줄기**를 써서 기존 시드의 땅이 안 밀립니다 — 시험이 그것을 못 박습니다.

| 항목 | 내용 | 난이도 |
|---|---|:--:|
| **지하 구조물** | 동굴 공기가 **8,586~24,846칸**인데 구조물이 **아예 없습니다**. 지상은 v76 에서 오두막을 다양하게 했지만, 굴을 파고 내려가면 볼 것이 광맥뿐입니다. 버려진 갱도(횃불 줄·나무 지지대·조약돌 바닥)나 작은 방이면 `src/world.js` 의 `buildHuts` 와 같은 틀을 쓸 수 있습니다 — **별도 난수 줄기**로 (v75·v76 처럼) | 중 |

| 항목 | 내용 | 난이도 |
|---|---|:--:|
| **실내 소품 더** | 책장·카펫은 v74 에서 들어갔습니다. 다음은 **색 카펫**(양털 16색과 짝) · 화분 · 액자. 간판은 글자 렌더가 붙어 큽니다 | 소~중 |

## 자문 12차가 남긴 것 (2026-09-08 · 실측 근거 있음)
> 열 건 중 **아홉**을 v78 에서 처리했습니다 — 물·용암 되돌리기 · 스텝업 눈높이 ·
> 폰 미니맵 겹침 · 번식 하트 · 폰 픽블록/핫바 2쪽 · 폰 도움말 · 놓기 반복 간격 ·
> 동물 무리 스폰 · 눈 쌓임. 남은 한 건은 아래입니다.

| 항목 | 내용 | 난이도 |
|---|---|:--:|
| **지하를 깊게** | 세계는 64칸인데 **지표에서 기반암까지 13.5~15.3칸뿐**입니다(최고점 위로는 30~38칸이 늘 빈 하늘). 석탄 중앙값 y=4, 철·금·다이아 y=3~4 — **"내려갈수록 좋은 것"** 이라는 사다리가 없습니다. `WY` 도 저장 포맷도 안 건드리고 **지형 기준 높이만 내려서** 같은 64칸 안에서 지하를 두 배로 만들 수 있습니다. 다만 **모든 시드의 땅이 달라지므로** v75·v76 의 난수 줄기 규칙으로는 못 덮습니다 — 새 세계에만 적용하고 예전 저장은 그대로 열리는 갈래가 필요합니다. **유저 승인이 필요한 항목입니다.** 백로그의 "무한 월드" 는 가로(96 고정) 이야기라 이것과 별개입니다. 지하 구조물을 넣더라도 **넣을 자리가 14칸뿐이라 이게 먼저입니다** | 중 |

## 건드리지 말 것 (자문 의견 — 마크보다 나은 부분)
- 놓을 자리 고스트 미리보기 · `Ctrl+Z` 되돌리기(대량 편집도 한 번에)
- 생성 시점에 모래/자갈 침강을 미리 처리
- `G` 하나로 풀블록/반블록/계단 전환 · 지하에서 미니맵이 단면도로 전환
- 영역 도구(채우기·복사·붙여넣기·청사진) — 마크 바닐라에는 없는 건축 도구
