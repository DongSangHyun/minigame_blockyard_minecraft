# HANDOFF — 세션 인계

> **새 세션은 `CLAUDE.md` → `docs/INDEX.md` → 이 파일** 순으로 읽으면 이어집니다.
> 이 문서는 `node tools/handoff.mjs` 가 다시 씁니다. 숫자는 손으로 고치지 마세요.
> 사람이 채우는 자리는 `<!-- keep:… -->` 로 감싸 두었고 도구가 덮지 않습니다.

## 지금 상태
| | |
|---|---|
| 마지막 판 | **v80** — 자문 13차 — v79 가 지상에 남긴 대가를 치렀다 (v80) |
| 마지막 커밋 | 2026-09-08 19:29 |
| 회귀 시험 | **297항목** + 폰 화면 9항목 (러너는 오류 검사 2개를 더해 308 로 찍습니다) |
| 전체 10회 | 약 5~7분 — 커밋 전 게이트입니다 |
| 코드 | 모듈 28개 · 10,326줄 |
| 저장 포맷 | v5 |
| 백로그 | 11건 |
| 공개 주소 | https://dongsanghyun.github.io/minigame_blockyard_minecraft/ |

## 이미 처리된 것 (자문을 띄울 때 **이 목록을 프롬프트에 넣으세요** — 중복 제안 방지)
- 자문 13차 — v79 가 지상에 남긴 대가를 치렀다
- 지하를 깊게 — 지형을 12칸 위로 밀었다
- 자문 12차 — 되돌리기가 물을 안 잡던 것부터
- 세션 인계 체계 — "새세션 준비해줘" / "새세션 시작"
- 오두막이 채마다 달라졌다
- 바다에 볼 것을 두다
- 실내 소품 — 책장과 카펫
- 자문 11차 — 손이 지루하던 자리와 귀가 비어 있던 자리
- 번져서 탄 집이 Ctrl+Z 한 번에 돌아온다
- 이어서 짓던 곳
- 바닥 한 장에 세계 전체를 다시 굽던 것
- 자문 10차 — 되돌리기가 거짓말하던 것부터
- 부팅 대기 조건이 사실은 아무것도 안 기다리고 있었다
- 배포본이 진짜 켜지는지 보는 도구
- 부팅이 실패하면 화면이 말하게
- tidy-imports 가 게임을 부순 것을 되돌리고 안전장치를 달았다
- 세계 이름과 돌아갈 수 있는 표식
- 터치 튜토리얼 · 날씨 저장 · 되돌리기 칸 상한
- 자문 9차 — 폰이 1급이 아니었다
- 캐기 속도를 실제 조작으로 잰다

## 마지막 세션이 한 일 (새것부터)
- 자문 13차 — v79 가 지상에 남긴 대가를 치렀다 (v80)
- 인계 문서에 v78·v79 반영
- 지하를 깊게 — 지형을 12칸 위로 밀었다 (v79)
- 자문 12차 — 되돌리기가 물을 안 잡던 것부터 (v78)
- 인계 문서에 v77 반영
- 세션 인계 체계 — "새세션 준비해줘" / "새세션 시작" (v77)
- 오두막이 채마다 달라졌다 (v76)
- 바다에 볼 것을 두다 (v75)
- 실내 소품 — 책장과 카펫 (v74)
- 자문 11차 — 손이 지루하던 자리와 귀가 비어 있던 자리 (v73)
- 번져서 탄 집이 Ctrl+Z 한 번에 돌아온다 (v72)
- 이어서 짓던 곳 (v71)

## 진행 중이던 것
<!-- keep:wip -->
**없습니다.** v78·v79·v80 을 전부 커밋·push·배포 확인(`node tools/check-live.mjs`)했고
워킹트리가 깨끗합니다. 전 항목 ×10회 통과 상태입니다.

이번 세션은 **자문 12차(10건)와 13차(10건)를 다 돌았습니다** —
v78 은 12차 9건, v79 는 12차의 마지막 1건(지하를 깊게), v80 은 13차 9.5건입니다.

**v79·v80 을 이어받을 때 반드시 알아야 할 것**
- **해수면(`SEA`)은 세계마다 다릅니다.** 판 1 = 11(v78 까지) · 판 2 = 23(v79 부터).
  `src/dims.js` 의 `export var` 라이브 바인딩이고 `setGen(g)` 만이 바꿉니다.
- **그 축을 읽는 곳을 전부 세세요.** v79 는 import 25곳만 확인했고,
  **파생값**(수평선·구름·시작 화면 카메라·미리보기 산 기준·갱도 과제·동굴 울림·용암 띠·
  광맥 씨앗)을 안 세어서 v80 에서 여덟 개를 되돌려야 했습니다.
  상수·게터·**오브젝트 속성**(`m.position.y = f()`) 셋 다 봐야 합니다.
- 판마다 다른 값은 함수가 정본입니다 — `oreCeil()` · `lavaTop()` · `seaLift()`.
  **시험이 그 함수를 읽습니다.** 숫자를 두 군데 적지 마세요.
- 좌표를 손으로 적는 시험은 **`B.SEA` 기준으로** 쓰세요.
- `generate(seed, gen)` 으로 판을 지정할 수 있습니다. 시험이 두 판을 견줍니다.
<!-- /keep:wip -->

## 백로그 전체 (`docs/BACKLOG.md` 에서 뽑음)
- **무한 월드 / 청크 스트리밍** (상) — WX·WZ 가 96 고정이라 끝에 닿으면 격자벽을 만난다. 배열 재할당과 저장 포맷이 함께 움직여야 한다…
- **면 병합(greedy meshing)** (중) — 지금도 여유가 크지만(전체 메싱 **52~65ms** — v79 로 지하가 두 배가 되며 34~45ms 에서 올랐다), 세계를 키우려면 필요하다. 전체 메싱은 세…
- **서바이벌 모드** (상) — 체력·낙하 피해·용암 피해·밤 위협. 지금은 완전한 크리에이티브다…
- **인벤토리·채집·크래프팅** (상) — 서바이벌과 한 묶음…
- **물·용암 지우개(양동이)** (중) — 물·용암은 **놓기는 되는데 좌클릭으로 지울 수가 없는 유일한 블록**이다(레이캐스트가 액체를 건너뛴다 — 물속에서 바닥을 캐야 하므로 그 자체는 맞다). 지우려…
- **핫바 칸마다 모양 기억** (소~중) — G 로 고른 모양(S.shapeMode)이 **전역 하나**라, 3번 칸(계단)에서 2번 칸(유리)으로 바꾸면 유리도 계단으로 나온다. 지붕 한 채에 G 를 스무…
- **지하 구조물** (중) — (v79 로 동굴 공기가 **34,000~58,000칸**이 되어 넣을 자리가 넉넉해졌습니다) 예전 진단: 동굴 공기가 **8,586~24,846칸**인데 구조물…
- **실내 소품 더** (소~중) — 책장·카펫은 v74 에서 들어갔습니다. 다음은 **색 카펫**(양털 16색과 짝) · 화분 · 액자. 간판은 글자 렌더가 붙어 큽니다…
- **지하 지도를 층으로 나누기** (중) — seenMap 의 지하 비트가 SEEN_UNDER **한 장**이라, 판 1 에서 13층이 나눠 쓰던 것을 판 2 에서는 **26층**이 나눠 씁니다. 위층 굴을…
- **굴 어귀를 더 뚫기** (중) — 굴 1,000칸당 어귀가 판 1 의 14~31개에서 판 2 에서 2.4~5.2개로 떨어졌습니다(뭍 기둥 대비로는 9~13% → 5~9.5%). 지표 카브 문턱을 …
- **`high` 과제가 산으로는 안 열린다** (소) — y > 50 인데 최고봉이 32~44 라 반드시 날거나 쌓아야 합니다. 판 1 에서도 그랬으니 v79 회귀는 아닙니다. 해수면 기준으로 바꾸면 "산꼭대기에 서다"…

## 다음에 할 후보
<!-- keep:next -->
자문 5~13차가 낸 것은 **거의 전부 처리**됐습니다. 남은 것은 `docs/BACKLOG.md` 에 있고, 요약하면:

1. **지하 지도를 층으로 나누기** (중) — `SEEN_UNDER` **한 장**이 26층을 나눠 씁니다.
   위층 굴을 밝히고 아래층에 내려가면 그 칸들이 "밝혀짐" 인 채 통돌 회색으로 그려집니다.
   `mm` 은 선택 필드라 저장 버전은 안 올려도 되지만, **칸당 값의 뜻이 바뀌므로
   예전 저장을 읽는 갈래가 필요합니다.**
2. **굴 어귀를 더 뚫기** (중) — 굴 1,000칸당 어귀가 14~31개에서 2.4~5.2개로 떨어졌습니다.
   **문턱(0.66)이 아니라 `carvedBelow` 조건이 병목**입니다 (v80 에서 재 봤습니다).
3. **지하 구조물 — 버려진 갱도** (중) — 동굴 공기가 34,000~58,000칸인데 구조물은 0개입니다.
   자문 13차는 "길을 잃는 문제가 볼 것이 없는 문제보다 크다" 며 1·2번을 먼저 꼽았습니다.
   설계 초안은 `memory.md` v80 항목 위쪽과 이번 세션 스크래치패드에 있습니다 —
   통로 2×3 · 조약돌 바닥 · 울타리 기둥과 원목 들보 · 횃불 · 작은 방.
   **물·용암을 안 뚫고 지표를 안 뚫는 것**이 지켜야 할 두 가지고, **별도 난수 줄기**를 씁니다.
4. **색 카펫·화분·액자** (소~중) · **핫바 칸마다 모양 기억** (소~중) ·
   **물·용암 지우개(양동이)** (중) · **`high` 과제를 해수면 기준으로** (소)
5. **면 병합** · **무한 월드** · **서바이벌·인벤토리** (상 — 큰 결심이 필요)

`작업해줘` 는 §4.5 의 다섯 단계(자문 10건 → 구현 → 부분 시험 → 전체 10회 → 보고)로 돕니다.
자문은 `general-purpose` 서브에이전트에 **"10년차 마크 고인물"** 페르소나로 띄우고,
**"이미 처리된 것" 목록을 프롬프트에 넣어야** 중복 제안이 안 나옵니다.
자문에게 **"v79 로 세계가 세로로 두 배가 됐으니 옛 수치를 인용하지 말고 다시 재라"** 고
일러 두면 좋습니다 — 13차에서 그 한 줄이 회귀 여덟 개를 찾아냈습니다.
<!-- /keep:next -->

## 이 세션에서 데인 자리 (다음 세션이 또 밟지 않게)
<!-- keep:traps -->
**실제로 사고가 났던** 자리입니다. 자세한 건 `memory.md` 의 해당 판 항목.

| 함정 | 무엇 |
|---|---|
| **큰 축을 옮기면 그 축을 읽는 곳을 전부 센다** | v79 가 해수면을 11→23 으로 옮기며 import 25곳은 확인했는데 **파생값**은 안 셌습니다 — 수평선·구름·시작 화면 카메라·미리보기 산 기준·갱도 과제·동굴 울림·용암 띠·광맥 분포 **여덟 개**가 예전 값에 굳어 v80 에서 되돌렸습니다. `grep` 으로 **그 축이 들어간 계산식**까지 훑으세요 |
| **`export var` 를 값으로 붙잡는다 — 세 가지 자리** | 라이브 바인딩은 ① 최상위 상수 ② 훅의 속성 ③ **오브젝트에 한 번 넣고 끝나는 곳**(`m.position.y = f()`) 에서 굳습니다. v79 가 ①②를 풀고 ③을 놓쳐 v80 에서 수평선이 12칸 어긋난 채 나갔습니다 |
| **자문의 진단이 맞아도 처방은 재 본다** | 자문은 "지형 기준 높이를 **내려서**" 라고 했는데 실제로는 **올려야** 지하가 생깁니다. "광맥 시도도 같이 두 배" 도 재 보니 반대였습니다(다이아 65~92개) — v79 |
| **남의 계측도 확인하고 쓴다** | 자문 12차의 "눈 5분 뒤 세계 변화 0칸" 은 틀렸습니다. 직접 재니 +31칸이었고 코드도 이미 있었습니다(v78) |
| **단독 통과 + 전체 실패 = 오염** | 여덟 번째입니다. v79 의 `v65 비행` 은 앞선 시험이 남긴 `S.flySpeed` 때문에 2배 차이가 1.5배로 읽혔습니다. 시험 첫 줄에서 **의존하는 양을 직접 못 박으세요** |
| **규칙을 완화하면 그 시험도 옮긴다** | `PLACE_REPEAT` 를 0.35→0.20 으로 되돌리자 `v22` 가 `repeat >= 0.3` 을 못 박고 있어 깨졌습니다. **상수에서 유도**하게 고쳤습니다(v78) |
| **도구가 코드를 지운다 · 시간대도 어긋난다** | `tidy-imports` 가 import 84개를 지워 안 켜진 판이 나갔습니다(v67). `stamp.mjs` 는 커밋 상태에선 KST, 미커밋 상태에선 UTC 로 찍어 **캐시 키가 뒤로 갔습니다**(v78). **도구를 돌린 뒤 시험을 한 번 더** |
| **임시 스크립트가 `src/` 를 되돌린다** | 타임아웃으로 백그라운드에 넘어가면 `finally` 가 안 돌거나(v68), 몇 시간 뒤 **낡은 사본으로 덮습니다**(v69). `ctx.route("**/src/x.js", r => r.abort())` 로 가로채세요 |
| **`x !== false` 는 x 가 없을 때도 참** | 훅에 `booted` 가 없어 시험이 부팅 완료를 한 번도 확인하지 않았습니다(v68). **있는지부터 단언**하세요 |
| **세계 생성에 무언가를 더할 때** | 기존 `rng` 를 이어 쓰면 모든 시드의 땅이 달라집니다. `makeRng(seed + 새 상수)` 로(v75·v76). 얹는 순서도 봅니다 — v79 는 **모래 가라앉히기가 장식 뒤에 돌아** 덤불이 허공에 떴습니다 |
| **"보인다" 를 잴 때** | `textContent` 는 `display:none` 도 읽습니다. `getClientRects()` 로 걸러 세세요(v71·v78) |
| **벤치마크 직전에 세계를 건드리면** | 청크 재굽기가 첫 측정에 얹혀 38배로 읽힙니다(v57·v64) |
<!-- /keep:traps -->

## 더 알아야 하면
| 궁금한 것 | 볼 곳 |
|---|---|
| 지금까지 뭘 했나 (시간순 원문) | `memory.md` — **맨 아래부터** 읽으세요 |
| 다음에 뭘 하지 | `docs/BACKLOG.md` |
| 그 함수 어느 파일이지 | `docs/CODEMAP.md` |
| 이 게임이 뭘 할 수 있나 | `docs/GAMEPLAY.md` |
| 어떻게 검증하나 | `docs/TESTING.md` → `node tests/run.mjs 10` |
| 왜 이렇게 돼 있지 | `docs/DECISIONS.md` |

## 자문을 띄우는 프롬프트 틀 (`작업해줘` 의 1단계)
`general-purpose` 서브에이전트에 이 틀로 띄웁니다. **"이미 처리된 것" 목록을 꼭 붙이세요.**

```
당신은 10년차, 매일 8시간씩 실제 마인크래프트만 해 온 고인물 플레이어입니다.
코드 리뷰어가 아니라 플레이어 감각으로 판단하세요.

검토 대상: <저장소 경로> — three.js r128 웹 복셀 샌드박스 (크리에이티브 전용 · 1인용)
읽는 순서: CLAUDE.md → docs/GAMEPLAY.md → docs/BACKLOG.md(여기 있는 것은 제안 금지)
          → memory.md 의 마지막 1/5 → src/*.js 직접

헤드리스로 직접 재현·계측해 주세요 (tests/harness.mjs 의 launch·serve, 폰은 PHONE).
**저장소 파일은 절대 수정하지 마세요** — 모듈을 깨뜨려 봐야 하면
ctx.route("**/src/x.js", r => r.abort()) 로 네트워크를 가로채세요. (CLAUDE.md 규칙 9)

이미 처리된 것 (다시 제안 금지): <위 "이미 처리된 것" 목록을 그대로 붙임>
백로그에 이미 있는 것 (제안 금지): <위 백로그 목록>
범위 밖: 서바이벌/체력 · 멀티플레이 · 새 외부 라이브러리 · 빌드 단계

보완할 사항 10가지를 우선순위대로. 각 항목마다
증상(플레이어가 겪는 장면) · 마크는 어떤가 · 왜 중요한가 · 어디를 볼까(src/파일.js) · 크기(소/중/대).
"소" 를 최소 5개 포함하세요.
```

## 시험 이름 필터 (`node tests/run.mjs 1 "필터"`)
이름 앞머리로 걸립니다 — 최근 판일수록 뒤 번호입니다.
`v76`(오두막) · `v75`(바다) · `v74`(소품) · `v73`(소리) · `v72`(불) · `v70`(재굽기) ·
`v69`(모래 되돌리기) · `v66`(계단 모서리) · `v62`(묘목) · `폰 ·`(폰 화면 전부).
전체 목록은 `grep -n '^test(' tests/run.mjs` · 갈래별 개수는 `docs/TESTING.md`.

## 반드시 지킬 것 (자세한 건 `CLAUDE.md`)
- **빌드 단계를 만들지 않는다.** three.js r128 CDN 외 라이브러리 금지. ES5 문체.
- 월드를 바꿀 때는 항상 `applyEdit` — `world[]` 를 직접 쓰지 않는다.
- 새 기능에는 회귀 시험을 같이 넣는다. 커밋 전 `node tests/run.mjs 10`.
- 도구(`tidy-imports` 등)를 돌린 뒤에는 **시험을 한 번 더** 돌린다 (v67 사고).
- 임시 스크립트가 `src/` 를 고쳤다 되돌리게 하지 않는다 (v68·v69 사고).
- push 뒤에는 `node tools/check-live.mjs` 로 **배포본이 진짜 켜지는지** 본다.
