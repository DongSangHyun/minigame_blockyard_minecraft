# HANDOFF — 세션 인계

> **새 세션은 `CLAUDE.md` → `docs/INDEX.md` → 이 파일** 순으로 읽으면 이어집니다.
> 이 문서는 `node tools/handoff.mjs` 가 다시 씁니다. 숫자는 손으로 고치지 마세요.
> 사람이 채우는 자리는 `<!-- keep:… -->` 로 감싸 두었고 도구가 덮지 않습니다.

## 지금 상태
| | |
|---|---|
| 마지막 판 | **v141** — QA 3차 반영 — 불 속도 · TNT 되돌리기 · 건축 도구와 문구 (v141) |
| 마지막 커밋 | 2026-09-22 21:42 |
| 회귀 시험 | **409항목** + 폰 화면 17항목 (러너는 오류 검사 2개를 더해 428 로 찍습니다) |
| 전체 10회 | 약 5~7분 — 커밋 전 게이트입니다 |
| 코드 | 모듈 31개 · 15,901줄 |
| 저장 포맷 | v5 |
| 백로그 | 29건 |
| 작업 트리 (이 도구를 돌린 때) | **미커밋 4개** — docs/HANDOFF.md · docs/INDEX.md · memory.md · tools/handoff.mjs |
| 로컬에서 열어 보기 | 저장소 뿌리에서 `python3 -m http.server 8000` → http://localhost:8000/index.html |
| 공개 주소 | https://dongsanghyun.github.io/minigame_blockyard_minecraft/ |

## 이미 처리된 것 (자문을 띄울 때 **이 목록을 프롬프트에 넣으세요** — 중복 제안 방지)
- QA 3차 반영 — 불 속도 · TNT 되돌리기 · 건축 도구와 문구
- QA 2차 반영 — 자연 나무 되돌리기 · 꺼지지 않는 불 · 모으기 과제 · 얼음
- QA 반영 — 공유 링크 새로고침 · 모으기 카드 · 트리거 반쯤
- 외부 시험 3차 — 과제 오판 · 로/으로 · 저장 검사 · 모으기 다이아
- 외부 시험 2차 — 동물 조준 뒤집힘 · 공유 링크 · 패드 캐기
- 외부 브라우저 시험 상위 셋 — 물 무한 수원 · /tp 좌표 · 사진 모드 안내
- 모으기 모드 안정화 — 실제 플레이로 찾은 것들
- 모으기 모드 — 나무부터 캐서 제작대로 하나씩 만든다
- 선물은 빈 칸으로, 사다리는 벽 쪽으로 밀 때만 오른다
- 마을 횃불이 남고, 집 짓기 손버릇을 다듬는다
- 동물이 24마리여도 꽃을 주면 따라온다
- 사다리 꼭대기에서 벽 위로 올라선다
- 맨손에서 모양이 안 바뀌고, 보던 핫바 쪽을 기억한다
- 맨손 — 아무것도 안 든 손, 그리고 우클릭 대상 정리
- 처음 노는 사람의 첫 하루는 두 배로 길다
- 저장 공간이 찰 때 본 저장을 지킨다
- 오래 켜 둔 세계 — 불이 큐와 저장을 안 불린다 · 눈이 마을을 안 망친다
- 지도에 내 집이 보이고, 상인이 빈집을 알려 준다
- 어제의 사고를 되돌린다 · 오늘 할 일이 보인다
- 둘째 날: 다시 켜도 마을이 살아 있다
- 새 세계에 이름 · 지형 단추가 지금 세계를 안 바꾼다
- 도움말 첫 다섯 줄 · 마을로 돌아가는 길
- 마을은 안 탄다 · 밤을 예고한다 · 핫바에 색 한 칸
- 튜토리얼을 아이 순서로 · 상인은 블록이 없어도 잡힌다
- 세계를 잃는 자리 셋 · 아이의 첫 30분
- 시작 마을 — 집·상인·동물·광산·개울에서 시작한다
- 마우스 없이 놀기 · 고대비가 지도까지 · 청사진 주고받기
- 자문 28차 — 눈과 손이 다른 사람
- 색 유리 16색 · 사암 · 돌벽돌 · 원과 구
- 자문 27차 — 30분째 플레이어
- 바다 위에 발판을 깔아도 그 밑이 안 마른다
- 자문 26차 — 물이 안 멎고, 첫 화면이 초보를 안 본다
- 되짚기가 v108 에서 잡은 일곱
- 말과 실제 — 자문 25차 열 건

## 마지막 세션이 한 일 (새것부터)
- QA 3차 반영 — 불 속도 · TNT 되돌리기 · 건축 도구와 문구 (v141)
- QA 2차 반영 — 자연 나무 되돌리기 · 꺼지지 않는 불 · 모으기 과제 · 얼음 (v140)
- QA 반영 — 공유 링크 새로고침 · 모으기 카드 · 트리거 반쯤 (v139)
- 외부 시험 3차 — 과제 오판 · 로/으로 · 저장 검사 · 모으기 다이아 (v138)
- 외부 시험 2차 — 동물 조준 뒤집힘 · 공유 링크 · 패드 캐기 (v137)
- 외부 브라우저 시험 상위 셋 — 물 무한 수원 · /tp 좌표 · 사진 모드 안내 (v136)
- 모으기 모드 안정화 — 실제 플레이로 찾은 것들 (v135)
- 모으기 모드 — 나무부터 캐서 제작대로 하나씩 만든다 (v134)
- 선물은 빈 칸으로, 사다리는 벽 쪽으로 밀 때만 오른다 (v133)
- 마을 횃불이 남고, 집 짓기 손버릇을 다듬는다 (v132)
- 동물이 24마리여도 꽃을 주면 따라온다 (v131)
- 사다리 꼭대기에서 벽 위로 올라선다 (v130)

## 진행 중이던 것
<!-- keep:wip -->
**없습니다.** v128~v141(**열네 판**)을 전부 커밋·push 했고, 판마다 `node tools/check-live.mjs` 로
**배포본이 켜지는 것까지 확인**했습니다(마지막 확인 v141 · 2026-09-22 21:42 · 「켜집니다 ✓」). 전 항목 ×10회 통과 상태입니다.

**이 세션(2026-09-17 ~ 09-22)에 일어난 큰 일 둘**

1. **모으기 모드(v134~) — 유저가 요청한 새 시스템.** 「나무부터 하나씩 캐어서 제작대로 만들어 가는」 방식입니다.
   유저 결정: **새 모드로 추가**(만들기 모드는 그대로) · **레시피 목록에서 누르기**(3×3 격자 아님) · **곡괭이 단계 넣기**.
   새 세계 › **놀이**(만들기/모으기)에서 고르고, 지형처럼 **새 세계를 만들 때** 적용됩니다.
   정본은 `src/survival.js`(가방 `S.inv` · `RECIPES` · `SV_GOALS` · 곡괭이 단계)와 `docs/GAMEPLAY.md` 의 v134~v141 절입니다.
2. **유저의 외부 브라우저 QA 3회 + 내부 QA 에이전트 3회.** v136~v141 이 전부 그 반영입니다.
   외부 시험은 「화면은 마크처럼 말하는데 규칙은 다르다」 를 잘 잡았고(물 무한 수원 · 동물 조준 반전 · 공유 링크 · 패드 캐기),
   내부 QA 에이전트는 **게임을 실제로 조작**해(스크래치패드에서 harness 로) 첫 단계 완주·건축 도구·문구를 검증했습니다.

**모으기 모드를 이어 볼 사람에게**
- 새 모으기 세계: `B.S.nextMode = 1; B.newWorld(시드); B.beginPlay();`
- 목록(E)이 「가방 · 만들기」 가 됩니다. 제작대·화로를 우클릭해도 열립니다(`S.forceStation`).
- 공짜 블록 길은 `svBlocked()`·`SV_CHEATS` 로 막혀 있습니다(되돌리기·영역·붙여넣기·청사진·/give…).
  **막힌 조작의 안내는 부르는 쪽에서 먼저** 막아야 합니다 — 안 그러면 뒤따르는 토스트가 덮습니다(v141).
- 과제 넷(쾅·대공사·색칠·수집가)은 모으기에서 불가능하므로 `CREATIVE_ONLY` 로 빠져 있습니다(분모 37).

**유저 결정 (그대로 유효)** — ① 동물 제거는 좌클릭 ② 배포는 끝나는 대로 push + check-live
③ "권장으로 묻지 말고 진행" ④ 아이(초등 저학년)가 실제 플레이어 — 2026-09-14 저녁에 공개했습니다.
<!-- /keep:wip -->

## 백로그 전체 (`docs/BACKLOG.md` 에서 뽑음)
- **무한 월드 / 청크 스트리밍** (상) — WX·WZ 가 96 고정이라 끝에 닿으면 격자벽을 만난다. 배열 재할당과 저장 포맷이 함께 움직여야 한다…
- **서바이벌 모드** (상) — 체력·낙하 피해·용암 피해·밤 위협. 지금은 완전한 크리에이티브다…
- **인벤토리·채집·크래프팅** (상) — 서바이벌과 한 묶음…
- **`WY` 를 96 으로 (대)** (대) — v79 가 지형을 12칸 올려 **지을 하늘이 45→33칸(−27%)** 이 됐습니다. **조사해 둔 것 (v107 · 2026-09-11)** — ① dims.…
- **곁가지 셋** (소) — ① 과제 팝업(#achpop)이 화면 위 한가운데라 E 를 연 순간 「블록」 제목과 필터 줄을 덮습니다(마크의 토스트는 늘 오른쪽 위) ② 잎이 빛은 완전히 막는…
- **세계 안에 글자를 남길 수 없다** (중) — 표식은 지도에만 있고 3D 씬에는 아무것도 안 그립니다. 간판은 글자 렌더가 붙어 크니, **싼 절반** — 표식을 세계 안에 떠 있는 이름표(캔버스 텍스처 스프…
- **지상 구조물이 오두막 한 종뿐** (중) — 뭍은 2,399~3,112칸(전체의 26~34%)인데 인공물은 오두막 3~6채가 전부입니다(지하는 v81 갱도·v105 노두로 채웠습니다). 난파선·돌탑 유적·야…
- **떠나 있는 동안 세계가 멈춘다** (중) — save.js 가 at: Date.now() 를 저장하지만 쓰는 곳은 슬롯 목록의 "3시간 전" 글자뿐입니다. 불러올 때 **상한(10분치) 안에서** growT…
- **지형을 자연스럽게 못 만진다** (중) — 집터를 내려고 /fill 공기 하면 직각 절벽이 남습니다. /smooth [반복] 은 영역 안 기둥의 표면 높이를 이웃 평균으로 옮기는 것이라 world 겉면만 …
- **핫바 20칸으로는 팔레트를 못 짠다** (use`). 세계 저장 포맷 v5 는 안 건드립니다) — 색만 32종인데 "이 건물의 팔레트" 를 통째로 불러올 길이 없습니다. **청사진과 똑같은 틀**로 S.bar·S.barAlt·S.shapeBar…·S.fillB…
- **시작 화면이 세계 목록이 아니다** (중) — 보여 주는 것은 지금 슬롯 하나뿐이고, 나머지 둘은 접힌 설정 **1,356px 아래**에 있습니다(폰 1,392px). 슬롯을 바꾸는 다른 길이 없습니다 — 슬…
- **슬롯 목록이 세계를 안 보여 준다** (중) — 적히는 것은 이름/N분·3시간 전 뿐 — 섬 그림도 시드도 지형도 없습니다. drawPreview 는 **지금 메모리에 올라온 세계만** 그립니다. 슬롯 2 가 …
- **세계를 만들 때 이름을 못 붙인다** (중) — 만들기 입력은 시드와 지형 둘뿐이고 이름은 만든 **뒤에** 설정 › 슬롯 › ✎ 로만 붙습니다. 폰에서는 시드 입력칸(y=1893)과 「새 세계」 단추(stic…
- **과제 40개 중 절반이 「조작을 아는가」인데 세계마다 다시 잠긴다** (소~중) — 20개가 조작 확인류(첫 삽·첫 벽돌·계단·꽃…)입니다. 발전 과제가 월드별인 건 마크와 같지만, 마크의 것은 "그 월드에서 겪을 일" 이지 "좌클릭을 해 봤나"…
- **20칸 팔레트가 슬롯을 안 넘어간다** (소) — 청사진·조작키·설정은 전역인데 핫바만 세계별입니다. 블록이 94종이라 팔레트 짜기가 마크보다 무겁습니다. 마크의 저장 핫바(Ctrl+1~9)는 **설치 단위**입…
- **튜토리얼 일곱 줄이 아이와 어긋난다** (중) — 네 번째가 G 반블록, 여섯 번째가 Alt+클릭 + Ctrl+F(**실제로 채워야** 넘어갑니다). 폰판은 같은 문제로 이미 고쳤는데 키보드판이 그대로입니다. 반…
- **도움말이 2,293자 · 45줄, 한 화면에 35%** (중) — 튜토리얼 마지막 줄이 여기로 보내는데 아이는 3초 보고 닫습니다. 맨 위에 **「처음 해 볼 것 다섯」**(캐기·놓기·E·상인·꽃) 한 블록을 큰 글씨로, 나머지…
- **10.2분이면 해가 지고 9.6분이 밤** (소~중) — 마을 안은 가로등으로 밝지만(블록광 10) **마을 밖 20칸은 0** 입니다. 첫 세계에서는 하루를 두 배로 늘리거나, 일몰 1분 전에 「곧 밤이에요 — 9번 …
- **불이 마을에 옮겨붙는다** (중) — opts.firespread 기본이 켜짐이고 집은 판자·원목입니다. 불은 계속 번지며 편집을 쌓아 Ctrl+Z 로도 못 돌립니다. 마을 반경 안에서 번짐을 막거나…
- **터치에는 마을로 돌아갈 길이 없다** (중) — 지도 표식은 찍히는데 /tp 광산 은 키보드 전용입니다. 터치 메뉴에 「마을로」 하나. 기본 핫바 20칸에 **색이 한 칸도 없는 것**도 같은 계열(상인 선물이…
- **상인이 "부탁" 을 하지 않는다** (중) — 선물만 주고 끝나 두 번째 방문에 할 일이 없습니다. 「오늘의 부탁」(이미 있는 과제 판정 재사용) — **새 시스템이라 유저 승인 필요**…
- **플레이 시작 직후 한 번 130~140ms 멈춤** (중) — S.active 를 켠 뒤 약 1초 무렵 step 한 번이 131ms. 세 번 중 두 번 재현, 프로파일러를 붙이면 안 나타나 원인 미상…
- **`villageAt` 의 `mine[1]` 이 서는 높이보다 1 높다** (소) — 지금은 해가 없다…
- **필터 실행에서만 깨지는 시험 둘** (소) — 「v30 동물: 공기 주머니」(1 놓) · 「v79 지하: 예전 판도 그대로」(1 "v79 지하" — 금 26 < 다이아 27). 전체 실행에서는 통과 (v132…
- **산소 게이지가 장식** (중) — 0 이 돼도 아무 일이 없고 구조 부스트는 한 번 걸리면 래치 — 약속과 다르다…
- **만들기 ↔ 모으기 전환이 없다** (중) — 지금 세계의 모드를 바꿀 길이 없다(새 세계만) — **새 기능이라 유저 확인**…
- **도움말과 모으기 규칙의 어긋남** (소) — 모으기에서 안 되는 것(되돌리기·E 블록 목록 등)을 도움말이 그대로 말하는 곳 점검…
- **isTouch 가 기동 때 고정** (중) — 태블릿에 키보드를 붙이거나 창을 바꿔도 조작 안내가 안 바뀐다…
- **공유 링크가 놀이 모드를 안 싣는다** (소) — 모으기 세계를 공유하면 받은 사람은 만들기로 연다(추가 다이아도 없음) — ?m=1 을 실을지 **유저 확인**…

## 다음에 할 후보
<!-- keep:next -->
**유저 확인이 필요한 것 (먼저 물어볼 것)**
1. **공유 링크에 놀이 모드를 싣기** (`?m=1`) — 지금은 모으기 세계를 공유하면 받은 사람은 **만들기**로 열고
   모으기 전용 다이아(`enrichDiamonds`)도 없습니다. 한 줄이면 됩니다.
2. **지금 세계의 모드를 바꾸기**(만들기 ↔ 모으기) — 지금은 새 세계에서만 고릅니다. 가방·과제·막힌 조작이 함께 움직입니다.
3. **상인의 「오늘의 부탁」** — 새 시스템이라 승인 거리로 남아 있습니다(백로그).

**바로 할 수 있는 것 (승인 없이)**
- **모으기 색깔 늘리기** — 양털이 흰·빨강·노랑 셋뿐입니다(꽃 두 종이 염료). 꽃을 더 쓰거나 조합을 넣습니다.
- **금괴가 쓸 데 없음** — `RECIPES` 어디에도 재료로 안 들어갑니다.
- **모으기 전용 과제** — 「첫 곡괭이」·「첫 제작대」 같은 것이 하나도 없습니다.
- **산소 게이지가 장식**(0 이 돼도 무해) · **isTouch 가 기동 때 고정** · **필터 실행에서만 깨지는 시험 둘**(백로그).
- **시작 화면이 세계 목록이 아니다 / 슬롯 미리보기** — 아이가 슬롯을 바꾸려면 설정을 한참 내려야 합니다.

**큰 것 (그대로 남아 있음)**
1. **`WY` 를 96 으로** (대) — D22 를 먼저 읽으세요(면 병합으로는 상쇄가 안 됩니다).
2. **무한 월드 / 청크 스트리밍** (상).
3. 서바이벌의 **체력·피해**(모으기 모드는 채집·제작까지만 들어갔습니다 — 아이용이라 죽음은 일부러 뺐습니다).

**QA 를 또 돌릴 때** — 내부 QA 에이전트에게 *"저장소를 고치지 말고 스크래치패드에서 harness 로 실제 조작"* 을 시키면
지금까지 여섯 번 중 여섯 번 다 결함을 물어 왔습니다. 아직 안 본 영역: **모바일 실기기 · 포인터 잠금 감도 · gist 동기화 · 번식 이후의 긴 플레이**.
<!-- /keep:next -->

## 이 세션에서 데인 자리 (다음 세션이 또 밟지 않게)
<!-- keep:traps -->
**실제로 사고가 났던** 자리입니다. 자세한 건 `memory.md` 의 해당 판 항목.

| 함정 | 무엇 |
|---|---|
| **시험이 코드의 착각을 그대로 베끼면 둘 다 틀린 채 통과한다** | `aimedMob` 의 `fy = -sin(pitch)` 로 **동물 조준이 위아래 반전**돼 있었는데(v137), 제 시험들이 `player.pitch` 를 양수로 두고 `camera` 를 `-pitch` 로 맞춰 **같은 착각을 양쪽에** 적어 통과했습니다. 규약: **양수 pitch 가 위**이고 `loop.js` 가 매 step 마다 `camera.rotation.x = player.pitch` 로 덮습니다 — 시험도 그 경로로 맞추세요 |
| **과제·목표는 「내가 한 일」 인지 가른다** | 세계가 스스로 하는 일(묘목 자람·바닷물 흐름)이 사람의 편집과 **같은 코드**를 지납니다. 가르지 않으면 가만히 서 있어도 「숲지기」·「수문장」 이 열립니다(v138). 손댄 칸(`isTouched`)이나 최근 행동 시각(`S.pouredAt`)으로 가르세요 |
| **자연이 하는 일을 사람의 편집 틀(batch)에 담지 마라** | 자연 묘목이 자라면 `beginBatch` 가 **불·물의 되돌리기 주인을 끊고** 되돌리기 맨 위에 앉아, Ctrl+Z 가 먼 나무를 지우고 탄 판자는 한 칸도 안 돌아왔습니다(v140) |
| **값의 뜻을 늘리면 그 범위를 가정한 시험을 찾는다** | 떨어지는 물에 레벨 8(`FALLING`)을 주자 「흐름 단계 ≤ 7」 을 가정한 v6 시험이 깨졌습니다(v136) |
| **고친 뒤 시험이 아니라, 고치기 전에 실패하는 시험** | v136~v141 의 새 시험은 전부 `git stash push src/…` 로 **옛 코드에서 실패하는 것부터** 확인했습니다. 옛 코드에서도 통과하면 아무것도 안 잰 것입니다 |
| **한 시험 안의 단계들은 자리를 떨어뜨린다** | v140 시험에서 앞 단계가 키운 **나무 잎**이 뒤 단계의 조준을 가렸고, 불 시험대가 하필 **마을 번짐 금지 구역**에 들어갔습니다 |
| **"여러 번 해 본다" 는 매번 달라야 한다** | v73 이 `animateLiquids(1.7)` 를 여섯 번 불러 **같은 그림**을 여섯 번 그려 놓고 "움직이지 않는다" 로 읽었습니다(10회 중 1회 · v140) |
| **목록에 칸을 더하면 세는 시험도 같이 옮긴다** | 맨손 칸(v128)·모으기 재료 9종(v134)을 목록에 더할 때마다 「목록 칸 수 = 블록+도구」 시험 다섯이 깨졌습니다 |
| **폰 화면은 스크린샷으로 본다** | 모으기 만들기 목록이 폰 가로에서 **2.5줄**만 보였습니다(가방이 한 줄도 안 보임 · v134·v139). 시험은 통과했고 그림이 잡았습니다 |
| **고칠 때 그것을 설명하는 말도 같이 찾는다** | 자문 25차 열 건 중 **절반**이 그것이었습니다 — v91 이 「꼭대기」 문턱을 올리며 주석에 이유까지 적고 `desc` 는 9 로 뒀고, v98 이 폰에 영역 도구를 붙이며 주석에 *"도움말이 거짓말"* 이라 적어 놓고 도움말은 안 고쳤습니다. **`CLAUDE.md` 5.5 절에 규칙으로 있습니다** (v108) |
| **무작위를 넣으면 그것을 재던 시험이 죽는다** | v106 이 소리에 ±8% 지터를 넣으면서, 같은 커밋의 「재질이 갈린다」 시험이 **78종이 전부 돌이어도 통과**하게 됐습니다(지터가 한 재질을 일곱 통으로 흩습니다). 잴 것은 **결정이 내려지는 표**지 결과로 나온 소리가 아닙니다 (v107) |
| **시험이 만든 지형·액체·동물·세계는 시험이 치운다** | v94(개체군)·v103(`generate`)·v104(물)에서 **세 번** 데었습니다. 뒤에 오는 `v13 동물` 이 "물 위를 걷는다" 로 10회 중 한두 번만 깨집니다 |
| **기본값을 바꾸면 시험대의 전제도 바뀐다** | v110 이 캐기 기본값을 「빠름」으로 옮기자 `v95 동물` 시험이 깨졌습니다 — **돌**벽 너머의 양이 "벽을 뚫고 잡혔다" 로 읽혔는데, 실제로는 0.5초 동안 **벽이 뚫린 것**입니다. 시험대가 기대던 것이 무엇인지 보고, 재려는 것(조준선이 벽을 통과하는가)만 남깁니다 — 벽을 **기반암**으로 바꿨습니다 |
| **`S.stick` 이 0 이 아니면 키보드가 통째로 무시된다** | `loop.js` 의 `if (S.stick.x \|\| S.stick.z) { ix = S.stick.x; iz = S.stick.z; }` 입니다. 폰 시험이 스틱을 밀어 둔 채 끝나면 뒤에 오는 이동 시험이 10회 중 한 번 흔들립니다 (v110 `v65 비행`). 이동을 재는 시험은 **스틱도 못 박습니다** |
| **확률로 도는 것을 기다리지 않는다** | 불·성장·번식은 확률입니다 — "20초 안에 판자가 줄어든다" 는 10회 중 한 번 흔들립니다 (v99) |
| **생성기에 더할 때 세 가지를 한꺼번에 밟는다** | v105 바위 노두가 ① 층 단위로 뜯어 **공중에 뜬 돌 51개** ② **스폰 시야와 굴 어귀**를 덮음 ③ 굴이 바위 밑동을 파냄 — 별도 난수 줄기 · 받침 검사는 맨 마지막(**아래에서 위로**) · 기존 것을 안 덮기 |
| **옮길 때는 옮겨 간 자리에 뭐가 있는지 본다** | 과제 팝업을 가운데에서 오른쪽 위로 옮겼더니 이번엔 **미니맵을 40% 덮었습니다** (v107) |
| **`endBatch` 는 기록되는 편집마다 돈다** | `applyEdit` 이 단일 편집에도 제 묶음을 엽니다. v97 이 거기서 통계를 세게 했더니 **손으로 놓은 것이 두 번** 세어져 「100칸」이 50칸에 열렸고(문은 3배), **자란 나무·번진 불까지** 사람 것으로 쳤습니다. 그리고 v98 의 `liftIfBuried` 도 거기 걸려 **반블록 바닥에서 블록 하나만 놓아도 사람이 튀어 올랐습니다.** 여기에 무언가를 얹을 때는 **부르는 쪽이 시킬 때만** 돌게 하세요 (v99) |
| **`isSolid(get(...))` 는 모양을 못 본다** | 반블록·계단·카펫·눈 위에 선 것을 "묻혔다" 로 읽습니다. 몸이 겹쳤는지는 `boxHitsWorld`, 한 점은 `pointSolid` 입니다 (v99) |
| **세는 시험은 "세지 말아야 할 것" 부터 못 박는다** | `v22 우클릭` 이 10회 중 1회 깨졌는데, 앞 시험이 남긴 **눈**이 시험장에 쌓여 셈에 든 것이었습니다. 블록을 세는 시험은 `S.weather = 0` 을 먼저 박으세요 (v96) |
| **확률로 도는 것을 기다리지 않는다** | 불·성장·번식은 확률입니다 — "20초 안에 판자가 줄어든다" 는 10회 중 한 번 흔들립니다. **"판자가 줄었거나 불이 살아 있다"** 처럼 확률에 안 기대는 문장으로 (v99) |
| **하위 명령은 넓은 갈래보다 앞에** | `/marks del 1` 을 넣었는데 `cmd === "marks"` 목록 갈래가 먼저 걸려 목록만 찍었습니다 (v100) |
| **클라우드 세계 이름은 ASCII 만 남는다** | `normalizeName` 이 `[^a-z0-9_-]` 를 걸러서 **한글 이름은 통째로 "main"** 이 됩니다(gist 파일명이라 그렇습니다). UI 문구는 한국어인데 이 값만 다릅니다 (v100) |
| **아틀라스 타일 번호는 남이 쓰고 있을 수 있다** | v92 가 새 묘목을 **담긴 양동이(81·82) 위에** 그렸습니다. 나중에 그린 쪽이 이기므로 묘목 그림이 소리 없이 지워졌는데, 시험은 "세 그림이 서로 다른가" 만 봐서 **통과했습니다**(양동이 두 개가 서로 다르니까). 이제 `paint()` 가 중복을 던지고, 시험이 **ALL_BLOCKS 의 타일이 실제로 그려진 것인지** 봅니다 |
| **`beginPlay()` 는 저장된 시점을 되살린다** | 시험이 `beginPlay` **앞에서** `player.yaw = 0` 을 넣어 두었더니 `S.savedYaw` 로 덮였습니다. `S.started` 가 이미 true 면 복원이 안 도니까 **단독 통과 · 전체 실패**가 됩니다 (v92) |
| **시험이 남기고 가는 상태는 언젠가 남이 본다** | `v10 3인칭` 이 `S.thirdPerson = 1` 인 채 끝나고 있었습니다(F5 를 네 번 눌러도 `(0+4)%3 = 1`). **3인칭에 볼 것이 생긴 판**(v92)에서야 드러났습니다. v94 시험이 동물을 1마리로 줄여 놓고 끝난 것도 같은 부류입니다(v13 이 간헐적으로 깨졌습니다) |
| **계기판은 `step` 이 아니라 `animate` 안에서 갱신된다** | `setPaused(true)` 로 재운 채 `step()` 만 돌리는 시험은 **화면 글자를 못 봅니다.** 실제 프레임을 돌려야 합니다 (v93) |
| **저장소가 이미 답을 갖고 있는 자리를 먼저 본다** | GPU 버퍼 누수는 `scene.js`·`hand.js` 가 **이미 dispose 하고 있었고** 수천 번 도는 `applyGeo` 만 빠져 있었습니다. 화각도 **바로 옆줄에서 손 카메라만** aspect 를 보고 있었습니다 (v93) |
| **남의 계측도 확인하고 쓴다** | 자문 19차의 "반쪽 문" 은 **`applyEdit` 을 직접 부른 계측**이었습니다 — 실제 좌클릭 경로(`mineAt`)는 v60 부터 두 칸을 묶어 지웁니다. 직접 재서 확인했습니다 |
| **빠르게 쓴 판은 스스로 되짚는다** | v84~v87 을 두 시간에 쓰고 `/code-review` 로 되짚었더니 **시험 332항목이 다 통과하는 채로 버그 여섯**이 나왔습니다(v88). 그중 하나는 **끝방이 통로 한가운데 생겨 버팀목·횃불을 지우고** 있었는데, 시험이 소품 **개수**만 세어 통과했습니다. **개수를 세는 시험은 "자리" 와 "순서" 를 못 잡습니다** — 자문을 기다리는 시간이 그 되짚기에 딱 맞습니다 |
| **첫 30초를 아무도 안 본다** | 자문 여러 차수가 판 것이 전부 "플레이 이후" 였습니다. 폰으로 열면 **「플레이」 단추가 화면 밖 160px** 에 있었고(v91), 화면에 보이는 유일한 안내가 "H 로 다시 볼 수 있습니다" 였습니다 — **폰에는 H 가 없습니다.** 자문에 "이번엔 첫 화면부터" 라고 이르세요 |
| **`var` 는 선언만 끌어올려진다** | `export var refreshWorldPills = ...` 를 IIFE **뒤에** 두어, IIFE 가 넣은 함수를 빈 함수가 덮었습니다(v91). 대입은 그 자리에서 돕니다 |
| **"보이는 것만 다른 것" 은 아이콘 층에서 가른다** | 담긴 양동이에 블록 아이디를 새로 만들면 `ALL_BLOCKS`·저장·"수집가" 과제까지 줄줄이 따라옵니다. `drawIcon(cv, block, tileOverride)` 로 **아이콘과 이름만** 갈랐습니다(v90) |
| **도구를 하나 더 넣을 때는 먼저 있던 도구와 규칙을 맞춘다** | 양동이(v86)가 `place()` 첫 줄에서 빠져나가 **문이 안 열렸습니다**(v89). 같은 도구인 부싯돌은 `tryInteract` 안에 있어 이미 답을 갖고 있었습니다. 아이콘·손·고스트도 `isItem` 을 안 봐 **회색 상자**로 보였습니다 |
| **문턱을 옮길 때는 그 축의 실제 분포를 센다** | v82 가 "꼭대기" 를 `y>50` → `SEA+9` 로 옮기며 **판 2 의 뭍 평균 높이(26.6~32.1)를 안 셌습니다** — 8시드 중 2개가 스폰 자리에서 이미 달성이었습니다. "지도장이" 도 분모가 지도 전체인데 **뭍은 23~31%뿐**이라 걸어서는 불가능했습니다 |
| **한 축을 좁히면 반대쪽이 열린다** | v85 가 "나무 밑에서 지도가 꺼지는 것" 을 고쳤더니 v86 에서 **내가 판 굴에서 안 켜지는 것**이 드러났습니다. 두 경우 다 **가 본 적 없는 곳이 "밝혀짐" 으로 칠해졌습니다** — 지도 기억 오염은 되돌릴 길이 없습니다. 판정을 좁힐 때는 **반대 극단**을 같이 재세요 |
| **나눠서 고친 것은 경계에서 뭘 잃는지 본다** | v81 이 지하 지도를 세 겹으로 나눈 것은 옳았지만, **경계를 넘나드는 순간**(15걸음에 한 번)이 고친 문제보다 자주 눈에 띄었습니다(v83) |
| **큰 축을 옮기면 그 축을 *읽는* 곳을 전부 센다** | v79 가 해수면을 옮기며 import 25곳은 봤는데 **파생값**(수평선·구름·카메라·과제·소리·용암·광맥·새) 여덟 개를 놓쳤습니다 |
| **`export var` 라이브 바인딩은 세 자리에서 굳는다** | ① 최상위 상수 ② 훅 속성 ③ **오브젝트에 한 번 넣고 끝나는 곳**(`m.position.y = f()`). v79 가 ①②를 풀고 ③을 놓쳐 수평선이 12칸 어긋난 채 나갔습니다 |
| **인덱스 0 은 거짓이다** | `if (markHere())` 로 뒀더니 **첫 표식(0번)만** 이름이 안 붙었습니다. `x !== false`(v68)와 같은 부류 — **`>= 0` 으로 못 박으세요** |
| **"계획한 것" 과 "실제로 된 것"** | 끝방을 `lastLeg = (i === n-1)` 로 달았더니, 줄기가 중간에 끊기면 방이 영영 안 왔습니다(세계당 0~2개 · 기대 3개). 루프가 끊길 수 있으면 **성공한 마지막**을 들고 있어야 합니다 |
| **`preventDefault` 와 이벤트 라우팅은 무관하다** | v85 가 "지도에서 움직이면 탭을 접는다" 로 시점을 넘겨줄 거라 여겼는데, target 이 지도면 캔버스 핸들러는 **안 돕니다.** 임자 없는 터치를 직접 승격시켜야 했습니다(v87) |
| **시험이 낡은 값을 읽는다** | `generate()` 뒤에 `relightAll(false)` 을 안 하면 `lightSky`·`lightBlk` 가 **앞 세계의 값**입니다. v81·v86 에서 두 번 데었습니다 — 단언이 조용히 아무것도 안 잽니다 |
| **저장소 밖 절대 경로를 하나만 박지 않는다** | `tests/harness.mjs` 가 playwright 를 **지난 세션 스크래치패드**에서 찾다가, 그 폴더가 청소되며 **시험이 통째로 안 돌았습니다**(v84). 후보를 훑게 고쳤습니다 |
| **같은 부류를 새로 넣을 때는 기존 것과 성질을 통째로 견준다** | 색 카펫 16색을 원래 카펫과 전부 대조하는 시험이 **v74 의 구멍**(카펫이 바닥을 캐도 안 걷힘)을 잡았습니다 |
| **남의 계측도 확인하고 쓴다** | 자문 12차의 "눈 5분 뒤 0칸" 은 틀렸습니다(직접 재니 +31칸). 자문 15차의 처방("지형 높이를 내려서")도 반대였습니다 |
| **단독 통과 + 전체 실패 = 오염** | 열 번을 넘겼습니다. 시험 첫 줄에서 **의존하는 양을 직접 못 박으세요** (`S.terrain`·`S.flySpeed`·동물 종 구성·`S.mmZoom`) |
| **도구가 코드를 지운다 · import 를 정규식으로 건드리면** | `tidy-imports` 가 import 84개를 지웠고(v67), v85 에서는 **내 정규식이 `refreshMinimapCap` 을 날려** 게임이 안 켜졌습니다. **손댄 뒤 반드시 시험** |
| **임시 스크립트가 `src/` 를 되돌린다** | 타임아웃으로 백그라운드에 넘어가면 낡은 사본으로 덮습니다(v68·v69). `ctx.route(...)` 로 가로채세요 |
| **세계 생성에 무언가를 더할 때** | 기존 `rng` 를 이어 쓰면 모든 시드의 땅이 달라집니다 — `makeRng(seed + 새 상수)`. 그리고 **받침 검사(`isCross` 걷어내기)는 생성기의 맨 마지막**이어야 합니다(v81·v79) |
| **"보인다" 를 잴 때** | `textContent` 는 `display:none` 도 읽습니다. `getClientRects()` 로 걸러 세세요 |
<!-- /keep:traps -->

## 더 알아야 하면
| 궁금한 것 | 볼 곳 |
|---|---|
| 지금까지 뭘 했나 (시간순 원문) | `memory.md` — **맨 아래부터** 읽으세요 |
| 다음에 뭘 하지 | `docs/BACKLOG.md` |
| 그 함수 어느 파일이지 | `docs/CODEMAP.md` |
| 이 게임이 뭘 할 수 있나 | `docs/GAMEPLAY.md` |
| 어떻게 검증하나 | `docs/TESTING.md` → `node tests/run.mjs 10` |
| 왜 이렇게 돼 있지 | `docs/DECISIONS.md` |

## 자문을 띄우는 프롬프트 틀 (`작업해줘` 의 1단계)
`general-purpose` 서브에이전트에 이 틀로 띄웁니다. **"이미 처리된 것" 목록을 꼭 붙이세요.**

```
당신은 10년차, 매일 8시간씩 실제 마인크래프트만 해 온 고인물 플레이어입니다.
코드 리뷰어가 아니라 플레이어 감각으로 판단하세요.

검토 대상: <저장소 경로> — three.js r128 웹 복셀 샌드박스 (크리에이티브 전용 · 1인용)
읽는 순서: CLAUDE.md → docs/GAMEPLAY.md → docs/BACKLOG.md(여기 있는 것은 제안 금지)
          → memory.md 의 마지막 1/5 → src/*.js 직접

헤드리스로 직접 재현·계측해 주세요 (tests/harness.mjs 의 launch·serve, 폰은 PHONE).
**저장소 파일은 절대 수정하지 마세요** — 모듈을 깨뜨려 봐야 하면
ctx.route("**/src/x.js", r => r.abort()) 로 네트워크를 가로채세요. (CLAUDE.md 규칙 9)

이미 처리된 것 (다시 제안 금지): <위 "이미 처리된 것" 목록을 그대로 붙임>
백로그에 이미 있는 것 (제안 금지): <위 백로그 목록>
범위 밖: 서바이벌/체력 · 멀티플레이 · 새 외부 라이브러리 · 빌드 단계

보완할 사항 10가지를 우선순위대로. 각 항목마다
증상(플레이어가 겪는 장면) · 마크는 어떤가 · 왜 중요한가 · 어디를 볼까(src/파일.js) · 크기(소/중/대).
"소" 를 최소 5개 포함하세요.
```

## 시험 이름 필터 (`node tests/run.mjs 1 "필터"`)
시험 **이름 어디에 들어가도** 걸립니다(`t.name.includes(필터)`) — 그래서 `모으기` 처럼 가운뎃말도 됩니다.
**최근 것** — `v141`(불 속도·TNT) · `v140`(과제·얼음·자연 나무) · `v138`(숲지기·다이아) · `v137`(동물 조준·공유 링크·패드) ·
`v134`~`v135`(모으기 모드) · `모으기`(모으기 전부) · `v128`(맨손) · `v130`(사다리 꼭대기).
**그 전** — `v76`(오두막) · `v75`(바다) · `v74`(소품) · `v73`(소리) · `v72`(불) · `v70`(재굽기) ·
`v69`(모래 되돌리기) · `v66`(계단 모서리) · `v62`(묘목) · `폰 ·`(폰 화면 전부).
전체 목록은 `grep -n '^test(' tests/run.mjs` · 갈래별 개수는 `docs/TESTING.md`.

## 반드시 지킬 것 (자세한 건 `CLAUDE.md`)
- **빌드 단계를 만들지 않는다.** three.js r128 CDN 외 라이브러리 금지. ES5 문체.
- 월드를 바꿀 때는 항상 `applyEdit` — `world[]` 를 직접 쓰지 않는다.
- 새 기능에는 회귀 시험을 같이 넣는다. 커밋 전 `node tests/run.mjs 10`.
- 도구(`tidy-imports` 등)를 돌린 뒤에는 **시험을 한 번 더** 돌린다 (v67 사고).
- 임시 스크립트가 `src/` 를 고쳤다 되돌리게 하지 않는다 (v68·v69 사고).
- push 뒤에는 `node tools/check-live.mjs` 로 **배포본이 진짜 켜지는지** 본다.
