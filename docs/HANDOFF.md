# HANDOFF — 세션 인계

> **새 세션은 `CLAUDE.md` → `docs/INDEX.md` → 이 파일** 순으로 읽으면 이어집니다.
> 이 문서는 `node tools/handoff.mjs` 가 다시 씁니다. 숫자는 손으로 고치지 마세요.
> 사람이 채우는 자리는 `<!-- keep:… -->` 로 감싸 두었고 도구가 덮지 않습니다.

## 지금 상태
| | |
|---|---|
| 마지막 판 | **v78** — 자문 12차 — 되돌리기가 물을 안 잡던 것부터 (v78) |
| 마지막 커밋 | 2026-09-08 14:38 |
| 회귀 시험 | **291항목** + 폰 화면 9항목 (러너는 오류 검사 2개를 더해 302 로 찍습니다) |
| 전체 10회 | 약 5~7분 — 커밋 전 게이트입니다 |
| 코드 | 모듈 28개 · 10,193줄 |
| 저장 포맷 | v5 |
| 백로그 | 8건 |
| 공개 주소 | https://dongsanghyun.github.io/minigame_blockyard_minecraft/ |

## 이미 처리된 것 (자문을 띄울 때 **이 목록을 프롬프트에 넣으세요** — 중복 제안 방지)
- 자문 12차 — 되돌리기가 물을 안 잡던 것부터
- 세션 인계 체계 — "새세션 준비해줘" / "새세션 시작"
- 오두막이 채마다 달라졌다
- 바다에 볼 것을 두다
- 실내 소품 — 책장과 카펫
- 자문 11차 — 손이 지루하던 자리와 귀가 비어 있던 자리
- 번져서 탄 집이 Ctrl+Z 한 번에 돌아온다
- 이어서 짓던 곳
- 바닥 한 장에 세계 전체를 다시 굽던 것
- 자문 10차 — 되돌리기가 거짓말하던 것부터
- 부팅 대기 조건이 사실은 아무것도 안 기다리고 있었다
- 배포본이 진짜 켜지는지 보는 도구
- 부팅이 실패하면 화면이 말하게
- tidy-imports 가 게임을 부순 것을 되돌리고 안전장치를 달았다
- 세계 이름과 돌아갈 수 있는 표식
- 터치 튜토리얼 · 날씨 저장 · 되돌리기 칸 상한
- 자문 9차 — 폰이 1급이 아니었다
- 캐기 속도를 실제 조작으로 잰다
- 계단 모서리가 그려지는지 · 미리보이는지

## 마지막 세션이 한 일 (새것부터)
- 자문 12차 — 되돌리기가 물을 안 잡던 것부터 (v78)
- 인계 문서에 v77 반영
- 세션 인계 체계 — "새세션 준비해줘" / "새세션 시작" (v77)
- 오두막이 채마다 달라졌다 (v76)
- 바다에 볼 것을 두다 (v75)
- 실내 소품 — 책장과 카펫 (v74)
- 자문 11차 — 손이 지루하던 자리와 귀가 비어 있던 자리 (v73)
- 번져서 탄 집이 Ctrl+Z 한 번에 돌아온다 (v72)
- 이어서 짓던 곳 (v71)
- 바닥 한 장에 세계 전체를 다시 굽던 것 (v70)
- 임시 스크립트가 src/ 를 고쳤다 되돌리지 못하게 (규칙 9)
- 자문 10차 — 되돌리기가 거짓말하던 것부터 (v69)

## 진행 중이던 것
<!-- keep:wip -->
**없습니다.** 마지막 판까지 전부 커밋·push·배포 확인(`node tools/check-live.mjs`)이 끝났고
워킹트리가 깨끗합니다. 전 항목 ×10회 통과 상태입니다.
(**숫자는 위 상태표만 믿으세요** — 이 칸은 사람이 적는 곳이라 낡을 수 있습니다.)

바로 이어서 할 수 있습니다. 다음 사이클은 **자문 12차를 띄우는 것**부터 —
위의 "이미 처리된 것" 목록을 프롬프트에 그대로 넣으세요.
<!-- /keep:wip -->

## 백로그 전체 (`docs/BACKLOG.md` 에서 뽑음)
- **무한 월드 / 청크 스트리밍** (상) — WX·WZ 가 96 고정이라 끝에 닿으면 격자벽을 만난다. 배열 재할당과 저장 포맷이 함께 움직여야 한다…
- **면 병합(greedy meshing)** (중) — 지금도 여유가 크지만(전체 메싱 **52~65ms** — v79 로 지하가 두 배가 되며 34~45ms 에서 올랐다), 세계를 키우려면 필요하다. 전체 메싱은 세…
- **서바이벌 모드** (상) — 체력·낙하 피해·용암 피해·밤 위협. 지금은 완전한 크리에이티브다…
- **인벤토리·채집·크래프팅** (상) — 서바이벌과 한 묶음…
- **물·용암 지우개(양동이)** (중) — 물·용암은 **놓기는 되는데 좌클릭으로 지울 수가 없는 유일한 블록**이다(레이캐스트가 액체를 건너뛴다 — 물속에서 바닥을 캐야 하므로 그 자체는 맞다). 지우려…
- **핫바 칸마다 모양 기억** (소~중) — G 로 고른 모양(S.shapeMode)이 **전역 하나**라, 3번 칸(계단)에서 2번 칸(유리)으로 바꾸면 유리도 계단으로 나온다. 지붕 한 채에 G 를 스무…
- **지하 구조물** (중) — (v79 로 동굴 공기가 **34,000~58,000칸**이 되어 넣을 자리가 넉넉해졌습니다) 예전 진단: 동굴 공기가 **8,586~24,846칸**인데 구조물…
- **실내 소품 더** (소~중) — 책장·카펫은 v74 에서 들어갔습니다. 다음은 **색 카펫**(양털 16색과 짝) · 화분 · 액자. 간판은 글자 렌더가 붙어 큽니다…

## 다음에 할 후보
<!-- keep:next -->
자문 5~11차가 낸 것은 **전부 처리**됐습니다. 남은 것은 `docs/BACKLOG.md` 에 있고, 요약하면:

1. **지하 구조물** — 동굴 공기가 8,586~24,846칸인데 구조물이 **아예 없습니다** (중)
2. **색 카펫·화분·액자** — v74 에서 책장·카펫을 넣었고 그다음 (소~중)
3. **면 병합(greedy meshing)** · **무한 월드** · **서바이벌·인벤토리** (상 — 큰 결심이 필요)

`작업해줘` 는 §4.5 의 다섯 단계(자문 10건 → 구현 → 부분 시험 → 전체 10회 → 보고)로 돕니다.
자문은 `general-purpose` 서브에이전트에 **"10년차 마크 고인물"** 페르소나로 띄우고,
**"이미 처리된 것" 목록을 프롬프트에 넣어야** 중복 제안이 안 나옵니다.
<!-- /keep:next -->

## 이 세션에서 데인 자리 (다음 세션이 또 밟지 않게)
<!-- keep:traps -->
이 세션에서 **실제로 사고가 났던** 자리입니다. 자세한 건 `memory.md` 의 해당 판 항목.

| 함정 | 무엇 |
|---|---|
| **도구가 코드를 지운다** | `tidy-imports` 가 `input.js` 의 import 84개를 지워 **게임이 안 켜진 채로 push** 됐습니다(v67). 원인은 정규식 리터럴 속 큰따옴표(`/"/g`). 도구에 안전장치를 달았지만, **도구를 돌린 뒤에는 반드시 시험을 한 번 더** 돌리세요 |
| **임시 스크립트가 `src/` 를 되돌린다** | 타임아웃으로 백그라운드에 넘어가면 `finally` 가 안 돌거나(v68), 몇 시간 뒤 돌면서 **낡은 사본으로 덮습니다**(v69). 네트워크를 가로채세요 — `ctx.route("**/src/x.js", r => r.abort())` |
| **`x !== false` 는 x 가 없을 때도 참** | 훅에 `booted` 가 없어 시험이 **부팅 완료를 한 번도 확인하지 않고** 있었습니다(v68). 상수·깃발과 견줄 때는 **있는지부터 단언**하세요(`hasOwnProperty`) |
| **단독 통과 + 전체 실패 = 오염** | 이 세션에만 **여섯 번** 걸렸습니다. `S.terrain`·낙하 큐·묘목 큐·동물 수 등. 시험 첫 줄에서 **의존하는 양을 직접 못 박으세요** |
| **벤치마크 직전에 세계를 건드리면** | 청크 재굽기가 첫 측정에 통째로 얹혀 38배로 읽힙니다(v57·v64). **가라앉히고**, 같은 조건에서 **한 변수만** 바꿔 견주세요 |
| **세계 생성에 무언가를 더할 때** | 기존 `rng` 를 이어 쓰면 **모든 시드의 땅이 달라집니다.** 새 장식은 `makeRng(seed + 새 상수)` 로 자기 줄기를 쓰세요(v75·v76) |
| **"보인다" 를 잴 때** | `textContent` 는 `hidden` 요소도 읽습니다. 접힌 것을 걸러 세세요(v71) |
<!-- /keep:traps -->

## 더 알아야 하면
| 궁금한 것 | 볼 곳 |
|---|---|
| 지금까지 뭘 했나 (시간순 원문) | `memory.md` — **맨 아래부터** 읽으세요 |
| 다음에 뭘 하지 | `docs/BACKLOG.md` |
| 그 함수 어느 파일이지 | `docs/CODEMAP.md` |
| 이 게임이 뭘 할 수 있나 | `docs/GAMEPLAY.md` |
| 어떻게 검증하나 | `docs/TESTING.md` → `node tests/run.mjs 10` |
| 왜 이렇게 돼 있지 | `docs/DECISIONS.md` |

## 자문을 띄우는 프롬프트 틀 (`작업해줘` 의 1단계)
`general-purpose` 서브에이전트에 이 틀로 띄웁니다. **"이미 처리된 것" 목록을 꼭 붙이세요.**

```
당신은 10년차, 매일 8시간씩 실제 마인크래프트만 해 온 고인물 플레이어입니다.
코드 리뷰어가 아니라 플레이어 감각으로 판단하세요.

검토 대상: <저장소 경로> — three.js r128 웹 복셀 샌드박스 (크리에이티브 전용 · 1인용)
읽는 순서: CLAUDE.md → docs/GAMEPLAY.md → docs/BACKLOG.md(여기 있는 것은 제안 금지)
          → memory.md 의 마지막 1/5 → src/*.js 직접

헤드리스로 직접 재현·계측해 주세요 (tests/harness.mjs 의 launch·serve, 폰은 PHONE).
**저장소 파일은 절대 수정하지 마세요** — 모듈을 깨뜨려 봐야 하면
ctx.route("**/src/x.js", r => r.abort()) 로 네트워크를 가로채세요. (CLAUDE.md 규칙 9)

이미 처리된 것 (다시 제안 금지): <위 "이미 처리된 것" 목록을 그대로 붙임>
백로그에 이미 있는 것 (제안 금지): <위 백로그 목록>
범위 밖: 서바이벌/체력 · 멀티플레이 · 새 외부 라이브러리 · 빌드 단계

보완할 사항 10가지를 우선순위대로. 각 항목마다
증상(플레이어가 겪는 장면) · 마크는 어떤가 · 왜 중요한가 · 어디를 볼까(src/파일.js) · 크기(소/중/대).
"소" 를 최소 5개 포함하세요.
```

## 시험 이름 필터 (`node tests/run.mjs 1 "필터"`)
이름 앞머리로 걸립니다 — 최근 판일수록 뒤 번호입니다.
`v76`(오두막) · `v75`(바다) · `v74`(소품) · `v73`(소리) · `v72`(불) · `v70`(재굽기) ·
`v69`(모래 되돌리기) · `v66`(계단 모서리) · `v62`(묘목) · `폰 ·`(폰 화면 전부).
전체 목록은 `grep -n '^test(' tests/run.mjs` · 갈래별 개수는 `docs/TESTING.md`.

## 반드시 지킬 것 (자세한 건 `CLAUDE.md`)
- **빌드 단계를 만들지 않는다.** three.js r128 CDN 외 라이브러리 금지. ES5 문체.
- 월드를 바꿀 때는 항상 `applyEdit` — `world[]` 를 직접 쓰지 않는다.
- 새 기능에는 회귀 시험을 같이 넣는다. 커밋 전 `node tests/run.mjs 10`.
- 도구(`tidy-imports` 등)를 돌린 뒤에는 **시험을 한 번 더** 돌린다 (v67 사고).
- 임시 스크립트가 `src/` 를 고쳤다 되돌리게 하지 않는다 (v68·v69 사고).
- push 뒤에는 `node tools/check-live.mjs` 로 **배포본이 진짜 켜지는지** 본다.
