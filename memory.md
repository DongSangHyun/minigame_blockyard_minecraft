# MEMORY — Blockyard 개발 진행내역

> 시간 역순이 아니라 **시간순**입니다. 새 항목은 맨 아래에 붙입니다.
> 진행내역만 적습니다. "왜 그렇게 했는가"는 `docs/DECISIONS.md`, "무엇을 할 수 있는가"는 `docs/GAMEPLAY.md`.

---

## 0. 이전 세션들 (코드에서 역추적)
문서가 없던 시기의 결과물을 코드에서 읽어 정리한 것입니다. 날짜는 알 수 없습니다.

- 단일 HTML 파일 · three.js r128 · 빌드 도구 없음이라는 형태가 정해짐
- 96 × 48 × 96 복셀 월드, 16³ 청크(108개), 그리디하지 않은 면 단위 메싱 + AO
- 절차적 텍스처 아틀라스 — 16×16 타일을 캔버스에 코드로 그림 (이미지 파일 0개)
- 값 노이즈 기반 지형 + 3바이옴(초원·설원·사막) + 동굴 + 나무
- 햇빛/블록광 15단계 BFS 조명, 편집 시 국소 재계산(`relightLocal`)
- 바닷물 침수(`waterTick`) · 모래/자갈 낙하(`fallTick`) — 예산제 틱
- 반블록·계단(`SHAPE_BOXES`) · 놓을 자리 고스트 프리뷰
- 저장: RLE + Base64 → localStorage (포맷 v4) · 20초 자동 저장
- 240단계 되돌리기/다시하기 · 도전 과제 16개 · 미니맵(지하에선 단면도)
- 낮밤 순환 · 날씨(비/눈) · 해와 달과 별 · 반딧불이/나비
- 포인터락 + 드래그 조작 + 터치 조작 3중 지원
- `window.__blockyard` 헤드리스 테스트 훅 (테스트 코드 자체는 남아 있지 않았음)

---

## 1. 2026-09-04 — 컨텍스트 체계 구축 + 마크 감각 보정 10건 (v5)

### 1-1. 이 세션에서 새로 만든 것
| 파일 | 내용 |
|---|---|
| `CLAUDE.md` | 작업 지침 · 절대 규칙 8개 · 성능 예산 · 작업 루프 |
| `docs/INDEX.md` | 색인 계층의 진입점 — "어디를 볼지"만 알려 준다 |
| `docs/CODEMAP.md` | 코드 색인(자동 생성) — 섹션 19개 · 함수 142개 · 훅 158개 |
| `docs/GAMEPLAY.md` | 기능 명세 · 조작표 · 블록표 · 블록 추가 체크리스트 |
| `docs/DECISIONS.md` | 설계 결정과 이유 |
| `docs/TESTING.md` | 테스트 실행법 |
| `docs/BACKLOG.md` | 남은 일 (고인물 자문 24건 중 미착수 14건) |
| `tests/harness.mjs` | playwright-core + 로컬 Chrome for Testing 기동 |
| `tests/run.mjs` | 회귀 테스트 36항목 |
| `tests/index.mjs` | CODEMAP 생성기 |
| `memory.md` | 이 파일 |

### 1-2. 고인물 자문
10년차 마인크래프트 플레이어 페르소나 에이전트에게 전체 코드를 읽히고
"5분 플레이하면 즉시 위화감을 느낄 지점"을 24건 받았습니다.
핵심 진단: **"월드는 마크에 충실한데 위화감이 전부 *손*에서 온다 —
걷고 / 캐고 / 놓는 세 동작에 문제가 몰려 있다."**
전체 목록은 `docs/BACKLOG.md`.

### 1-3. 반영한 개선 10건

| # | 개선 | 이전 | 이후 |
|---|---|---|---|
| 1 | **채굴감** | 캐는 1.25초 동안 팔 정지·무음 | 0.28초마다 팔 휘두름 + 재질별 "턱-턱" 반복음 |
| 2 | **스텝 높이** | 1.05 — 1블록 벽을 걸어서 통과 | `STEP_UP = 0.6` · 올라선 뒤 표면까지 내려붙임 |
| 3 | **Shift** | 달리기 | 웅크리기 + 발판 밖으로 안 나감 · 달리기는 Ctrl / W 더블탭 |
| 4 | **픽블록** | Q 만 | 휠 클릭(마우스 가운데 버튼) 추가 |
| 5 | **광석** | 칸마다 독립 확률 → 낱개로 흩뿌려짐 | 씨앗에서 랜덤워크로 자라는 **광맥** (석탄 4~12칸 · 철 3~8칸) |
| 6 | **비·눈** | 지하 100칸에서도 눈앞으로 통과 | 하늘이 뚫린 칸에만 그림 (`columnTop` 판정) |
| 7 | **소리·속도** | 놓기 소리 하나 · 달리기 1.6배 · 시각 신호 없음 | 재질별 놓기 소리 · 달리기 1.3배(6.0) · 달릴 때 FOV +5.5° |
| 8 | **선택 상자** | 항상 정육면체 (반블록 위에 떠 있음) | 모양별 선택 상자 6종 · 균열도 반블록에 맞춤 |
| 9 | **방위·기반암** | 방위 표시 없음 · 바닥이 그냥 돌인데 안 캐짐 | HUD 에 북/서/남/동 · `BEDROCK` 블록 신설(전용 텍스처) |
| 10 | **이동 관성** | 즉시 출발·즉시 정지 · 공중에서도 100% 조작 | 속도 기반 이동 + 마찰 · 공중 가속 24% |

부수적으로: **R 키 오폭 방지** — 한 번 누르면 경고 토스트, 2.2초 안에 한 번 더 눌러야
새 세계가 생성됩니다. (기존에는 R 한 번에 세계가 통째로 날아갔습니다.)

### 1-4. 검증
`node tests/run.mjs 10` → **36항목 × 10회 = 360 통과 / 0 실패.**
개선 10건마다 회귀 테스트를 하나 이상 새로 넣었습니다 (`개선N ...` 이름).

### 1-5. 이 과정에서 잡힌 실제 버그
- **스텝업 공짜 상승**: 스텝 높이를 0.6으로 낮춰도, 벽에 막힌 채 0.6칸을 올라간 뒤
  0.2칸만 전진하고 그대로 유지되는 경우가 있었습니다.
  → "요청한 이동량의 90% 이상을 실제로 갔을 때만" 스텝업을 인정하도록 수정.
- **R 키 첫 2.2초 무방비**: 타이머 초기값이 `0`이라 페이지 로드 후 2.2초 안에는
  R 한 번으로 세계가 재생성됐습니다. → 초기값 `-1e9`.

---

## 2. 2026-09-05 — 2차·3차 개선 8건 + 공개 게시 (v6)

### 2-1. 반영한 개선 8건 (고인물 자문 실행 순서 2차·3차)

| # | 개선 | 이전 | 이후 |
|---|---|---|---|
| 10 | **상단 반블록 · 반전 계단** | 하단 슬랩 + 정방향 계단 4종뿐 | 모양 11종 · 밑면/옆면 위쪽 클릭으로 위 변형 (천장 마감·아치 가능) |
| 16 | **용암** | 없음 — 지하가 완전한 무광 암흑 | 세계 바닥 동굴에 고임 · `EMIT 15` · 통과 가능 · 시야 안 오렌지 안개 |
| 17 | **얼음** | 눈 덮인 해변에 열대 바다 | 설원 수면(y=SEA)만 `ICE` · 반투명 · 빛 통과 |
| 6 | **풀·꽃** | 식물 0개 — "초록 평면" | X자 교차 쿼드 렌더러 신설 · 잔디 포기 · 양귀비 · 민들레 (바람에 흔들림) |
| 7 | **횃불** | 광원이 램프 풀블록뿐이라 좁은 굴을 막음 | 얇은 `TORCH` · `EMIT 14` · 바닥이 있어야 놓임 |
| 13 | **잎 부패** | 나무를 베면 잎이 영구히 공중에 뜸 | 원목에서 5칸 안에 이어지지 않은 잎이 시차를 두고 떨어짐 |
| 20 | **경계 · 높이** | `WY=48` — 해수면 위 37칸뿐 | `WY=64` (해수면 위 53칸) · 섬 밖 바다 여백 확대 · 만년설 기준 22→25 |
| 15 | **물 배치** | 바다 아래를 파는 것 외에 물을 못 만듦 | `WATER` 선택 가능 · 아래로 떨어지고 옆으로 3칸 확산 · 근원이 사라지면 마름 |

곁들여 바꾼 것: 하루 길이 기본값 5분 → 10분 (낮이 2.5분이라 짓다 보면 계속 밤이었다).

### 2-2. 저장 포맷 v5
`WY` 가 바뀌어 셀 수가 달라졌으므로 포맷을 v5 로 올리고 **v2·v3·v4 저장을 자동 이관**한다
(`liftLegacy` — 높이 48 짜리 배열을 아래에서부터 새 배열로 옮겨 담는다).
물 흐름 단계(`waterLvl`)도 v5 부터 함께 저장한다.

### 2-3. 이 과정에서 잡은 실제 버그
- **`enqueueWater` 의 해수면 상한**: `if (y > SEA) return;` 때문에 해수면 위로는 물이 아예
  큐에 들어가지 않아, 놓은 물이 한 칸도 흐르지 않았다.
- **떨어지는 물기둥이 옆으로 퍼짐**: 물기둥 아래가 물이면 "받쳐졌다"고 보고 매 층에서
  옆으로 퍼져 → 다시 떨어지고 → 또 퍼지는 연쇄로 12칸까지 번졌다.
  **단단한 바닥(`isSolid`)을 딛은 물만** 옆으로 퍼지도록 고쳤다.
- **모래 침강이 용암을 끌어올림**: 생성 시 모래가 액체와 자리를 바꾸도록 돼 있어
  용암이 기둥을 타고 올라왔다. 물하고만 바꾸도록 제한.

### 2-4. 공개 게시 (사용자 요청)
- **주소 https://dongsanghyun.github.io/minigame_blockyard_minecraft/**
- 저장소 `DongSangHyun/minigame_blockyard_minecraft` (public) · GitHub Pages (main / 루트)
- `blockyard.html` → **`index.html`** 로 이름 변경 (Pages 진입점)
- **모바일 대응**: viewport 메타(없었다 — 폰에서 980px 로 렌더되던 가장 큰 문제) ·
  PWA manifest + 아이콘 · 홈 화면 추가 · 전체화면 요청 · 안전영역(노치) ·
  터치 기기 렌더 해상도 1.25 상한 · 시야거리 72 · 좁은 화면 HUD/핫바 축소 · 세로 회전 안내
- 실측: 폰 에뮬레이션(844×390, DPR 3)에서 **부팅 1.7초 · 120 FPS · 콘솔 오류 0**

### 2-5. 검증
`node tests/run.mjs 10` → **50항목 × 10회 = 500 통과 / 0 실패.**
개선 8건마다 회귀 테스트를 새로 넣었다 (`v6 ...` 이름).

### 2-6. 사용자가 푼 제약
**"파일 하나로만 만들지 않아도 된다"** — `CLAUDE.md` 절대 규칙 1번을
"파일은 한 개다" 에서 **"빌드 단계를 만들지 않는다"** 로 바꿨다.
게임 코드의 ES 모듈 분리는 사용자 승인 아래 다음 작업으로 진행 중.

---

## 3. 2026-09-05 — ES 모듈 분리 (v7)

사용자가 "파일 하나" 제약을 풀어 준 뒤, 4,000줄짜리 IIFE 하나를 `src/` 아래 **모듈 24개**로 나눴다.
겉보기 동작은 하나도 바뀌지 않았고 회귀 테스트 50항목이 전후 모두 통과한다.

### 3-1. 구조
```
state · dims · queues     아무것도 import 하지 않는 뿌리
blocks · atlas            블록 정의와 텍스처
world · light · fluids    세계 데이터와 시뮬레이션
mesh · scene              그리기
player · mine · sky       플레이어와 상호작용
hud · hand · input        화면과 조작
loop · main               매 프레임과 조립
```
`index.html` 은 마크업과 CSS 만 남기고 `<script type="module" src="src/main.js">` 하나로 시작한다.

### 3-2. 이 과정에서 겪은 사고 네 가지
1. **최상위 `return`** — IIFE 안에서 쓰던 `bail(...); return;` 이 모듈에서는 구문 오류다. 예외로 바꿨다.
2. **객체 리터럴 키까지 치환** — `bar: bar` 를 `S.bar: S.bar` 로 바꿔 버렸다.
   키 앞 글자는 항상 `{` · `,` · 줄머리라는 점을 이용해 값 쪽만 바꾸도록 고쳤다.
3. **가짜 순환 import** — 지역 변수 이름(`tone`, `player`)이 다른 모듈의 export 와 겹쳐
   쓰지도 않는 모듈을 import 하게 됐고, `atlas → audio → … → hand → atlas` 순환이 생겨
   `tileOrigin` 이 `undefined` 인 채로 기하를 만들다 터졌다.
   → 모듈 안에서 지역 선언된 이름은 import 하지 않도록 걸렀다.
4. **import 한 바인딩에 대입** — `Assignment to constant variable`.
   여러 모듈이 바꾸는 값은 `state.js` 의 `S` 로, 한 모듈만 바꾸는 값은 그 모듈로 선언을 옮겼다.

### 3-3. 남긴 도구 / 지운 도구
- 남김: `tools/codemap.mjs`(모듈 지도 생성) · `tools/tidy-imports.mjs`(안 쓰는 import 정리) ·
  `tools/make-icons.mjs`(PWA 아이콘)
- **지움: 자동 분리 스크립트와 분리 전 단일 파일.**
  저장소에 두면 "단일 파일" 과 "모듈" 두 개의 진실이 생겨 다음 세션이 헷갈린다.
  지금부터 진실은 `src/` 하나이고, 분리 전 원본은 git 이력에 있다.

### 3-4. 테스트 하네스 변경
ES 모듈은 `file://` 에서 CORS 로 막히므로, 하네스가 **임시 HTTP 서버**를 띄우고
그 위에서 페이지를 연다 (`tests/harness.mjs` 의 `serve()`).

### 3-5. 검증
`node tests/run.mjs 10` → **50항목 × 10회 = 500 통과 / 0 실패.**

---

## 4. 2026-09-05 — 새 블록을 "상호작용 계층" 에 등록 (v8) + 마지막 업데이트 표시

### 4-0. 시작 화면에 마지막 업데이트 시각 (사용자 요청)
카드 오른쪽 위(`VOXEL SANDBOX` 맞은편)에 `마지막 업데이트 YYYY-MM-DD HH:MM` 을 띄운다.
`src/version.js` 는 `tools/stamp.mjs` 가 git 최신 커밋에서 만든다(커밋 전이면 지금 시각).
**커밋 해시는 화면에 쓰지 않는다** — 도장을 찍는 순간 해시가 스스로 바뀌어 항상 한 발 늦는다.

### 4-1. 3차 고인물 자문의 진단
> **새 블록들이 "월드 생성기" 에는 들어갔는데 "상호작용 계층" 에는 안 들어갔다.**

풀·꽃·횃불은 지형에 그려지지만 놓기·조준 표시·모양 시스템·물·모래 어디에도 등록되지 않았고,
용암과 얼음도 텍스처와 배치만 있고 물리·소리·마찰이 없었다. TOP 10 중 7건이 난이도 "하" 인 이유 —
새 시스템이 아니라 **누락 메우기**였기 때문.

### 4-2. 반영한 10건
| # | 개선 | 이전 | 이후 |
|---|---|---|---|
| 1 | **물이 풀을 쓸어버린다** | 잔디밭에 물을 부으면 **한 칸도 안 퍼짐**(주변 48칸이 풀로 막힘) | 풀·꽃·횃불을 쓸며 정상 확산 |
| 2 | **모래가 풀을 부순다** | 풀 포기 한 개가 모래를 받쳐 공중에 뜸 | 부수고 지면까지 낙하 |
| 3 | **풀 위에 블록을 놓는다** | 풀을 조준하면 이웃 칸에 놓여 **허공에 블록이 뜸** (1분 안에 100% 발생) | 풀 자리를 덮어쓴다 |
| 4 | **조준 표시** | 실오라기 횃불 주위에 1×1×1 상자 · v6 상단슬랩/반전계단 균열이 풀사이즈 | `HL_CROSS` + `SHAPE_BOUNDS` 로 11모양 + 얇은 블록 전부 정확 |
| 5 | **모양 강제** | G(반블록) 상태로 물을 놓으면 **반쪽짜리 물덩이** | 액체·얇은 블록은 `SH_FULL` 고정 |
| 6 | **반블록 합치기** | 반블록 위에 반블록을 놓으면 0.5칸 틈이 남음 | 겹치면 온전한 한 블록 |
| 7 | **횃불** | 화면상 두께 0.05칸(마크의 1/2.5) · 반블록 위에서 0.5칸 뜸 | 0.26 반너비 · `crossBase` 로 아래 칸 윗면에 붙음 |
| 8 | **얼음 마찰** | 반투명한 돌 | 붙는 힘·마찰 0.14배 — 미끄러진다 |
| 9 | **물 흐름** | `waterTick` 이 같은 호출에서 새 이웃까지 처리해 **한 프레임에 완결** | 틱마다 한 칸씩 번진다 (`fallTick` 도 동일) |
| 10 | **콘텐츠 노출** | 소개문 "64×64 섬"(실제 96×96) · 도전과제 16개가 v5·v6 반영 0 · 기본 핫바에 횃불 없음 | 소개문 갱신 · 과제 22개(용암·얼음·횃불10·꽃·폭포·반블록합치기) · 핫바 9번 = 횃불 |

### 4-3. 이 과정에서 잡은 것
- `place()` 에 `SH_FULL` 을 쓰면서 **import 를 빼먹어** 반블록 합치기·모양 강제 경로가
  런타임에 터졌다. 회귀 테스트가 바로 잡아냈다.
- 새로 만든 `crossBase` / `HL_CROSS` / `SHAPE_BOUNDS` 를 `window.__blockyard` 훅에 노출하는 것도
  테스트가 강제했다 (규칙 6이 작동한 사례).

### 4-4. 문서에 남긴 것
`docs/GAMEPLAY.md` 의 **블록 추가 체크리스트에 10~13번을 추가**했다 —
놓기 / 조준 표시 / 물·중력 / 소리·마찰. 이번 사고의 재발 방지책이다.

### 4-5. 검증
`node tests/run.mjs 10` → **60항목 × 10회 = 600 통과 / 0 실패.**

---

## 5. 2026-09-06 — 액체·바이옴·크리에이티브 (v9)

자문이 "다음 판에" 라고 남긴 11건 중 9건 + 미뤄둔 것 2건.

| # | 개선 | 이전 → 이후 |
|---|---|---|
| 1 | 용암 호수 | 동굴 바닥의 45%가 용암(평야) → 노이즈 0.055/0.74 로 드문드문한 호수 |
| 2 | 물속 조명 | 햇빛 15가 해저까지 그대로 → `WATER_DIM = 2` 로 한 칸마다 두 단계씩 감쇠 |
| 3 | 물속 감각 | 소리 그대로 · 수면 전환 0.12칸 어긋남 → 로우패스 420Hz + 윗면 높이에 맞춘 판정 |
| 4 | 액체 애니메이션 | 정지 화면 → 아틀라스의 물·용암 타일만 세로로 흘린다(0.14초 주기, 셰이더 무수정) |
| 5 | 꽃 패치 | 칸마다 독립 확률 → 저주파 마스크로 패치, 한 패치에 한 종류 |
| 6 | 사막·설원 식물 | 0개 → 선인장(2~3칸 solid) · 죽은 덤불 · 마른 풀 |
| 7 | 얼음 상호작용 | 생성 시점 고정 → 설원에 부은 물이 2.2초 뒤 언다 · 광원 옆 2칸은 녹는다 · 횃불은 물속에 못 놓는다 |
| 8 | 소리·불티 | 없음 → 횃불 불티 파티클 · 풀숲 바스락 · 얼음 발소리(유리질) |
| 9 | 물 + 용암 | 나란히 붙어만 있음 → 조약돌 + 치익 소리 + 파편 |
| 10 | 크리에이티브 관용구 | `F` 만 → 스페이스 더블탭 비행 · `F1` HUD 숨기기 · 드래그로 줄 긋듯 연속 배치 |

**설계 판단** — 설원 얼리기는 처음에 waterTick 안에서 즉시 얼렸더니 **물이 한 칸도 못 퍼졌다**
(퍼질 칸이 즉시 얼음이 되어 흐름이 끊긴다). `freezeQ` 로 분리해 2.2초 뒤에 얼린다.

**잡은 것** — `window.__blockyard.B` 에 새 블록 3종을 안 넣어서 테스트가 전부 `undefined` 를
비교하고 있었다. 규칙 6("훅에 노출")을 지켰다면 없었을 낭비.

검증: `node tests/run.mjs 10` → **70항목 × 10회 = 700 통과 / 0 실패.**

---

## 6. 2026-09-06 — 건축·시점·분위기 (v10)

| # | 개선 | 이전 → 이후 |
|---|---|---|
| 1 | 원목 축 회전 | 어떻게 놓아도 나이테가 위 → 클릭한 면 방향으로 눕는다 (`SH_AXIS_X/Z`, 면 종류만 바꾸는 "모양") |
| 2 | 벽 횃불 | 바닥에만 → 벽면 클릭 시 벽에 붙는다(`SH_WALL_*`), 벽이 사라지면 함께 떨어진다 |
| 3 | F5 3인칭 | 없음 → 1인칭 / 뒤 / 앞 3단계, 벽에 파묻히지 않게 물러난다, 3인칭에선 손을 안 그린다 |
| 4 | 바이옴 균형 | 시드 5개 중 3개가 육지의 70%가 설원 → 임계값 조정으로 사막·초원이 살아난다 |
| 5 | 나무 종류 | 참나무 한 종 → 초원에 자작나무 섞임 · 설원은 가문비나무(원뿔 수형·짙은 잎) |
| 6 | 해와 달 | 매끈한 원반 → 각진 픽셀 + **달 위상 8단계** (자정을 넘길 때마다 바뀐다) |
| 7 | 동굴 울림 | 없음 → 깊고 어두운 곳에서 낮은 울림과 물방울 |
| 8 | 착지 먼지 | 소리만 → 세게 떨어질수록 발밑 재질의 먼지가 인다 |
| 9 | 미니맵 | 항상 전체 → `[` `]` 로 ×1 / ×2 / ×4 확대 |
| 10 | 산소 | 무한히 숨 쉼 → 물속 18초 게이지, 다 떨어지면 숨이 차서 수면으로 떠오른다 |

**잡은 것** — v8에서 `crossBase` 를 레이캐스트에 반영하는 패치가 배치 도중 실패해
**조준 상자만 옛 자리에 남아 있었다**(반블록 위 횃불이 보이는 곳과 다른 곳에서 맞았다).
벽 횃불을 넣으며 같은 자리를 손대다 발견해 함께 고쳤다.

검증: `node tests/run.mjs 10` → **79항목 × 10회 = 790 통과 / 0 실패.**

---

## 7. 2026-09-06 — 세계와 날씨 (v11)

| # | 개선 | 이전 → 이후 |
|---|---|---|
| 1 | 천둥·번개·빗소리 | 비가 소리 없이 내림 → 하늘이 번쩍이고 거리만큼 늦게 천둥, 빗소리가 깔린다 |
| 2 | 눈이 쌓인다 | 눈은 그냥 파티클 → 지표가 조금씩 하얘지고 비가 오면 다시 녹는다 |
| 3 | 날씨 전환 | 켜졌다 꺼졌다 → `weatherMix` 로 서서히 짙어지고 옅어진다 |
| 4 | 광석 종류 | 석탄·철 둘뿐 → 금(y≤11) · 다이아(y≤7) 추가, 깊이가 보상이 된다 |
| 5 | 동굴 형태 | 고주파 노이즈 한 겹 → 넓은 방(저주파) + 세로 협곡 |
| 6 | `F2` 화면 저장 | 없음 → 지금 화면을 PNG 로 내려받는다 |
| 7 | 저장 슬롯 | 하나 → 셋 (1번은 기존 키를 그대로 써 예전 세계를 잃지 않는다) |
| 8 | 밝기 설정 | 없음 → 셰이더 감마 슬라이더 |
| 9 | 세계의 끝 | 보이지 않는 벽 → 가까이 가면 옅은 격자벽이 떠오른다 |
| 10 | 도전 과제 진행도 | 토스트만 → HUD 에 `n / 22`, 달성 토스트에도 진행도 |

**잡은 것** — 날씨 페이드 코드를 `if (!S.weather) return;` **뒤에** 두는 바람에
날씨가 꺼져도 입자가 계속 짙게 남았다. 조기 반환 앞으로 옮기고, 다 옅어진 뒤에만 빠져나가게 했다.

검증: `node tests/run.mjs 10` → **88항목 × 10회 = 880 통과 / 0 실패.**

---

## 8. 2026-09-06 — 건축 부품과 상호작용 (v12)

| # | 개선 | 내용 |
|---|---|---|
| 1 | **동적 모양 시스템** | 이웃에 따라 상자가 바뀌는 블록을 위한 `boxesAt(b, sh, x, y, z)` — 메싱·충돌·조준이 모두 이 하나를 쓴다 |
| 2 | 울타리 | 기둥 + 이웃 쪽 팔. 넘어갈 수 없다 |
| 3 | 유리판 | 홀로 서면 십자, 이어지면 한 장이 된다 |
| 4 | 문(게이트) | 우클릭으로 여닫힌다. **웅크리면 상호작용 대신 블록을 놓는다** (마크 규칙) |
| 5 | 사다리 | 벽에 붙고 타고 오른다 (`isClimbable`) |
| 6 | 시작 지점 | `V` — 지금 자리를 시작 지점으로. 저장에도 실린다 |
| 7 | 시드 복사 | 메뉴에서 지금 세계의 시드를 클립보드로 |
| 8 | `F3` 성능 정보 | 보이는 청크 · 삼각형 · 굽는 중 · 시뮬 대기열 · DPR |
| 9 | 파편 | 바닥에 닿으면 한 번 튕기고 잦아든다 |
| 10 | 해 | 가산 블렌딩으로 새하얗게 타던 것을 노란빛이 남게 |

**설계 판단** — `dynamicBoxes` 는 처음에 모듈 공유 배열을 재사용했는데,
두 번 부르면 앞선 결과가 덮이는 함정이 있었다(테스트가 바로 걸렸다).
동적 블록은 드물어 매번 새 배열을 만들어도 비용이 없다 — 안전한 쪽을 택했다.

**잡은 것** — 성능 패널을 HUD 갱신 블록 안에 넣었더니 `animate()` 에만 있어
`step()` 만 도는 테스트에서 영원히 비어 있었다. `refreshPerf()` 로 떼어 둘 다에서 쓴다.

검증: `node tests/run.mjs 10` → **98항목 × 10회 = 980 통과 / 0 실패.**

---

## 9. 2026-09-06 — 건축 도구와 생명 (v13)

| # | 개선 | 내용 |
|---|---|---|
| 1 | **양털 16색** | 건축의 팔레트. 표·이름·굳기·목록·아틀라스를 한 루프로 등록한다 |
| 2 | **영역 선택** | `Ctrl`+좌/우클릭으로 두 모서리, 초록 테두리로 보인다 |
| 3 | **채우기** | `Ctrl`+`F` — 고른 블록으로 영역을 채운다 (최대 4만 칸) |
| 4 | **복사·붙여넣기** | `Ctrl`+`C` / `Ctrl`+`V` — 빈칸은 덮어쓰지 않는다 |
| 5 | **묶음 되돌리기** | 대량 편집이 되돌리기 기록 **한 개**가 된다 (`beginBatch`/`endBatch`) |
| 6 | 비행 속도 | `Ctrl`+휠 로 ×0.5 ~ ×4 |
| 7 | 미니맵 표식 | `B` — 돌아올 곳을 12개까지 찍는다 |
| 8 | 핫바 2쪽 | `Tab` 으로 전환, 두 쪽 다 저장된다 |
| 9 | 조작 도움말 | `H` — 조작이 40개를 넘어 게임 안에서 볼 곳이 필요했다 |
| 10 | **동물** | 양·돼지·소가 지면을 따라 걸어 다닌다. 절벽에서 안 떨어지고 가끔 운다 |

**이 판에서 겪은 것** — 패치 스크립트의 앵커가 앞선 수정 때문에 빗나가
**훅 등록·Tab 처리·저장 필드가 통째로 빠진 채 지나갔다.** 테스트가 9개를 한꺼번에
잡아 주지 않았으면 조용히 나갔을 변경이다. 앵커가 어긋나면 조용히 넘어가지 않도록
치환마다 `assert` 를 두는 습관이 값을 했다.

검증: `node tests/run.mjs 10` → **107항목 × 10회 = 1,070 통과 / 0 실패.**

---

## 10. 2026-09-06 — 마무리와 안전 (v14)

| # | 개선 | 내용 |
|---|---|---|
| 1 | 동물 충돌 | 플레이어와 동물이 서로를 밀어낸다 — 뚫고 지나가지 않는다 |
| 2 | 동물 그림자 | 발밑 원반 — "땅에 붙어 있다" 는 느낌 |
| 3 | **세계 내보내기·가져오기** | `.json` 파일로 브라우저 밖에 세계를 꺼낸다. 형식·버전을 확인한 뒤에만 덮어쓴다 |
| 4 | **자동 백업** | 저장할 때마다 직전 내용을 슬롯별 백업으로 밀어 둔다 · 메뉴에서 "직전으로 되돌리기" |
| 5 | 구름 두 겹 | 높은 층이 더 얇고 빠르게 흘러 하늘에 깊이가 생긴다 |
| 6 | 하늘색 보간 | 날씨·번개로 목표색이 튀어도 실제 하늘은 부드럽게 따라간다 |
| 7 | 물 반짝임 | 수면에 스치는 빛 (셰이더 네 줄, `uTime` 재사용) |
| 8 | 핫바 스와이프 | 폰에서 작은 칸을 정확히 누르지 않아도 좌우로 쓸어 고른다 |
| 9 | 화면 표시 크기 | 설정 슬라이더 80~180% (폰 기본 110%) |
| 10 | 먼 청크 해제 | 시야의 2.4배를 넘어가면 정점 버퍼를 놓아 준다 (다시 다가오면 굽는다) |

**잡은 것** — 물 반짝임을 넣으며 `uTime` 을 프래그먼트 셰이더에 선언하지 않아
**셰이더 컴파일이 통째로 실패**했다. 화면이 검게 나오는 대신 콘솔 오류로 잡혔고,
"콘솔 오류 0" 테스트가 이것을 즉시 실패로 만들었다.

검증: `node tests/run.mjs 10` → **117항목 × 10회 = 1,170 통과 / 0 실패.**

---

## 11. 2026-09-06 — 사건이 있는 세계 (v15)

| # | 개선 | 내용 |
|---|---|---|
| 1 | **TNT** | 횃불을 들고 우클릭하면 터진다. 반경 4, 가장자리는 너덜너덜, 기반암은 남는다. **폭발 전체가 되돌리기 한 번** |
| 2 | **불** | 횃불로 나무·양털·풀에 불을 붙이면 옆으로 번지고, 태울 것이 없으면 꺼진다. 광원 13 |
| 3 | 물고기 | 물속을 헤엄친다 (점으로 값싸게) |
| 4 | 새 | 하늘을 돈다 |
| 5 | **3D 소리** | `PannerNode` — 소리가 어디서 나는지 들린다. 듣는 방향도 매 프레임 갱신 |
| 6 | 지형 유형 | 보통 · 평지 · 산악 · 군도 — 메뉴에서 고르면 다음 새 세계부터 |
| 7 | 구조물 | 버려진 오두막 — 무너진 벽, 유리창, 안에 횃불 하나 |
| 8 | 동물 | 물·얼음·용암 위로는 걸어가지 않는다 |
| 9 | 배경음 | 몇 분에 한 번 스치는 세 음짜리 화음 (낮/밤이 다르다). 루프 음악이 아니라 "가끔 들리는 것" |
| 10 | 밝은 별 | 밤하늘에 늘 같은 자리 — 방향을 잡는 표식 |

**잡은 것 두 가지**
- **불이 영영 안 꺼졌다.** 번진 틱에는 자기 자신을 큐에 다시 넣지 않아 큐에서 빠졌고,
  그 뒤로는 아무도 그 칸을 검사하지 않았다. 살아 있으면 반드시 다시 넣도록 고쳤다.
- **문에 불이 붙었다.** 횃불을 들고 게이트를 우클릭하면 여는 대신 불을 붙여 문을 쓸 수 없었다.
  여닫는 블록을 점화보다 먼저 처리한다.

검증: `node tests/run.mjs 10` → **127항목 × 10회 = 1,270 통과 / 0 실패.**

---

## 12. 2026-09-06 — 편의와 완성도 (v16)

| # | 개선 | 내용 |
|---|---|---|
| 1 | 첫 로딩 표시 | 세계를 만들고 굽는 동안 진행 막대와 문구 — 멈춘 것처럼 보이지 않는다 |
| 2 | 튜토리얼 확장 | 4단계 → 6단계 (횃불 · 영역 도구 · 도움말) |
| 3 | 고대비 화면 | 설정 체크 하나로 테두리와 글자를 또렷하게 |
| 4 | 미니맵 등고선 | `\` — 높이 4칸마다 한 줄, 지형의 높낮이를 읽는다 |
| 5 | 조작키 재배치 | 비행·모양·복사·도움말 네 개를 원하는 키로 (localStorage 에 남는다) |
| 6 | 월드 통계 확장 | 지형 유형 · 슬롯 · 표식 · 놓아 본 블록 종류 · 되돌리기 단계 |
| 7 | 성능 자동 조절 | 프레임이 32 아래로 세 번 이어지면 시야거리를 줄이고, 55를 넘으면 되돌린다 |
| 8 | **가까운 청크부터 굽기** | 눈앞이 먼저 채워진다 — 같은 시간이 짧게 느껴진다 |
| 9 | 블록 목록 검색·갈래 | 35종이 넘어 한 판에 쏟으면 못 찾는다. 자연/건축/색/빛·물 + 이름 검색 |
| 10 | 최근 쓴 블록 | 목록을 열면 방금 쓴 것이 맨 앞 |

**설계 판단** — 성능 자동 조절과 굽기 순서를 `animate()` 안에 두면
테스트가 닿지 않는다(이미 두 번 겪었다). `autoTuneFar(fps)` 와 `setBuildFocus()` 로
바깥에서 부를 수 있게 떼어 두었다.

검증: `node tests/run.mjs 10` → **137항목 × 10회 = 1,370 통과 / 0 실패.**

---

## 13. 2026-09-06 — 나누고 다루기 (v17)

| # | 개선 | 내용 |
|---|---|---|
| 1 | **URL 세계 공유** | `?seed=1234&t=2` — 링크 하나로 같은 지형이 열린다. 메뉴에 "공유 링크 복사" |
| 2 | **명령창** | `/` 로 열고 `tp` `time` `weather` `fill` `give` `count` `bp` `seed` `gm` `help` |
| 3 | **청사진** | 복사한 영역에 이름을 붙여 저장하고 다시 꺼내 쓴다 (`bp save/use/list`) |
| 4 | **영역 통계** | `count` — 고른 영역이 무엇으로 지어졌는지 세어 준다 |
| 5 | 동물 먹이 주기 | 꽃·풀을 들고 우클릭하면 잠시 따라온다 |
| 6 | 사진 모드 | `F6` — HUD 없이 비행. `F2` 로 저장 |
| 7 | 도전 과제 알림 | 토스트 대신 화면 위 큰 알림 (이름 + 설명) |
| 8 | 시작 화면 미리보기 | 들어가기 전에 섬 모양·육지 비율·지형 유형을 본다 |
| 9 | HUD 지대·청크 | 지금 어느 바이옴, 몇 번 청크인지 |
| 10 | 도전 과제 2개 추가 | 친구(먹이 주기) · 사진사 |

**잡은 것 (중요)** — 명령창의 `Escape` 가 먹지 않았다. 원인은 **모듈 순환**이었다:
`hud.js` 와 `input.js` 가 서로를 import 하는데, input 이 먼저 평가될 때
hud 의 `cmdIn` 은 아직 `undefined` 였고 `if (cmdIn)` 가드가 조용히 리스너를 건너뛰었다.
DOM 에서 직접 잡도록 고쳤다. **`CLAUDE.md` 모듈 규칙의 "최상위에서 순환 상대의 값을 읽지 말 것"이
정확히 이 사고를 가리킨다** — 규칙을 알고도 걸렸다.

**플레이키 하나** — 동물 밀어내기 테스트가 10회 중 1회 실패했다. 다른 동물이 우연히
시험 좌표에 있었던 것. 10회 반복이 아니었으면 못 잡았을 종류다.

검증: `node tests/run.mjs 10` → **147항목 × 10회 = 1,470 통과 / 0 실패.**

---

## 14. 2026-09-06 — 오프라인과 손에 맞추기 (v18)

| # | 개선 | 내용 |
|---|---|---|
| 1 | **오프라인 플레이** | 서비스 워커. 우리 파일은 "네트워크 먼저"(새 배포가 캐시에 갇히지 않게), CDN 은 "캐시 먼저" |
| 2 | **게임패드** | 왼쪽 스틱 이동 · 오른쪽 시점 · A 점프 · B 웅크림 · RT 캐기 · LT 놓기 · X 복사 · Y 비행 · 십자 핫바 |
| 3 | 터치 영역 도구 | 두 손가락으로 짧게 눌러 모서리를 찍는다 |
| 4 | 왼손잡이 배치 | 스틱과 버튼을 좌우로 맞바꾼다 · 터치 버튼 크기 80~160% |
| 5 | 스크린샷 각인 | 시드 · 좌표 · 시각을 사진 아래에 새긴다 |
| 6 | 자동 저장 주기 | 5~120초 설정 |
| 7 | 되돌리기 단계 | 60~2000단계 설정 |
| 8 | 새 세계 확인 | 30번 넘게 손댄 세계는 한 번 더 묻는다 |
| 9 | 첫 방문 환영 | 처음 온 사람에게만 한 줄 안내 |
| 10 | 오프라인 안내 | 시작 화면에 "홈 화면에 추가하면 인터넷 없이도 열립니다" |

**플레이키 하나 더** — 먹이 주기 테스트가 10회 중 1회 실패했다.
`feedNearbyMob` 은 **가장 가까운** 동물을 고르는데, 시험 대상이 아닌 동물이 더 가까웠던 것.
v14 의 밀어내기 테스트와 정확히 같은 원인이었다. 동물이 등장하는 테스트는
**시험 대상 외의 동물을 먼저 치우는 것**이 규칙이 되어야 한다.

검증: `node tests/run.mjs 10` → **157항목 × 10회 = 1,570 통과 / 0 실패.**

---

## 15. 2026-09-06 — 손과 손이 부딪히던 곳 (v19)

4차 고인물 자문의 진단: **"세계는 좋아졌다. 문제는 v13~v18 이 v5~v12 를 밟고 서 있다는 것."**
새 기능을 넣을 때 "이 입력·이 블록이 이미 무슨 뜻이었나" 를 확인하지 않은 자리가 전부 사고 지점이었다.

### 심각한 것 (모두 조용히 배포돼 있었다)
| # | 문제 | 고침 |
|---|---|---|
| 1 | **게임패드를 꽂아 두기만 해도 좌클릭 채굴·Space·Shift 가 죽었다** — `pollGamepad` 가 매 프레임 무조건 대입 | 눌렸을 때만 켜고, **패드가 직접 뗐을 때만** 끈다 |
| 2 | **설정 7개가 새로고침마다 리셋** — `loadOpts` 화이트리스트가 6개에서 멈춰 밝기·표시크기·고대비·왼손잡이·터치버튼·자동저장·되돌리기단계가 버려졌다 | 기본값 객체의 **모든 키**를 읽는다. 목록을 따로 두면 또 빠뜨린다 |
| 3 | **자동 시야 조절이 사용자 설정을 영구히 깎았다** — `opts.far` 를 줄이고 저장 → 세션마다 래칫처럼 내려가 안 돌아옴 | 런타임 전용 `S.farNow` 로 분리. 사용자가 정한 값은 건드리지 않는다 |
| 4 | **눈·비가 사람이 놓은 블록을 되돌리기 없이 바꿔치기** | `touched` 배열로 사람이 손댄 칸을 기억해 건너뛰고, 눈은 **위에 얹고**, 변화는 되돌릴 수 있게 |
| 5 | **불은 되돌릴 수 없었다** (TNT 는 되는데) · 번짐 상한 없음 · 물로 못 끔 | `applyEdit(record)` 경유 · 최초 지점에서 9칸 상한 · 물이 닿으면 꺼짐 |
| 6 | **우클릭 홀드가 문을 초당 5회 여닫았다** + 셀 해시 자리올림 충돌(`*4096 + *64` 인데 WZ=96) | 반복 호출은 "놓기" 만. 해시는 `idx()` 그대로 |
| 7 | **동물이 4칸 안에 있으면 꽃·풀이 아예 안 심어졌다** (수직 무제한) | 조준선이 실제로 동물을 향할 때만 먹이 주기. 위아래 3칸 밖은 세지 않는다 |
| 8 | **튜토리얼 인덱스가 두 단계 어긋나** 시키는 대로 해도 안 넘어갔다 | 횃불 배치 → 4, 영역 선택 → 5, 도움말 → 6 |
| 9 | **기본 HUD 10줄** — F3 를 안 눌러도 이미 F3 화면 | 기본 3줄(좌표·시각·모양), 나머지는 `F3` |
| 10 | 진행 중이던 Alt/부싯돌 수정의 구멍 | 부싯돌을 `ALL_BLOCKS` 에서 빼 **`ITEMS`** 로(안 그러면 "수집가" 과제가 영영 불가능) · `applyEdit` 에서 도구 배치 차단(`Ctrl+F`·`/fill` 우회) · `R` 키에도 편집량 확인 · 횃불 진척도 저장 |

### 곁들여
Ctrl 달리기와 겹치던 마우스 영역 도구를 **Alt** 로 옮겼고, 시작 화면의 조작 28줄을 접었고,
명령 자동완성·히스토리, 설정 갈래 묶기, 도움말 안 도전 과제 탭을 넣었다.
**블록·과제·훅의 등록 누락을 자동으로 잡는 점검 테스트 3개**도 추가했다 —
v9·v13 에서 반복된 "훅에 안 넣어서 조용히 죽은 기능" 부류를 이제 테스트가 잡는다.

### 남긴 규칙
`docs/GAMEPLAY.md` 에 **입력·상호작용 추가 체크리스트 6줄**을 넣었다.
이번에 찾은 12건 중 10건이 그 6줄 중 하나에 걸린다.

검증: `node tests/run.mjs 10` → **175항목 × 10회 = 1,750 통과 / 0 실패.**

---

## v20 — 물에서 못 나오고, 블록 옆이 뚫려 보이던 이유 (2026-09-06)

유저 제보 두 건이 사실은 **한 가지 원인**이었습니다.
> "물에 빠졌는데 스페이스로도 F(비행)로도 못 나가"
> "블럭의 옆부분이 투명해서 땅속 지하가 다 보여"

헤드리스로 재현해 보니 몸이 블록에 겹친 순간 **모든 축의 이동이 영구히 막혔습니다.**
`moveAxis` 는 이미 겹쳐 있으면 첫 0.001칸에서 바로 충돌로 판정해 되돌립니다 —
즉 걷기도 점프도 비행도 통하지 않습니다. 그리고 그 상태에서는 눈이 블록 안에 있으니
뒷면이 잘려 나간 세계가 그대로 보입니다. "옆이 투명하다" 는 건 렌더 버그가 아니라
**카메라가 블록 속에 있었다는 증거**였습니다. (스크린샷으로 확인)

| # | 고침 | 왜 |
|---|---|---|
| 1 | **`unstick()`** — 몸이 블록에 묻히면 매 프레임 위 → 옆 → 아래 순으로 가장 가까운 빈 자리로 밀어낸다 | 갇히면 어떤 조작으로도 못 빠져나온다. 탈출로는 게임이 보장해야 한다 |
| 2 | **얼음이 사람을 가두지 않는다** — `freezeTick` 이 플레이어가 있는 칸은 건너뛴다 | 설원 호수에서 헤엄치면 수면이 얼어붙어 몸째 갇혔다. 제보의 가장 유력한 경로 |
| 3 | **수면 도약** — 물속은 그대로 4.4, 수면에서 `Space` 는 `JUMP × 0.8` 로 **한 칸 물가에 올라선다** | v6 에서 "튀어 오름" 을 막으려 0.9 로 고정한 게 과했다. 0.9 로는 영원히 물가에 못 오른다 |
| 4 | **`moveAxis` 를 0.4칸씩 끊어 민다** (`SWEEP`) | 도착지만 비면 통과시키던 빠른 경로가 얇은 바닥(반블록 등)을 뚫는다. 갇힘의 또 다른 입구 |

곁들여: 도움말에 "물에서는 Space 로 떠오르고, 수면에서 W+Space 로 물가에 올라섭니다" 한 줄.
`enqueueFreeze()` 를 fluids 에서 내보내 훅에 실었습니다 (테스트가 얼음을 직접 재현하려면 필요).

검증: `node tests/run.mjs 10` → **179항목 × 10회 = 1,790 통과 / 0 실패.**

## v21 — X축 면이 통째로 안 그려지고 있었다 · 클라우드 이어하기 (2026-09-06)

### 1. "블럭 옆이 투명해서 땅속이 다 보인다" — 진짜 렌더 버그였다
v20 에서 "카메라가 블록 안에 있어서"라고 진단했지만 **틀렸습니다.**
유저가 보내 준 사진으로 다시 파고들어, 빈 세계에 돌 한 개를 놓고
여섯 방향에서 레이캐스트를 쏘아 **+x · -x 면만 가까운 면에 맞지 않는 것**을 찾았습니다.

`FACES` 의 x 두 면만 **정점 순서가 뒤집혀** 있었습니다. 법선을 손으로 계산해 확인:
`+x` 면의 삼각형 법선이 `(-1,0,0)` 이었습니다. 즉 **뒷면 제거(FrontSide)에 걸려 GPU 가 버렸습니다.**
그래서 x 축으로 블록을 보면 옆면이 없어 속이 훤히 보였습니다.
(y·z 면은 정상이라 위에서 내려다보는 화면·항공샷에서는 멀쩡해 보였습니다.)

고침은 두 면의 `c` 배열에서 정점 1·2 를 맞바꾼 것뿐입니다.
같은 장면의 어두운 픽셀이 **3.06% → 0.16%** 로 떨어졌습니다.

> **왜 몇 달을 살아남았나:** 테스트 182개 중 *그려진 결과*를 보는 것이 하나도 없었습니다.
> 정점 수·인덱스 수는 맞았습니다 — 뒤집힌 면도 정점 수는 같으니까요.
> 이제 여섯 면을 레이캐스트로 확인하는 회귀 테스트가 있습니다.

곁들여 v20 의 진단이 완전히 헛되지는 않아서, 눈이 블록에 파묻히면
화면을 덮는 **질식 화면**(`#inblock`)을 넣었습니다 — 마크와 같은 처리입니다.

### 2. 기기 사이 이어하기 (GitHub Gist)
빌드도 서버도 라이브러리도 없이 폰과 PC 가 같은 세계를 잇습니다.
- `api.github.com` 이 `access-control-allow-origin: *` 라 브라우저가 직접 부릅니다 (실측 확인)
- 세계 하나 **32KB** · secret gist 한 개에 `index.json` + `<세계이름>.json` 로 담습니다
- **충돌 감지** — 이 기기가 마지막으로 본 판 번호(`baseRev`)보다 원격이 앞서면 덮지 않고 물어봅니다.
  덮어써도 gist 리비전 기록에 이전 판이 남습니다
- **세계 이름(ID)** 으로 진행을 나눕니다. 화면에 "잠금장치가 아니다" 를 적어 두었습니다
- 토큰은 이 기기 `localStorage` 에만 두고 GitHub 외에는 보내지 않습니다. 연결 해제로 지웁니다
- 테스트는 `S.netFetch` 를 갈아 끼워 진짜 GitHub 을 부르지 않습니다

올리기 직전에는 **항상** `saveGame()` 합니다. 안 그러면 슬롯에 남아 있던 예전 세계가 올라갑니다
(테스트가 실제로 잡아냈습니다).

검증: `node tests/run.mjs 10` → **182항목 × 10회 = 1,820 통과 / 0 실패.**

## v22 — 우클릭 한 번이 여러 개로 놓이던 문제 (2026-09-06)

유저 제보: "오른쪽 클릭 한 번에 블록이 여러 개 만들어져. 누르고 있을 때 연속 생성 간격을 늘려 줘."

원인은 v13 에 넣은 **"조준한 칸이 바뀌면 쿨다운을 건너뛴다"** 규칙이었습니다.
마크처럼 드래그로 줄을 긋게 하려던 것인데, 클릭하는 순간 손이 조금만 떨려도
조준 칸이 바뀌어 **0.18초 간격을 무시하고 프레임마다 놓였습니다.**

키보드 자동 반복과 같은 모양으로 바꿨습니다 — **한 번 놓고 · 뜸을 들이고 · 천천히 반복.**

| | 전 | 후 |
|---|---|---|
| 첫 반복까지 | (없음 · 칸이 바뀌면 즉시) | `PLACE_DELAY` **0.50초** |
| 반복 간격 | 0.18초 (칸이 바뀌면 무시) | `PLACE_REPEAT` **0.35초** (항상 지킨다) |

간격은 이제 **예외 없이** 지킵니다. 드래그로 줄 긋기는 그대로 되지만 초당 3개 남짓으로 느긋해집니다.

회귀 테스트: 짧게 눌렀다 떼면 정확히 1개, 3초 홀드에 5~10개.
(첫 시도에서 테스트가 "블록이 쌓여 올라와 플레이어에 막히면 더 못 놓는다" 를 잡아내
시험대를 다시 짰습니다 — 조준점이 여러 칸을 훑도록.)

검증: `node tests/run.mjs 10` → **183항목 × 10회 = 1,830 통과 / 0 실패.**

## v23~v24 — SOP 첫 사이클: 자문 10건 + 백로그 정리 (2026-09-06)

유저가 **"작업해줘" SOP** 를 등재한 뒤 처음 돈 사이클입니다.
고인물 자문이 **실제로 헤드리스로 돌려 확인한** 10건을 우선순위와 함께 보내 왔고,
그중 8건 + 자문이 덤으로 찾은 1건 + 백로그 6건을 처리했습니다.

### 자문이 짚은 것 (체감 ÷ 작업 순)
| # | 증상 | 고침 |
|---|---|---|
| 1 | **사다리에서 Shift 를 누르면 오히려 두 배로 미끄러졌다** (마크는 정지) | `crouchKey` 를 하강이 아니라 **정지**로. 무입력 하강은 −1.1 → −1.6 |
| 2 | **울타리를 점프로 넘고, 동물도 위로 걸어 나갔다** — 울타리가 장식이었다 | 충돌 높이만 **1.5칸** (마크와 같다 · 렌더는 1.0 그대로) + 동물 이동이 울타리·문을 막힌 것으로 본다 |
| 3 | **벽을 부숴도 사다리가 허공에 남았다** | `dropCross` 가드에 `isClimbable` — 벽 횃불과 같은 규칙 |
| 4 | **눈이 풀블록으로 쌓여 평지가 점프 장애물이 됐다** | `SH_SLAB` 으로 쌓는다 — 0.5칸이라 `STEP_UP` 0.6 안에 들어와 그냥 걸어진다 |
| 5 | **휠클릭 복사가 모양을 안 가져왔다** (계단을 복사하면 풀블록) | `hit.shape` 로 `S.shapeMode` 까지 맞춘다. 방향은 시선이 정한다(마크와 동일) |
| 6 | **물이 3칸에서 끊겼다** (마크는 7칸) | `MAXFLOW` 3 → **7** |
| 7 | **용암이 죽은 블록이었다** — 안 흐르고 불도 안 붙였다 | `lavaTick` — 주변 10칸을 드문드문 훑어 가연물 옆에 불을 놓는다 (기존 `Q.fireQ` 재사용) |
| 8 | **TNT 가 부싯돌 대는 순간 터졌고, 연쇄는 죽은 코드였다** | 도화선 **3초** + 흰 상자가 점점 빨리 깜빡임 + 치직 소리. 폭발 반경 안의 TNT 는 지우지 않고 **0.3~0.8초 도화선으로 연쇄** |
| 9 | **동물이 46칸 밖에서 플레이어 옆으로 순간이동했다** | 주변 40칸에 **한 마리도 없을 때만** 데려온다. 가둬 둔 무리는 그대로 |
| — | (덤) **얼음을 깨면 공기가 됐다** | 마크처럼 **물**이 남는다 |

10번(잔디 번짐·죽음)은 다음 사이클로 넘겼습니다.

### 백로그에서 함께 정리한 것
- **`Ctrl+Shift+F` · `/fill 공기`** — 고른 영역을 한 번에 비운다 (되돌리기 한 번, 기반암은 남는다)
- **달 위상 저장** — `md` 필드. 없던 저장은 0으로 시작하므로 포맷 버전은 v5 그대로
- **조작키 충돌 검사** — 이동키·이미 쓰는 키로는 재배치되지 않는다
- **재배치가 화면에 반영** — `[data-bind]` 표시를 단 곳과 힌트 줄이 따라 바뀐다 (v16 잔여)
- **`/undo <n>` · `/redo <n>`** · `/give` 뒤 핫바 즉시 갱신

### 걸린 함정
1. **`unstick()` 이 울타리를 무력화했다.** 울타리의 "보이지 않는 0.5칸" 을 진짜 블록으로 착각해
   점프한 사람을 울타리 **위로** 밀어 올렸다. → `boxHitsWorld(.., ignoreTall)` 로 갈라,
   갇힘 탈출은 진짜 블록만 본다.
2. **충돌 판정이 자기 칸 위쪽만 훑고 있었다.** 상자를 1.5칸으로 늘려도 `y0 = floor(py)` 부터
   훑으니 한 칸 아래의 울타리를 못 봤다. → `y0 - 1` 층까지 보되 그 층에서는 울타리·문만 본다.
3. 테스트 두 개가 **랜덤에 걸렸다** — 폭발 가장자리는 확률로 건너뛰므로 연쇄 시험은 바로 옆 칸에,
   용암 점화는 표본 범위를 18 → 10칸으로 좁혀 확정적으로 만들었다.

## v25 — 세계가 스스로 변한다 (2026-09-06)

1차 사이클에서 넘긴 자문 10번과, 자문이 덤으로 짚은 `fireOrigin` 문제를 처리했습니다.

- **잔디 번짐·죽음** — 흙을 메우면 옆 잔디가 번져 오고, 덮으면 흙으로 돌아간다.
  기둥을 뽑아 **지표만** 본다(허공을 헛짚지 않는다). 2초에 12기둥.
  **`isTouched` 칸은 절대 건드리지 않는다** — v19 의 "날씨가 내 건축물을 개조한다" 재발 방지.
- **`S.fireOrigin` 이 전역 하나였다** — 두 번째로 불을 붙이면 첫 불이 새 원점 기준으로
  번짐 상한을 재서 갑자기 멈췄다. `S.fireOrigins` 배열(최대 12개)에 **가장 가까운 원점**으로 잰다.

테스트에서 걸린 것 둘:
- 잔디 표본이 **너무 성겼다.** 48×10×48 부피에서 무작위로 뽑으니 목표 칸을 만날 확률이 27% 였다.
  → 기둥(x,z)을 뽑고 `topMap` 으로 지표 y 를 바로 집는 방식으로 바꿔 표본 하나하나가 후보가 됐다.
- **덮인 잔디는 지표가 아니다.** 돌로 덮으면 `topMap` 이 돌을 가리켜 그 아래 잔디를 영영 못 봤다.
  → 지표가 빛을 막는 블록이면 **바로 아래 한 칸**을 먼저 본다.

## v26 — 자문 2차: 아무도 안 보던 구석 (2026-09-06)

2차 자문은 소리·손·HUD·하늘·모바일·"첫 5분" 을 봤습니다. 이번 사이클에 넣은 것:

| # | 증상 | 고침 |
|---|---|---|
| 4 | **폰에서는 블록 목록도 웅크리기도 없었다** — 39종 중 10종만 평생 쓴다 | 터치 버튼 `목록`·`웅크림` 추가. **놓기는 홀드로 반복**(키보드와 같은 `PLACE_DELAY/REPEAT` 경로) |
| 6 | **열어 둔 울타리 문도 동물이 못 지나갔다** — v24 울타리가 이것 없이는 미완성 | `penned` 판정이 `shapeAt !== 1`(닫힘)일 때만 막는다 |
| 3 | **지붕을 얹으면 동물이 그 자리에서 얼어붙었다** | **되돌렸다** — 아래 "10회 반복이 잡아낸 것" 참고. 물 회피와 충돌한다 |
| 7 | **캄캄한 굴에서 손과 든 블록만 형광등처럼 환했다** | 서 있는 칸의 `lightSky×dayLight`·`lightBlk` 로 손 색을 낮춘다 (4/s 로 부드럽게) |
| 10 | **달 위상 8단계가 순수 장식이었다** | 밤의 밝기 하한을 `0.10 + 0.09×보름도` 로 — 보름밤은 돌아다닐 만하고 그믐밤은 코앞도 안 보인다 |

남긴 것(다음 사이클): 미니맵 표식 그리기 · 소리에 방향 주기(`PannerNode` 가 통째로 죽은 코드) ·
블록 이름 한국어화 · 실내에서 빗소리 줄이기 · 비가 불을 끄기.

**10회 반복이 잡아낸 것 — 그리고 되돌린 것:**
`groundAt` 을 "발밑을 본다" 로 바꾸자 **얕은 물이 걸어 들어가는 길이 됐다.**
물 아래 두 칸에 바닥돌이 있으면 그것을 땅으로 보고, 발밑 판정(`footB`)도 돌을 가리켰다.
4/10 회로만 터져 **1회 실행으로는 절대 안 보였을** 결함이다.

두 겹으로 막아 봤지만(젖은 기둥은 `topMap` 으로 넘기기 + 기둥 훑어 젖음 판정)
그래도 2/10 이 남아 **지붕 항목만 되돌렸다.** 기둥 훑는 젖음 판정은 그 자체로 개선이라 남겼다.
다시 손댈 때는 **물 회피와 지붕 아래 보행을 한 판정으로 묶어야 한다** —
두 규칙이 같은 `groundAt` 하나를 서로 다른 뜻으로 쓰고 있는 것이 진짜 원인이다.

## v27 — 자문 2차 나머지 (2026-09-06)

- **미니맵에 표식이 그려진다** — `B` 로 찍고 "탐험가" 과제까지 뜨는데 화면엔 아무것도 없었다.
  화면 밖 표식은 가장자리에 눌러 붙인다(작은 점). 테스트는 **픽셀 차이**로 확인한다.
- **실내·지하에서 빗소리가 잦아든다** — 지하 30칸에서도 똑같이 들렸다.
  입자는 이미 `columnTop` 으로 지붕을 보고 있었는데 소리만 안 봤다. 완전히 끊지 않고 12% 를 남긴다.
- **비가 하늘 뚫린 불을 끈다** — 방화 실수를 하늘이 수습해 준다. 지하 굴의 불은 그대로 산다.

남은 자문 항목: 소리에 방향 주기(`PannerNode` 가 통째로 죽은 코드) · 블록 이름 한국어화.

## v28 — 블록 이름 한국어화 (2026-09-06)

`NAMES` 39개가 전부 영어였다 — `COBBLE`·`DEAD BUSH`·`POPPY`. 양털만 "WOOL 흰색" 으로 반쪽 한국어라 더 헷갈렸고,
목록 검색창에 "돌" 을 쳐도 0개였다. **CLAUDE.md 4번(UI 문구는 한국어)에 정면으로 걸리는 자리.**

- `NAMES` 를 한국어로, 영어는 **`NAMES_EN`** 에 남겼다 (`nm(b, 한글, 영어)` 한 줄씩).
- 목록 검색과 `/fill`·`/give` 의 `findBlock` 이 **둘 다** 알아듣는다 — "조약돌" 도 "cobble" 도.
  `findBlock` 은 낱말 단위 정확 일치 → 공백 제거 일치 → 부분 일치 순서로 본다.
- 양털은 "흰색 양털" 꼴로 통일.
- 회귀 테스트: `ALL_BLOCKS` 에 라틴 대문자만인 이름이 0개여야 한다(TNT 제외) — 다음에 블록을 넣으며
  영어로 적으면 바로 걸린다. 기존 테스트 4개는 영어 이름을 단언하고 있어 한국어로 고쳤다.

## v29 — 소리에 방향이 생겼다 (2026-09-06)

`audio.js` 의 `at(x,y,z)` 패너는 **아무도 import 하지 않는 죽은 코드**였다. `listenAt()` 은 `loop.js` 가 매 프레임
듣는 사람 방향을 갱신하고 있었으니 — 귀만 돌고 소리는 전부 정중앙이었다 (자문 2차 2번).

- `tone(freq, dur, type, gain, node)` · `crunch(dur, gain, cutoff, node)` — 마지막 인자로 나갈 자리를 받는다.
  비우면 예전처럼 `masterGain`. 패너는 한 번 쓰고 버린다(소리가 끝나면 그래프에서 떨어진다).
- 자리를 아는 세 곳만 갈아 끼웠다: **용암 뽀글**(가장 가까운 용암 칸 — 예전엔 개수만 셌다) ·
  **동물 울음**(`m.x, m.y+0.6, m.z`) · **TNT 점화음·치직·폭발음**(TNT 칸).
- 발소리·채굴음·놓기음은 플레이어 자리라 그대로 정중앙이 맞다.

회귀 테스트는 `PannerNode` 가 없는 브라우저면 건너뛴다(SwiftShader 헤드리스는 있다).

## v30 — 지붕 아래 동물, 이번엔 제대로 (2026-09-06)

v26 에서 되돌린 항목을 다시 붙였다. 지난번과 다른 점 셋:

1. **먼저 확정적 시험대를 만들었다** — 1칸·2칸 깊이 물웅덩이로 동물을 8초 직진시켜 물가를 넘는지 본다.
   지난번엔 이게 없어서 10회 반복이 결함을 찾아 줘야 했다.
2. **땅의 정의를 하나로 묶었다** — "지금 높이 ±2 에서 첫 단단한 블록" 인데, **그 위 몸 칸이 물·용암·얼음이면
   땅이 아니고, 딛는 것이 얼음이면 땅이 아니다.** 못 찾으면 topMap 으로 떨어지지 않고 그냥 안 간다.
   지난번 구멍은 정확히 이 조건이 없어 "얕은 물의 바닥돌" 을 땅으로 본 것.
3. **진짜 원인은 걷기가 아니었다.** 진단 스크립트로 물 위에 있는 동물의 기둥을 찍어 보니 —
   `startY: 5, top: 11, footB: 모래, inCell: AIR` — **해저 공기 주머니에 놓여 태어난** 동물이었다.
   (`generate()` 는 `refreshAllTops()` 를 부르므로 topMap 은 멀쩡했다. 지형 자체에 물 밑 동굴이 있다.)
   → `strandedAt()`: 0.5초마다 기둥 겉면이 물·용암·얼음이거나 겉면보다 아래에 있으면 `placeMob` 으로 다시 놓는다.
   놓인 뒤 물이 차는 경우까지 한꺼번에 덮는다.

교훈: **랜덤 세계 시험이 1/10 로 흔들리면, 걷는 코드를 의심하기 전에 "어디에 놓였나" 를 먼저 찍어 본다.**

## v31 — 자문 3차: 확정 버그 넷 (2026-09-06 · 12:30 마감 전 마지막 사이클)

3차 자문은 시드 24개를 실제로 생성해 세었다 (`scratchpad/probe3.mjs` · `probe3b.mjs` 에 잣대가 남아 있다).
시간이 12분뿐이라 **실측으로 확정된 버그 중 몇 줄짜리**만 골랐다.

| # | 증상 (실측) | 고침 |
|---|---|---|
| 2 | **되돌리기가 물을 복원 못 했다** — 근원 하나가 113칸으로 퍼진 뒤 `Ctrl+Z` → 113칸 그대로. 흐르는 물(레벨 3)을 캐고 되돌리면 **근원(0)** 이 되어 무한 물이 생김 | `rec.wl` 에 편집 전 물 레벨을 남기고 `applyCell` 이 복원한다. WATER 가 오가면 `enqueueDryAround` + `enqueueWaterAround` 를 둘 다 건다 |
| 6 | **새 세계 4.8분 만에 첫 밤** — 07:12 시작 · 하루 10분. 튜토리얼 4단계에 닿기 전에 캄캄해진다 | 하루 기본 **20분**(마크와 같다) · 새 세계 **06:00**(t=0.25). 저장된 opts 는 그대로라 기존 유저는 10분 유지 |
| 5c | 화면의 유일한 영어 버튼 "CLICK TO PLAY" | "플레이" |
| 7b | "쾅" 과제가 **부싯돌을 대는 순간** 떴다 — 도화선 3초를 넣고도 | `unlock("boom")` 을 `explode()` 로 |

### 다음 사이클로 넘긴 것 (자문 3차 · 전부 실측 근거 있음)
- **스폰 위치** — 24시드 중 7개가 나무 꼭대기·오두막 지붕·바다 위에서 시작. `spawn()` 을 나선 탐색 + `topMap` 블록이 잔디·흙·모래·눈일 때만. (체감 상 · 15줄 · 시드 20개 회귀 테스트)
- **숲 없음 + 바이옴 편중** — 나무 4~71그루/세계, 바이옴 한 덩어리가 55%. 숲 노이즈 + 바이옴 임계를 백분위로
- **동굴 입구 없음 · 바다 밑 공기** — 지표 3칸이 절대 안 뚫림, 물 아래 공기 300~1,400칸(편집하면 갑자기 쏟아짐)
- **폰 튜토리얼 안 보임** — `#hint` 가 480px 아래에서 display:none · 첫 방문 토스트가 시작 화면 뒤에서 소진
- 다이아·금 과제 없음 · 다이아 0개 시드 · 슬롯 UX(빈 슬롯이 시드 무시·지우기 없음·시각 없음) · 게임패드 배치 · 파편 단색

10회 재실행에서 새 좌초 시험이 1/10 흔들렸다 — `placeMob` 이 플레이어 주변 18~30칸에서 24번을 다 물에
떨어뜨리면 그대로 실패했다. 코드는 **못 찾으면 8~30칸으로 한 번 더**, 시험은 플레이어 주변 65×65 를 마른 돌판으로.

## v32 — 다이아몬드! (2026-09-06 · 12:30 직전)
광석 과제가 석탄·철뿐이어서 깊이 7 아래까지 내려가 다이아를 찾아도 아무 일이 없었다 (자문 3차 #7a).
`gold`(금맥) · `diamond`(다이아몬드!) 과제를 넣고, 다이아는 한 옥타브 위 세 음으로 따로 기념한다.
과제 수를 고정한 곳은 없었다 — 전부 `ACHIEVEMENTS.length` 를 읽는다. "다이아 0개 시드" 보장 루프는 백로그에 남겼다.

## v33 — 첫 5분: 스폰 · 폰 튜토리얼 · 다이아 보장 (2026-09-06 · 13:00 사이클)

자문 3차의 실측 항목 중 "처음 온 사람이 겪는 것" 셋.

- **스폰** — 24시드 중 7개가 나무 꼭대기·오두막 지붕·바다 위에서 시작했다. `spawn()` 이 가운데 기둥의
  첫 블록을 그냥 잡았기 때문. 가운데에서 반지름 20까지 **나선**으로 `topMap` 블록이 잔디·흙·모래·눈이고
  `SEA+1` 위이며 머리 두 칸이 비어 있는 첫 기둥을 고른다. 못 찾으면 예전 동작. 시드 12개 회귀 테스트.
- **폰 튜토리얼** — `#hint` 가 `max-height: 480px` 미디어 쿼리에서 `display:none` 이라 가로로 든 폰은
  튜토리얼을 **평생** 못 봤다. 이제 9px 한 줄로 남긴다. 문구도 `TUT_TOUCH`(캐기·놓기·목록·스틱·웅크림 버튼)로 —
  **단계 수를 `TUT` 와 같게** 둬야 `advanceTut(step)` 의 인덱스가 맞는다.
  첫 방문 토스트는 부팅 0.9초 뒤(시작 화면 뒤에서 소진)가 아니라 `beginPlay()` 의 첫 진입에서 나간다.
- **다이아 0개 시드** — 3%×~500회가 동굴·용암에 걸러져 0개인 시드가 있었다. 광석 루프 뒤에
  "y 0~6 층의 다이아를 세어 8개 미만이면 최대 600회 재시도". 시드 8개 회귀 테스트.

**10회 반복이 잡은 것 (v33 재실행):** 좌초 시험이 1/10 "기둥 겉면보다 아래" 로 흔들렸다. 파고 보니
`strandedAt()` 이 **단단한 지붕 아래도 좌초로 봤다** (`m.y < topMap`). 즉 헛간의 양이 0.5초마다 밖으로 튕겨 나갔고,
v30 의 지붕 시험은 "움직였다" 만 재서 **순간이동으로도 통과**하고 있었다. 정의를 바로잡았다 —
겉면이 물·용암·얼음이고 그 아래에 있거나, 몸·발이 액체에 잠긴 것만 좌초. 지붕 시험에는
"지붕 영역 안에 그대로 있어야 한다" 를 더했다 (걸어서도 벗어나지 않게 4초만 걷는다).

## v34 — 섬이 섬다워졌다 (2026-09-06 · 13:00 사이클)

자문 3차의 지형 묶음 넷을 한 번에. 잣대는 자문이 남긴 방식대로 **8시드 실측**(`scratchpad/measure.mjs`).

| 지표 | 전 | 후 |
|---|---|---|
| 굴 입구(지표 3칸 안에 공기) | **0개** (초원에선 아무 데나 파야 했다) | 36~278개 |
| 바다 밑 공중 물 | 300~1,400칸 | **0칸** |
| 바이옴 최대 비율 | 55~69% | **39%** |
| 나무 | 4~71그루 | 36~89그루 |

- **굴 입구** — 모든 동굴 조각이 `y < h-2` 안에 있어 지표 3칸이 절대 안 뚫렸다.
  `carvedBelow` 플래그를 두고 **아래가 이미 뚫렸을 때만** 이어서 뚫는다. 언덕 옆구리에 입이 벌어진다.
- **공중 물** — 모래 침강이 바다 밑에서도 일어나 해저에 구멍을 냈다. 물이 공기 위에 떠 있다가
  근처를 편집하는 순간 쏟아졌다. 위가 물이면 마른 동굴로 안 떨어뜨린다 (한 줄).
- **바이옴** — 고정 임계(0.30/0.62)는 96칸 섬에 노이즈 주기가 두 번밖에 안 들어가 시드에 따라
  한 덩어리가 절반을 먹었다. **그 세계의 t 분포 백분위(30%·65%)** 로 바꿨다.
- **숲** — 숲 노이즈(0.06)로 덩어리를 만들고 그 안에서 밀도 20배.
  **함정:** 처음엔 나무가 19~46 밖에 안 늘었다. 겹침 방지 반경을 2로 뒀더니 **5×5 당 한 그루가 상한**이라
  밀도를 올려도 소용이 없었다. 반경 1로 줄이자 36~89. 상한을 만드는 규칙을 먼저 의심할 것.

## v35 — 저장 슬롯이 목록다워졌다 (2026-09-06)

자문 3차 #8. 셋 다 실측으로 확인된 빈틈이었다.

- **빈 슬롯이 SEED 입력을 무시했다** — 시드를 적고 2번 슬롯을 눌러도 무조건 무작위였다.
  이제 `seedIn` 값을 `hashSeed` 로 쓰고, 비어 있을 때만 무작위. 토스트에 시드를 찍는다.
- **슬롯을 지울 수 없었다** — `clearSave()` 는 export 되어 훅에도 있는데 UI 어디서도 안 불렀다.
  슬롯 버튼 오른쪽 위에 ✕. 파괴적 조작이라 **4초 안에 두 번**(규칙 8, `altBtn` 과 같은 방식).
- **언제 저장했는지 몰랐다** — 저장 JSON 에 `at: Date.now()` 를 넣고(선택 필드라 `v` 는 5 그대로)
  슬롯에 "SEED 4321 · 12분 · 3시간 전". `agoText()` 는 방금/분/시간/일로 끊는다.

## v36 — 파편에 무늬가 생겼다 (2026-09-06)

자문 3차 #10. `burst()` 가 `AVG_SIDE`(타일 평균색) 한 색에 밝기 지터만 줘서, 도트로 정성껏 그린 무늬가
파편에서는 전부 사라졌다. 잔디를 캐면 초록 점 14개, 돌이면 회색 점 14개.

- `tileSwatch(i)` — 부팅 때 타일마다 **실제 픽셀 16개**를 고르게 뽑아 둔다(투명 픽셀 제외).
  `SWATCH_SIDE[blockId]` 로 두고, `burst` 는 파편마다 그중 하나를 쓴다.
- 캐기 파편 14 → **24개**. `PMAX 220` 이라 초당 3블록을 캐도 50개 남짓이라 넉넉하다.

훅에 `pColArray()`·`pCount()` 를 함수로 노출했다 — `pCount` 는 live binding 이라 객체에 값으로 넣으면
훅 만들 때의 0 이 박힌다.

**10회 반복이 잡은 것 (v36):** 두 시험이 각각 1/10 흔들렸다. 둘 다 **시험 쪽 문제**였다.
- 파편 시험이 앞선 시험이 남긴 파편에 `PMAX`(220)를 먹혀 24개를 못 채웠다 → 먼저 `updateParticles(5)` 로 비운다.
- 좌초 시험은 `placeMob` 이 무작위 24회 시도라 한 번에 성공하지 못할 수 있는데 1.5초만 돌렸다
  → 물 밖으로 나갈 때까지 최대 5초(나가면 즉시 멈춤).
**교훈:** 전역 자원(파티클 풀)과 무작위 재시도를 쓰는 코드는 시험도 그 성질을 감안해 써야 한다.

## v37 — 게임패드를 베드락 배치로 (2026-09-06)

자문 3차 #9. 트리거(RT 캐기·LT 놓기)는 베드락과 같아 그대로 두고, 나머지를 맞췄다.

| | 전 | 후 |
|---|---|---|
| LB/RB | 트리거와 **똑같이** 캐기/놓기 (있으나 마나) | **핫바 좌/우** |
| LT 놓기 | `tapped` — 한 번 누르면 한 번 | **홀드 반복** (마우스·터치와 같은 `PLACE_DELAY/REPEAT` 경로) |
| Y | 비행 토글 | **블록 목록** (베드락의 인벤토리 자리) |
| A | 점프만 | 점프 + **A×2 비행** (마우스 Space×2 와 같은 300ms 판정) |
| L스틱 클릭 | 없음 | **달리기** |
| 메뉴(9) | 블록 목록 | **나가기**(`endPlay`) |
| 시작 화면 | A 를 눌러도 무반응 (`pollGamepad` 는 playing 일 때만 돈다) | `pollGamepadMenu()` 가 `animate()` 에서 A 만 본다 |

시험은 `navigator.getGamepads` 를 가짜 패드로 갈아 끼워 버튼을 하나씩 눌러 본다.

## v38 — 용암이 흐른다 (2026-09-06)

자문 2차 #4 의 남은 절반. 용암은 "안 흐르고 안 태우는 주황색 벽돌" 이었다.
v24 에서 **불 붙이기**(`lavaTick`)를 넣었고, 이번에 **흐르기**를 넣었다.

- `lavaFlowTick` / `lavaDryTick` — 물 쪽(`waterTick`/`dryTick`) 구조를 그대로 따라가되
  **`LAVA_FLOW = 2`**(마크 오버월드 4의 절반 · 96칸 섬 스케일)에 **물의 4분의 1 속도**(4틱마다 한 번).
  걸어서 피할 수 있어야 한다.
- 레벨은 `waterLvl` 배열을 함께 쓴다 (이름은 물이지만 "액체 레벨" 이다). 저장 포맷 그대로.
- **물에 닿으면 조약돌** — `waterTick` 의 `meltsLava` 와 대칭으로 용암 쪽에도 넣었다.
- 근원을 캐면 흘러 나간 것이 물러난다 (`lavaDryTick`). 없으면 캔 자리에 자국이 영영 남는다.
- 용암은 광원(EMIT 15)이라 흐를 때마다 `relightLocal` 을 부른다.

성능: 프레임 **2.1ms**(예산 16.6) · 용암 틱은 큐가 비면 0ms. 생성 10.7 · 조명 25.7 · 메싱 36.9ms 로 예전과 같다.

## v39 — 진짜 2칸 문 (2026-09-06)

`docs/GAMEPLAY.md` 블록 표에 "문" 이라 적어 놓고 실제로는 **울타리문(GATE)뿐**이었다.
집을 짓는 게임에서 문이 없는 것이 가장 눈에 띄는 구멍이라 넣었다.

- `DOOR = 55` (36~51 양털 · 52 TNT · 53 FIRE · 54 FLINT 다음의 첫 빈 번호) · 타일 57
- **상태를 새로 만들지 않았다** — 모양 바이트에 `SH_DOOR_N..W`(17~20 닫힘) + 4(21~24 열림)로 담고,
  **아래위 어느 칸인지는 밑칸이 문인지로 판별**한다(`doorOther`).
- 놓기: 위칸이 비어 있어야 하고, **서 있는 쪽을 바라보게** 놓인다. 두 칸을 함께 쓴다.
- 열기: 두 칸이 **함께** 열린다. 반쪽만 열리면 문이 아니다.
- 캐기: 반쪽을 캐면 나머지도 걷는다 — 허공에 반쪽이 남지 않는다.
- 닫힌 문은 사람도 **동물도** 막는다(`mobs.js` 의 `blocks()` 에 추가). 울타리 + 문 = 목장이 완성된다.

**걸린 것:** `tidy-imports` 를 훅에 쓰기 **전에** 돌려서 `doorOpen` import 가 지워졌고 부팅이 죽었다
(`doorOpen is not defined`). 도구는 코드를 다 쓴 **뒤에** 돌린다.

## v40 — /clone 과 문 발견성 (2026-09-06)

- **`/clone <dx> <dy> <dz>`** — 고른 영역을 그 방향으로 한 벌 더. 계단·기둥처럼 되풀이되는 것을
  손으로 다시 짓지 않게 한다. 이미 있던 `copySelection` + `pasteClip` 을 명령 하나로 묶은 것뿐이다.
- **두 번째 핫바에 문을 넣었다** (`DEFAULT_BAR2` 의 `WOOL0` 자리). 새 블록은 목록에만 있으면 못 찾는다.
  기존 저장은 `barAlt` 를 그대로 두므로 영향 없다.

**백로그에서 뺀 것 — "원목 축 3방향":** 확인해 보니 문제가 아니었다. `TILES[LOG] = [나이테, 껍질, 나이테]`
이고 `faceKindFor` 가 X·Z 축일 때만 면을 돌린다. 즉 **기본(세로)이 이미 위아래에 나이테를 보여 준다** —
그게 Y축 표시다. 백로그 항목 자체가 오해였다.

**화면으로 확인함(v39):** 벽 가운데에 닫힌 문·열린 문을 나란히 세워 렌더해 봤다
(`scratchpad/doorshot.mjs`). 닫힌 쪽은 문간을 막고 널 홈·손잡이가 보이고, 열린 쪽은 옆 기둥에 접혀
통로가 뚫린다. **x축 면 뒤집힘(v21)을 몇 달 못 잡은 이유가 "테스트가 화면을 안 봐서" 였으므로,
새 블록은 반드시 한 번 렌더해서 눈으로 본다** (D18).

알고 넘어간 것: `lightPass` 에 `DOOR` 를 넣어 **닫힌 문도 빛을 통과시킨다.** 마크는 막지만,
`lightPass` 가 블록 단위(상태별이 아님)라 막게 하면 **열린 문간이 캄캄해진다.** 지금 구조에선 통과가 낫다.

**화면으로 확인함(v38 용암):** 절벽 위에 용암을 부어 렌더해 보니 평지에서 2칸씩 퍼지는 모습은 맞았는데,
그 지형에선 절벽 끝까지 닿지 않아 **낙하를 눈으로 못 봤다.** 코드를 다시 보니 낙하는 되는데
**시험이 없었다** — 가로 도달·마름·조약돌만 재고 있었다. 낭떠러지 시험을 더했다.
화면 확인은 버그를 못 찾아도 **시험의 구멍**을 찾아 준다.

**화면으로 확인함(v34 지형):** 시드 9717 의 가장 빽빽한 기둥에서 내려다보니 참나무 캐노피가 이어지고
설원 쪽에 가문비나무가 서 있다 — 예전의 "듬성듬성 4~71그루" 와 확실히 다르다.
(찍을 때 `yaw = π` 가 **+z** 방향이라 숲 반대쪽을 봤다. 이 엔진은 `fwd = (-sin yaw, 0, -cos yaw)` 다.)

## v41 — 죽은 섬을 되살렸다 (2026-09-06 · 자문 5차 1~3위)

### 1. 동물·물고기·새가 **한 마리도 없었다** (체감 특대 / 1줄)
`seedMobs()`·`seedFlocks()` 가 **`newWorld()` 안에서만** 불렸다. 즉 `R` 두 번으로 새 세계를 만든
그 세션에만 동물이 있고, **새로고침하거나 저장에서 이어하면 죽은 섬**이었다.
실측: 부팅 직후 `mobs.length === 0`, `beginPlay()` 뒤에도 0, 60틱 뒤에도 0.

> **왜 테스트가 못 잡았나 —** 동물 시험 6개가 **전부 스스로 `B.seedMobs()` 를 부르고 있었다.**
> 시험이 구멍을 메워 주고 있었다. v24 울타리 · v26 열린문 · v30 지붕보행 · v33 좌초 · v39 문막기 —
> **동물 관련 다섯 사이클 전부가 실제 플레이어에게는 보이지 않았다.**
> 새 회귀 시험은 `seedMobs()` 를 **부르지 않고** 부팅만으로 14마리를 단언한다.

### 2. 문·눈·불이 되돌리기 기록을 먹었다 (체감 상 / 9글자)
문 한 번 여닫으면 기록 2개(두 칸), 10번이면 20개. `HISTORY_MAX` 240 이라 120번이면 오전 내내
지은 것의 되돌리기가 통째로 날아간다. `applyEdit(record)` 는 `S.future` 도 비우므로 다시하기도 사라진다.
→ **세계가 스스로 하는 편집은 `record=false`.** 문·울타리문 토글, 눈 쌓기/녹이기, 불 번짐/꺼짐.
사람이 **놓고 캐는** 것과 **직접 붙인 첫 불**(v19)은 그대로 기록한다.

**덤으로 낫는 것:** 눈이 영영 안 녹던 버그 — 눈을 `record=true` 로 놓으니 `markTouched` 가 찍히고,
녹이는 쪽이 `isTouched` 로 건너뛰어 **자기 손자국을 사람 손자국으로 착각**하고 있었다.

### 3. 이어하기하면 "내 건축물 보호" 가 풀렸다 (체감 중 / 2줄)
`touched`(44만 칸)가 저장에 없었다. v19·v25 에서 넣은 `isTouched` 가드가 **새로고침 한 번에 사라진다.**
→ `tc: encodeArrB64(touched)` (0/1 이라 RLE 가 잘 먹는다) · 선택 필드라 **저장 포맷 v5 그대로.**

## v42 — 복제한 물과 가둔 동물 (2026-09-06 · 자문 5차 5~6위)

- **복사·붙여넣기가 물을 전부 근원으로 만들었다.** 근원 하나에서 109칸으로 퍼진 연못을
  `Ctrl+C`→`Ctrl+V` 하면 109칸이 **전부 근원**이 됐다 (`applyEdit` 은 손으로 놓은 물을 근원으로 본다).
  정원 한 구석을 복제했을 뿐인데 근원 109개가 각각 7칸씩 뻗어 세계가 잠긴다.
  → 클립보드에 `levels` 를 담고, 붙여넣을 때 물·용암이면 레벨을 되쓰고 `enqueueDryAround` 를 건다.
- **울타리 안 동물을 게임이 몰래 데려갔다.** "주변 40칸에 한 마리도 없으면 데려온다"(v24) 규칙이
  배열 순서대로 도느라 **하필 가둔 놈부터** 뽑아 갔다. 울타리에 부딪히면 `m.pennedAt = 12`(초)를
  세우고, 그동안은 데려가지 않는다.
- **덤(3글자):** 먹이 소리가 `at(m.x, ...)` 로 나가고 있었다 — `m` 은 앞 루프에 남은 **마지막 동물**이라
  엉뚱한 쪽에서 소리가 났다. 먹인 동물(`mm`)로 고쳤다. v29 에서 소리에 자리를 준 뒤에야 드러난 버그다.

**10회 반복이 잡은 것(v42):** 가둔 동물 시험이 7/10 흔들렸다. `pennedAt`(부딪힌 기억)만 믿었더니
**한 번도 안 부딪힌 동물을 첫 프레임에 데려갔다.** 부딪힘을 기다리지 말고 **주변 4칸에 울타리·문이 있는지
직접 본다**(`nearPen`). 데려가는 조건이 원래 드물어 그때만 9×9 를 훑으므로 비용도 없다.

## v43 — 허공에도 영역을 찍는다 (2026-09-06 · 자문 5차 4위)

- 20칸 탑 자리를 고르려면 **임시 블록을 놓고 → 찍고 → 지우는** 짓을 반복해야 했다.
  `raycast(6)` 이 허공을 그냥 지나치기 때문. → `aimCell()` 이 **블록을 맞히면 그 칸, 허공이면 시선 4칸 앞**을 준다.
- 토스트가 `"영역 8,748칸"` 뿐이라 **가로×높이×세로가 안 보였다.** 건축에서 필요한 숫자는 칸수가 아니다.
  → `"영역 27×12×27 · 8,748칸"`.

## v44 — 뜨는 문과 사라진 지도 (2026-09-06 · 자문 5차 덤)

- **문 밑을 캐면 문이 허공에 떴다.** `needsFloor(DOOR)` 은 true 인데 `dropCross` 는
  `isCross`·`isClimbable` 만 보고 있었다 — 사다리를 넣을 때 넓힌 가드에 문을 빠뜨렸다.
  **어제 넣은 기능의 버그를 오늘 자문이 잡아 줬다.** 아래칸이 판단하고 윗칸도 함께 걷는다.
- **`docs/CODEMAP.md` 가 `mobs.js`·`cloud.js`·`version.js` 를 빠뜨리고 있었다** (24개라 적혀 있는데 실제 27개).
  `tools/codemap.mjs` 의 `ORDER` 가 **손으로 적은 목록**이라 모듈을 새로 넣으면 조용히 빠졌다.
  이제 `readdirSync` 로 실제 파일을 훑어 목록에 없는 것은 뒤에 붙이고 **이름을 찍어 알린다.**
  자문의 지적이 날카로웠다 — *"지도에 없는 모듈은 아무도 안 본다"*. 실제로 v41 의 `seedMobs` 구멍이
  `mobs.js` 에 있었다.

## v45 — 굴에서 보이는 보상 (2026-09-06 · 자문 5차 덤)

v34 로 굴 입구가 뚫려 이제 걸어 들어갈 수 있는데, **걸어도 볼 게 없었다.**
실측: 세계당 다이아 11~25개 중 **공기에 면한 것이 0~3개**, 금은 0~10개.
결국 y=5 에서 삽질하는 것이 유일한 길이었다.

`exposeVeins(kind, want)` — 광맥 옆의 **굴 벽 돌 한 칸**을 걷어 드러낸다
(광맥 자체는 건드리지 않는다 · 돌 너머가 공기여야 "벽" 이다 · 통돌 한가운데는 안 판다).
결과: 다이아 **2~6개**, 금 **2~14개**가 어느 시드에서나 눈에 띈다.

한계: `want`(다이아 6)까지는 못 채운다 — 굴에서 두 칸 이상 떨어진 광맥은 그대로 두기 때문이다.
시험은 **하한 2개**를 지킨다 (예전엔 0개인 시드가 있었다).

## v46 — 동물 번식 (2026-09-06 · 자문 5차 9위)

울타리(v24) · 열린 문 통과(v26) · 지붕 아래 보행(v30) · 2칸 문(v39) 까지 붙여 **목장이 지어지는데**
14마리 고정이라 **우리를 채울 방법이 없었다.** 채울 방법이 없으면 지을 이유도 없다.

- 꽃을 주면 `m.love = 20`(초). 같은 종 둘이 **3칸 안**에 있으면 새끼가 난다.
- 새끼는 60초 동안 **작게 그려지고 서서히 자란다** — 자라는 게 눈에 보여야 번식이 사건이 된다.
  새끼는 번식하지 않는다.
- `MOB_COUNT 14`(처음 뿌리는 수)와 **`MOB_MAX 24`**(번식 상한)를 나눴다.
- 하트 파티클은 **분홍 양털 색을 빌려** 쓴다 (새 텍스처를 만들지 않는다).
- 과제 **"목장주"**.

**설계 판단:** `breedTick` 은 `unlock` 을 부르지 않고 **태어난 수를 돌려준다.**
`mobs → edit` import 를 만들면 순환 위험이 생기고, 무엇보다 동물이 도전 과제를 알 이유가 없다.
과제는 부른 쪽(`loop.js`)이 준다.

## v47 — /expand 로 영역을 늘린다 (2026-09-06 · 자문 5차 덤)

조준 사거리가 6칸이라 **30칸 영역은 두 모서리 사이를 날아가야** 했다.
`/expand <±dx> <±dy> <±dz>` — 고른 상자를 여섯 방향으로 늘린다. 세계 밖으로는 안 나간다.
v43(허공에도 찍힌다)과 짝이다: 대충 찍고 명령으로 다듬는다.

**시험을 쓰다가 뜻을 바꿨다.** 처음엔 "음수면 줄인다" 로 만들었는데, 코드는 `lo += d` 라
실제로는 **아래로 늘어났다.** 어느 쪽이 맞나 보니 — 여섯 방향으로 늘릴 수 있는 쪽이 훨씬 쓸모 있고
WorldEdit 의 `//expand <n> <dir>` 과도 같다. **코드를 두고 설명과 시험을 맞췄다.**

**이름을 바꿨다:** 처음엔 `/sel` 이었는데 `seed` 와 앞글자가 겹쳐 **`se` 자동완성이 깨졌다**(10/10 실패).
WorldEdit 과 같은 `expand` 로. 명령을 새로 넣을 때는 **기존 명령과 앞글자가 겹치는지** 본다.

## v48 — 지은 것을 보는 과제 (2026-09-06 · 자문 5차 7위)

자문의 지적: **과제 30개가 전부 "기능을 한 번 만져 봤나" 였다.** 캔다·놓는다·내려간다·밟는다·터뜨린다.
40분이면 `collector`·`diamond` 빼고 다 뜨고, 그 뒤엔 아무것도 없다.
**이 게임이 하라는 건 짓는 것인데 지은 것을 보는 과제가 하나도 없었다.**

다섯을 넣었다 — 전부 **`touched`(사람이 손댄 칸)만** 센다. 자연 지형이 우연히 조건을 채우면
"내가 지었다" 는 느낌이 사라지기 때문이다.

| 과제 | 조건 |
|---|---|
| **내 집** | 문이 달린 27칸 이상의 밀폐 공간 (문 옆 공기에서 6방향 flood · 하늘이 보이거나 600칸을 넘으면 방이 아니다) |
| **전망대** | 한 기둥에 사람이 쌓은 블록 20칸 연속 |
| **다리** | 해수면 위, 아래가 물인 자리에 사람 블록이 20칸 이어짐 |
| **갱도** | y ≤ 8 에 사람이 파낸 칸 200개 + 횃불 10개 |
| **색칠** | 한자리(반경 32)에 사람이 놓은 양털 8색 |

도움말 과제 탭 맨 위에 **"다음에 해 볼 것 — 세 개"** 를 띄운다. 거의 다 딴 사람이 목표를 잃지 않게.

**걸린 것:** `DIRS` 는 `[+x, -x, +y, -y, +z, -z]` 라 **앞 4개에 수직이 섞여 있다.**
`for (d = 0; d < 4)` 로 수평만 돌려던 것이 문의 **±z 이웃을 아예 못 봐** 방을 영영 못 찾았다.
6개를 다 훑고 수직만 건너뛴다.

**성능:** 최악 7.4ms · 중앙값 1.7ms (10초에 한 번). 다섯을 다 따면 아예 돌지 않는다.

## v49·v50 — 세계를 갈아탈 때 부수던 것들 (2026-09-06 · 자문 6차 1~7위)

자문 6차의 한 줄 요약이 정확했다: **`newWorld()` 는 상태를 제대로 비우는데,
나머지 네 갈아타기 경로(슬롯 · 파일 가져오기 · 클라우드 내려받기 · 백업 복원)가
그 함수를 안 거치고 각자 반쪽씩 했다.**

| # | 증상 (실측) | 고침 |
|---|---|---|
| ③ | **세계 A 의 되돌리기 기록이 세계 B 로 따라와, `Ctrl+Z` 한 번에 599칸이 사라졌다** | `afterWorldSwap()` 첫머리에서 history·future·clip·selA/B·primed·fireOrigins·`resetQueues()` 를 전부 비운다 |
| ④ | **갈아타면 저장된 내 자리를 버렸다** (50.5,20 → 47.5,**14.2**) | `if (!loaded) spawn();` — 짓던 탑 꼭대기든 갱도 바닥이든 그 자리를 지킨다 |
| ② | **동물이 저장에 없었다** — 목장이 탭 하나 닫으면 빈 우리 | `mb` 필드(24마리 ≈ 200바이트). 없는 예전 저장만 새로 뿌린다 |
| ⑤ | **우리 안 물 한 칸에 동물이 20칸 밖으로 날아갔다** | 발밑 물은 좌초가 아니다(몸이 잠기거나 용암·얼음일 때만) + 가둔 동물은 `nudgeToDry` 로 **한 칸만** 민다 |
| ⑥ | **시계가 CSS 로 잘못 숨겨져 있었다** — 주석은 "좌표·시각·모양 세 줄" 인데 규칙이 2번째(시각)를 숨김 | `nth-of-type(2)` 를 lean 목록에서 뺐다 |
| ⑧ | **`/weather` 가 `setWeather` 를 건너뛰어** 비는 안 보이는데 눈은 쌓였다 | `setWeather(wv)` 한 줄 |
| ⑦ | **24마리에서 꽃을 줘도 하트만 뜨고 새끼는 안 났다** | 상한이면 `-1` 을 돌려 `mine.js` 가 안내한다. **JS 에서 `-1` 은 참**이라 그냥 두면 과제까지 떴다 |

**네 경로를 한 함수로 모은 것**이 ③·④를 한꺼번에 끝냈고, 다음에 생길 같은 사고도 막는다.
슬롯 전환 블록을 통째로 `afterWorldSwap("슬롯 n 을 불러왔습니다", true)` 로 바꿨다.

**또 시험 오염(v49):** 동물 저장 시험이 `eq(count, 14)` 로 숫자를 박아 10/10 실패했다 —
앞서 도는 **v46 번식 시험이 24마리까지 늘려 놓기** 때문이다. 필터를 걸고 돌릴 땐 v46 이 안 도니 통과했다.
**저장한 수와 견주도록** 바꿨다. 메모리 `blockyard-testing` 6번(전역 상태 누수)의 또 다른 얼굴이다 —
이번엔 전역이 아니라 **앞 시험이 만든 세계 상태**였다.

## v51 — 수평선 (2026-09-06 · 자문 6차 1위)

해변에서 바다를 보면 **물이 일직선으로 잘리고 그 위가 회갈색 안개판**이었다. 세계 끝에서는 아예
베이지색 허공. 스폰이 섬 한가운데라 아무 방향으로 40칸만 걸으면 2분 안에 이 그림을 봤다.
**47판을 고쳐 놓고도 첫인상이 "작은 상자 세계" 인 이유가 이것 하나**였다.

- 섬을 둘러싸는 물 평면(`outerSea`), y = **`SEA + 1 - 0.12`** — `mesh.js` 의 물 윗면 보정과 같은 값이라
  진짜 물과 이음새가 없다.
- 안개는 `voxUniforms` 를 **그대로 받아** 지형과 같은 곡선으로 녹는다. 가까운 곳엔 잔물결.
- 물속에서는 감춘다 (`updateOuterSea(camY)`) — 잠수 중에 머리 위로 판이 지나가면 안 된다.

**두 번 갈아엎었다:**
1. 정점 보간 안개 → 700칸짜리 삼각형 몇 장에 **이음새가 줄무늬로** 드러났다. 픽셀마다 `length(vW - cameraPosition)` 으로.
2. **구멍 뚫린 `Shape`** → 삼각분할이 겹쳐 동일 평면 z-fighting(대각선 줄무늬)이 났다.
   섬 둘레의 **판 네 장**으로 바꿨다 (겹치지 않고 삼각형 8장). three r128 에는 번들에 merge 유틸이 없어
   위치+인덱스만 잇는 `mergeGeo` 를 12줄로 붙였다.

**화면으로 세 장 확인**(해변·세계 끝·물속). 성능 그대로 — 프레임 1.96ms.

## v52 — 대량 편집이 화면을 얼리던 것 (2026-09-06 · 자문 6차 9위)

실측: 32,768칸 채우기 **411ms** · 되돌리기 288ms · 다시하기 281ms (프레임 예산 16.6ms).
`REGION_MAX` 40,000 이면 최악 500ms — 25프레임 정지다. v43·v47 로 큰 영역을 고르기 쉬워져
**오늘부터 더 자주** 겪는 문제였다.

원인은 `applyEdit` 이 **칸마다** `relightLocal`(양 채널 BFS)·`refreshTop`(64칸 기둥 스캔)·
`touch`(청크 7개 표시)를 부르는 것. 32,768번 반복하면 그게 전부다.

- 묶음 편집 중에는 셋을 **미룬다.** `endBatch` 에서 한 번에 맞춘다.
- 400칸이 넘으면 **세계 전체를 다시 켜는 게 더 싸다** (`relightAll` 은 25ms 고정).
  이하이면 예전처럼 칸마다.
- 되돌리기·다시하기도 같은 길(`applyCell(e, side, defer)` + `settleWorld()`).

| | 전 | 후 |
|---|---:|---:|
| 채우기 32,768칸 | 411ms | **40ms** |
| 되돌리기 | 288ms | **28ms** |
| 다시하기 | 281ms | **26ms** |

6,000칸이 넘으면 **미리 토스트**로 알린다 — 왜 잠깐 멈췄는지는 보여 준다.

> **처음엔 152ms 라고 적었다가 바로잡았다.** 벤치마크가 `requestPlay()` 를 건너뛰어
> 그 안에서 만들어지는 **AudioContext 생성 비용 115ms** 를 채우기 시간으로 세고 있었다.
> 실제 게임은 "플레이" 를 누르는 순간 그 비용을 치르므로 게임 중에는 없다. 아래 v57 참고.

**시험이 또 오염에 걸렸다**(3번째): "바뀐 칸 수" 를 단언했는데 앞 시험이 그 자리에 같은 블록을
남겨 두면 몇 개 적게 나온다. **끝난 상태**(전부 유리인가 · 되돌리면 사라지는가)를 재도록 바꿨다.

## v53 — 지도는 걸어서 채운다 (2026-09-06 · 자문 6차 10위)

부팅 3초 만에 96×96 섬의 지형·해안선·설원·사막이 **전부** 드러났다.
v34 로 나무 36~89그루·굴 입구 36~278개를 만들어 놨는데 **찾아갈 이유를 미니맵이 미리 없앴다.**

- `seenMap`(0/1, `WX*WZ`) — 그릴 때마다 플레이어 주변 **반경 14**(지하는 6)를 칠한다.
  안 가 본 칸은 흰 종이(`#0c1014`).
- 저장에 `mm` 필드 — 0/1 이라 RLE 로 몇 백 바이트. 선택 필드라 **포맷 v5 그대로**.
  예전 저장은 흰 종이에서 다시 시작한다. 새 세계는 `generate()` 가 지운다.
- **시작 지점(`V`) 표식**을 파란 점으로 그린다 — 집 자리를 찍어 놨는데 지도에 안 나오면 찍은 보람이 없다.
- 과제 **"지도장이"** — 섬의 8할을 밝힌다. 세 시간짜리 플레이어에게 섬을 한 바퀴 돌 이유가 생긴다.

화면으로 확인: 반 바퀴만 돌면 **반원만 열리고** 나머지는 검다.

## v54 — 붙여넣기 회전과 거울 (2026-09-06 · 자문 6차 8위)

`Ctrl+V` 가 조준한 면에 그대로 박히기만 해서, **대칭 건물의 반대쪽을 손으로 다시 지어야** 했다.

- **`Ctrl+R`** — Y축 90° 회전. `(x,z) → (d-1-z, x)`, 가로와 세로가 맞바뀐다.
- **`Ctrl+Shift+R`** — X 거울.
- **모양 방향도 함께 돈다.** 계단(2~5·7~10) · 벽(13~16) · 문(17~24) 은 `N→E→S→W` 로,
  원목 축은 `X↔Z` 로. 거울은 **동↔서만** 맞바꾼다(북·남은 그대로, 원목 축은 안 바뀐다).
  이게 없으면 돌린 계단이 엉뚱한 데를 보고 서 있다.
- `R` 단독은 새 세계, `Alt`+휠은 비행 속도라 둘 다 못 쓴다 — `Ctrl` 조합이 유일하게 빈 자리였다.

**시험이 훅 구멍을 드러냈다:** `B.SH_STAIR_N` 이 훅에 없어 `applyEdit(..., undefined)` → `sh || SH_FULL`
로 **계단이 애초에 안 놓였다.** 시험은 "계단 방향이 안 돌았다" 고 말했지만 진짜 원인은 시험대였다.
`undefined` 를 조용히 기본값으로 바꾸는 자리는 이렇게 숨는다.

## v55 — 붙여넣을 자리가 보인다 (2026-09-06 · 자문 6차 8위 마무리)

`Ctrl+V` 가 눈을 감고 박혀서 **붙여넣고 → 보고 → `Ctrl+Z` → 조금 옮겨서 → 다시** 를 반복해야 했다.
조준한 자리에 클립 크기의 **파란 상자**를 미리 그린다(초록은 복사한 원본 영역).
복사한 게 없거나 조준한 곳이 없으면 감춘다.

화면으로 확인. 미리보기는 **조준이 6칸 안에 닿을 때만** 뜬다 — 처음에 안 보여 당황했는데
사거리 밖을 겨누고 있었을 뿐이고, 그게 맞는 동작이다.

이로써 자문 6차 10건 중 **10건 전부** 처리했다.

## v56 — 청사진을 줄였다 (2026-09-06 · 자문 6차 덤)

`bp save` 가 `JSON.stringify(숫자 배열)` 이라 15,376칸이 **63KB** 였다.
세계 저장 3슬롯 + 백업과 `localStorage` 를 나눠 쓰는데 청사진 몇 개면 밀어낸다.

- 세계 저장과 **같은 `encodeArrB64`(RLE+Base64)** 로. 실측: 4,096칸이 3KB 미만.
- **액체 레벨(`levels`)도 함께** 담는다 — v42 에서 클립보드에 넣은 것이 청사진에는 빠져 있었다.
- `v: 2` 로 표시하고, **예전 청사진(숫자 배열)도 그대로 읽는다.** 저장해 둔 것이 사라지면 안 된다.

## v57 — 묶음 기록을 타입 배열로 · 그리고 측정의 함정 (2026-09-06)

백로그에 "대량 채우기 남은 152ms — 되돌리기 기록 객체 3만 개 할당" 을 적어 두고 손댔는데,
**그 진단이 틀렸다.** 쪼개 재 보니:

| 구간 | 시간 |
|---|---:|
| `applyEdit` 루프 32,768회 | **3.1ms** |
| `endBatch`(= `settleWorld`) | 26ms |
| 그런데 측정된 전체 | 150ms |

차이 120ms 를 쫓아가 보니 `unlock()` → `tone()` → **`ac()` 가 AudioContext 를 처음 만드는 비용 115ms**
였다. 두 번째 `tone` 은 1ms, `crunch` 는 0.6ms.

**실제 게임에는 이 문제가 없다.** `requestPlay()`(플레이 버튼)가 이미 `ac()` 를 부르므로
비용은 클릭 순간에 치러진다. **벤치마크가 그 경로를 건너뛰어 나를 속인 것이다.**

- 그래서 실제 채우기는 411ms → **40ms**(10배)였고, 152ms 는 처음부터 유령이었다.
- 그래도 묶음 기록은 **타입 배열**로 바꿔 두었다(객체 3만 개 대신 `Uint16/Uint8` 병렬 배열).
  좌표·블록·모양·물레벨이 전부 8~16비트에 들어간다. 할당이 사라져 GC 부담이 준다.

**교훈:** 헤드리스 벤치마크는 **진짜 시작 경로를 타지 않는다.** 한 번만 드는 초기화 비용
(오디오·셰이더 컴파일·첫 텍스처 업로드)이 첫 측정에 통째로 얹힌다. **같은 것을 두 번 재서 비교한다.**

**시험 오염 네 번째(v57):** 또 "바뀐 칸 수" 절대값을 박아 10/10 실패했다 —
v52 가 **같은 좌표**에 유리를 남겨 두기 때문. 두 가지로 막았다:
① 절대값 대신 하한 비교, ② **시험대 좌표를 서로 다르게** 잡는다.
오늘만 네 번 걸렸다. `tests/run.mjs` 는 한 페이지를 공유하므로,
**새 시험은 좌표부터 남과 겹치지 않게 고른다.**

## v58 — 만들어 놓고 화면까지 못 간 것들 (2026-09-06 · 자문 7차 1~5위)

자문 7차의 총평이 정확했다: **"결함은 거의 안 남았고, 남은 건 만들어 놓고 화면까지 못 간 것"**.

| # | 증상 (실측) | 고침 |
|---|---|---|
| 1 | **`B` 표식이 지도에 한 픽셀도 안 그려졌다.** 소리도 나고 토스트도 뜨고 "탐험가" 과제도 뜨는데 화면엔 없다 | `marks` 는 `[x, z]` 두 원소인데 `hud.js` 가 **`mk[2]`** 를 읽어 `NaN` → 캔버스 `arc()` 가 **조용히** 아무것도 안 그렸다. `mk[1]` 로. **한 글자** |
| 2 | **튜토리얼 일곱 줄이 낮에 안 보였다** — 모래 위 회색 글씨, 배경판 없음 | `#toast` 와 같은 판을 준다. 고대비 목록(`.hc`)에서도 `#hint` 만 빠져 있었다 |
| 3 | **유리 지붕을 얹으면 "내 집" 이 영영 안 떴다** — 채광창을 낸 사람만 못 딴다 | 빛(`lightSky === 15`)이 아니라 **막혔는가**(`topMap <= cy`)를 본다. 유리는 빛을 통과시킨다 |
| 4 | **첫 세계가 07:12 에 시작했다** — v31 이 `newWorld()` 만 고쳐서 **처음 온 사람에게만** 안 적용 · 시작 화면에서도 시계가 돌아 현실 1분에 게임 1.2시간이 갔다 | `state.js` 기본값 0.25 · 시계 전진을 `S.started` 안으로 |
| 5 | **`CLAUDE.md`·`INDEX.md` 숫자 여섯 곳이 옛날 것** — 특히 "저장 포맷 **현재 v4**" 는 다음 세션에게 **잘못된 지시**다 | `stamp.mjs` 가 코드에서 읽어 찍는다 (모듈 수·줄 수·저장 버전·월드 크기·시험 수·현재 상태 줄) |

**1번이 오늘 가장 값싼 큰 수확이다.** 한 글자인데, v27 에서 표식을 넣은 뒤로 줄곧 안 보이고 있었다.
캔버스 `arc()` 에 `NaN` 을 주면 **예외도 안 나고 아무것도 안 그린다** — 그래서 시험도 사람도 못 봤다.
v58 의 시험은 **금색 픽셀을 직접 세어** 그 부류를 잡는다.

5번은 v44(`codemap.mjs` 의 손으로 적은 `ORDER`)와 같은 병이다 — **손으로 적은 숫자는 반드시 어긋난다.**

## v59·v60 — 되돌리기의 구멍과 배려 설정 (2026-09-06 · 자문 7차 6~9위)

| # | 증상 (실측) | 고침 |
|---|---|---|
| 6 | **`Ctrl+Z` 로 물건을 잃는다.** 횃불을 얹은 받침돌을 캐면 횃불도 사라지는데 **기록은 1개** — 되돌리면 돌만 돌아오고 횃불은 없다. 문 밑바닥을 캐면 되돌린 뒤 **반쪽 문이 허공에** 뜬다 | 두 겹으로 고쳤다 |
| 7 | **`H` 도움말에 동물 얘기가 한 줄도 없었다** — 꽃을 주는 것도, 새끼가 나는 것도 게임 안에서 알 길이 없다 | "동물" 절 세 줄 (먹이·번식 조건 3칸/20초·우리) |
| 7b | **첫 과제가 가만히 서 있는데 열렸다.** 사막·설원에 스폰하면 1프레임 만에 "사막" 이 뜬다 | `S.walked >= 8` 을 걸었다. 발밑을 보는 과제(사막·설원·얼음)만 |
| 8 | 멀미·손목 배려가 **OS 설정(`prefers-reduced-motion`)에만** 있었다 | 게임 안 설정 둘 — **화면 흔들림 줄이기**(머리 흔들림·달리기 시야각·구름), **웅크리기 전환식** |
| 9 | **미니맵 안개를 지상과 지하가 한 배열로 공유**했다 — 굴을 파고 지나가면 **걸어 본 적 없는 산**이 지도에 뜬다 | `seenMap` 한 칸을 두 비트로 (`SEEN_TOP=1` · `SEEN_UNDER=2`). "지도장이" 과제는 지상 비트만 센다 |

### 6번 — 두 겹이었다
첫 겹은 `CLAUDE.md §6` 의 그 규칙 그대로였다: `dropCross()` 와 `meltIceAround()` 가
**`applyEdit` 을 거치지 않고 `world[]` 를 직접 썼다.** 기록 경로로 돌리고 재귀 깊이 상한(8)을 뒀다.

그런데도 시험이 그대로 실패했다. 둘째 겹이 진짜였다 —
딸려 사라진 횃불이 **별개의 history 항목**으로 쌓였고, `undo()` 는 하나만 꺼낸다.
**사람의 편집 하나 = 되돌리기 하나** 여야 한다. `applyEdit` 이 `record && !S.batch && !depth` 일 때
**암묵 묶음**을 열고 닫게 했다(`beginBatch(8)`). 딸린 것은 같은 묶음에 담긴다.
문의 두 칸도 같은 이유로 `mine.js` 에서 한 묶음으로 감쌌다.

**교훈:** "기록이 남는가" 와 "한 번에 되돌아오는가" 는 **다른 문제**다.
전자만 고치고 시험이 통과하길 기다린 30분을 후자에서 되찾았다.

### 9번 — 한 프레임 늦은 판단
안개 비트를 나누다 보니 `drawMinimap()` 이 `markSeen()` 을 부른 **뒤에** `S.mmUnder` 를 정하고 있었다.
지금까지는 반경(6 대 14)만 한 프레임 늦어 눈에 안 띄었는데, 비트까지 그걸로 고르면
지하에 들어선 첫 프레임이 **지상 지도를 밝힌다**. 판단을 앞으로 옮겼다.

### 곁가지 — 시험이 게임 규칙보다 엄격했다
전체 10회에서 `v15 동물: 물과 얼음 위로는 걸어가지 않는다` 가 **1/10** 로 깨졌다.
단독으로 10회 돌리면 10/10 통과라 오염인 줄 알고 두 번 헛짚었다.

진짜 원인은 **시험이 낡은 규칙을 지키고 있었던 것**이다.
v42 에서 "**발목 물은 좌초가 아니다**" 로 정했는데(마크의 소도 얕은 물에 서 있고,
우리 안 물구유 하나로 동물이 튀어 나가면 목장을 지을 수가 없다),
v15 시험은 여전히 "기둥 겉면이 물이면 실패" 였다. 그래서 해변에 선 동물 한 마리가
가끔 걸렸다. 시험을 `strandedAt` 과 **같은 잣대**(몸이 잠겼는가)로 맞췄다.

**교훈:** 규칙을 완화할 때 그 규칙을 지키던 **시험도 같이 옮겨야 한다.**
안 옮기면 몇 달 뒤 "무작위로 깨지는 시험" 이 되어, 오염부터 의심하며 시간을 버린다.
겸사겸사 `seedMobs()` 가 **비우지 않고 모자란 만큼만 채운다**는 것도 시험에 못 박았다.

### 곁가지 — `docs/TESTING.md` 의 항목 표도 자동 생성으로
표에 **175개**라고 적혀 있었다(실제 250개). v58 에서 배운 그대로 —
**손으로 적은 숫자는 반드시 어긋난다.** `tools/stamp.mjs` 가
`<!-- stamp:tests -->` 구간을 `tests/run.mjs` 의 이름 앞머리로 묶어 세어 다시 쓴다.

## v61 — 나무 그리기를 `src/tree.js` 로 (2026-09-06 · 묘목의 앞자락)

묘목(자문 7차 10위)은 새 블록 하나에 체크리스트 8곳 + 아틀라스 타일 + 자람 큐라
한 번에 넣기엔 컸다. **되돌리기 쉬운 앞자락만** 먼저 떼어 냈다 —
세계 생성 안에 박혀 있던 나무 그리기를 `growTree()` 로 빼고, 생성이 그걸 쓰게 했다.

`tree.js` 는 **아무것도 import 하지 않는다.** 블록 번호도, 놓는 방법(`put`)도 부르는 쪽이 넘긴다.
그래야 생성은 `set` 으로, 묘목은 `applyEdit` 으로 **같은 함수**를 쓸 수 있다.

**순수 리팩터임을 어떻게 확인했나** — "같은 시드 = 같은 지형" 시험은 *내부 일관성*만 본다.
옛 코드와 새 코드가 같은 세계를 만드는지는 못 본다. 그래서 임시 시험으로
`world[]` 전체의 FNV 해시를 세 시드(777·12345·42)로 찍고, `git checkout src/world.js` 로
옛 코드에 돌아가 다시 찍어 비교했다. **세 값이 모두 동일**했다.

`rng()` 부르는 차례가 곧 시드다 — `trunk` 를 뽑고 `continue` 하던 자리를
`return false` 로 바꿀 때 그 차례가 흐트러지지 않는지가 유일한 관건이었다.

**교훈:** 리팩터가 "출력이 같다" 를 주장하려면 **옛 출력과 직접 비교**한다.
새 코드끼리의 일관성 시험은 그 주장을 못 한다.

### v61 곁가지 — 새로 넣은 방어가 두 번째 세계에서 뚫렸다
v60 에서 "스폰 자리에 서 있기만 해도 열리는 과제" 를 `S.walked >= 8` 로 막았는데,
**`S.walked` 를 새 세계에서 안 지웠다.** `newWorld()` 는 `S.earned = {}` 로 과제를 다시 잠그므로,
두 번째 세계가 사막이면 **스폰하자마자 "사막" 이 뜬다** — 방금 막은 그 장면 그대로다.
`newWorld()` 와 `afterWorldSwap()` 두 경로 모두에서 지운다.
세계 전환 회귀 시험(v55)에 `walked` 단언을 얹었다.

**교훈:** **세션 누적값을 새로 넣으면 세계 전환 두 경로를 같이 본다.**
`S.history` · `S.clip` · `S.selA/B` 가 이미 그 목록에 있는데도 새 값만 빠뜨렸다.
`afterWorldSwap` 이 단일 경로가 된 뒤로 놓치기는 쉬워졌지만, **놓치면 두 번째 세계에서만** 드러난다.

### v61 곁가지 2 — `beginPlay()` 가 사람을 옮긴다
`v11 눈: 눈이 오면 지표에 쌓인다` 가 10회 중 1회 깨졌다.
시험은 초원 한복판 `gx,gz` 를 찾아 **거기에 사람을 놓고 나서** `beginPlay()` 를 불렀는데,
`beginPlay()` 는 첫 호출에서 `S.savedPos` 로 사람을 **옮긴다**.
그러면 눈은 옮겨 간 자리에 쌓이고, 시험은 원래 자리 ±16 만 세니 `after === before` 가 된다.
자리 잡기를 `beginPlay()` **뒤로** 옮겼다.

같이 드러난 것 — 시험대가 원래 부실했다. "첫 번째 풀칸" 을 집다 보니
눈이 쌓일 수 있는 칸이 **27개**(1,089칸 중)뿐인 숲 가장자리가 걸렸다.
이제 **쌓일 칸이 가장 많은 자리**를 골라 잡고(99999 시드 최대 86칸),
그 개수를 시험대 단언으로 못 박는다.

**교훈:** 준비 함수를 부르는 **차례**도 시험대의 일부다.
`beginPlay` · `spawn` · `generate` 처럼 **사람 위치를 건드리는 함수 뒤에** 자리를 잡는다.
그리고 "골랐다" 로 끝내지 말고 **고른 자리가 쓸 만한가를 단언으로 못 박는다**(v56 교훈의 재확인).

## v62 — 묘목 (2026-09-06 · 자문 7차 10위, 마지막 항목)

나무를 베면 끝이었다. 심을 방법이 없으니 **숲은 줄기만 하고 늘지 않는다.**
크리에이티브에서도 "여기 숲을 만들자" 가 안 되는 게 이상했다.

- 새 블록 `SAPLING`(56) · 아틀라스 타일 58 · 교차 쿼드 · 바닥 필요
- `queues.js` 에 `growQ`/`growTimer` · `fluids.js` 에 `growTick(dt)`
- 1초에 한 번 큐를 돌며 8% 확률 → 평균 12초. 심고 돌아서면 자라 있다
- 종류는 **바이옴이 정한다**(설원→가문비 · 그 외 참나무/자작 34%).
  묘목을 세 종류로 나누면 목록만 세 칸 늘고 얻는 게 없다 (자문 7차 의견)
- 도전 과제 "숲지기" 추가

**자라는 조건 — 마크와 같게 셋**
1. 발밑이 흙·풀·모래·눈 (돌·유리 위에서는 남아 있되 안 자란다)
2. 빛 9 이상
3. **위로 6칸이 비어 있다**

3번은 시험을 쓰다 발견했다. 어두운 방 시험을 짜다 보니
`growTree` 는 줄기를 **무조건 덮어쓴다** — 사방 막힌 방에 심으면 **천장을 뚫고 올라온다.**
크리에이티브에서 세계가 내 건축물을 말없이 부수는 것만큼 신뢰를 깨는 게 없다(v11 눈과 같은 원칙).

**되돌리기** — 한 그루가 `Ctrl+Z` 한 번이다. `growTick` 이 `beginBatch(64)` 로 감싼다.
v59 에서 만든 암묵 묶음이 바로 여기서 쓰였다.

**큐는 저장하지 않는다.** 대신 `S.growDirty` 를 세우고(`newWorld`·`afterWorldSwap`)
`growTick` 이 한 번 세계를 훑어 묘목을 다시 담는다(589,824칸에 1ms 남짓).
저장 포맷은 그대로 v5 — `SAPLING` 은 `world[]` 의 블록 번호일 뿐이다.

## v63 — 청사진 목록 UI (2026-09-06 · 자문 5차가 남긴 마지막 항목)

용량은 v56 에서 RLE+Base64 로 이미 줄였는데(63KB → 몇 KB), **목록이 없었다.**
저장해 놓고 `/bp list` 를 쳐야만 보이면 저장한 보람이 없다.

ESC 메뉴에 **"청사진"** 절을 넣었다 — 이름 · 크기(`3×2×4 · 24칸`) · [불러오기] [지우기].
`/bp del <이름>` 도 같이 넣어 명령창과 짝을 맞췄다.

**이름은 `textContent` 로 넣는다.** 사람이 지은 이름을 `innerHTML` 로 끼우면
`<b>굵게</b>` 같은 이름이 마크업이 된다. 시험이 그 이름으로 저장해 보고
`.bp b b` 개수가 0인지 센다 — 나중에 누가 `innerHTML` 로 바꾸면 걸린다.

지우기는 `window.confirm` 을 건다 (`CLAUDE.md` 2절 8번 — 되돌릴 수 없는 것은 단추 하나로 사라지지 않는다).

### v63 곁가지 — 자란 순간이 안 보이고 안 들렸다
묘목을 넣고 옆에 서서 지켜보니 **나무가 소리도 없이 그냥 솟았다.**
v58 에서 배운 "만들어 놓고 화면까지 못 간 것" 과 같은 부류라 바로 이었다 —
잎조각 12개(`burst`)와 낮은 두 음(`tone`)을 자란 자리에서 낸다.

같이 고친 것: `place()` 의 `"물속에서는 꺼집니다"` 는 **횃불 문구**였다.
꽃·묘목을 물에 놓으려 해도 같은 말이 떠서 무슨 소린지 알 수가 없다.
`TORCH`·`FIRE` 가 아니면 `"물속에는 놓을 수 없습니다"` 로 바꿨다.
블록을 늘리면 **예전에 한 종류만 보고 쓴 문구**가 이렇게 뒤에서 어긋난다.

### v63 곁가지 2 — 나무가 사람을 감싸고, 줄기가 남의 블록을 뚫는다
묘목을 넣고 나서 두 자리를 더 막았다.

**① 줄기 길이를 잘못 셌다.** 천장 확인을 6칸으로 뒀는데 **줄기는 최대 7칸**이다
(가문비·자작이 `5 + floor(rnd*3)`). 7번째 칸이 남의 블록을 덮어쓸 수 있었다.
잎은 빈 칸에만 놓이므로 **줄기 길이만 보면 된다** — 7칸으로 고쳤다.

**② 사람이 선 자리에서도 자랐다.** 묘목 옆에 서서 자라기를 지켜보는 것이
이 기능을 쓰는 가장 흔한 장면인데, 그러다 **줄기 속에 갇힌다.**
`freezeTick` 이 헤엄치는 사람을 얼음에 안 가두는 것과 같은 이유로 `playerOccupies` 를 본다.

**교훈:** 세계가 스스로 블록을 놓는 기능(`growTick`·`freezeTick`·`grassTick`·눈 쌓기)은
**세 가지를 늘 같이 본다** — 사람이 그 자리에 있나 · 사람이 손댄 칸인가 · 남의 블록을 덮어쓰나.
셋 중 하나만 빠져도 "세계가 내 것을 말없이 부순다" 가 된다.

### v63 곁가지 3 — 블록 목록까지 닿는지 시험으로 못 박았다
묘목을 넣고 나서 "정말 목록에 뜨나" 를 눈으로 확인했다. 그건 다음 세션에 안 남는다.

`ALL_BLOCKS + ITEMS` 의 모든 항목이 **블록 목록(E)에 단추로 있는지**,
**갈래(자연/건축/색/빛·물)가 비어 있지 않은지**, **이름과 영문명으로 찾아지는지**를 센다.
블록을 새로 넣고 `categoryOf` 나 목록 등록을 빠뜨리면 이 시험이 바로 잡는다 —
v58 의 "만들어 놓고 화면까지 못 간 것" 을 블록 쪽에서 막는 그물이다.

세다가 알게 된 것: 목록 단추는 `ALL_BLOCKS.length` 가 아니라 **`+ ITEMS.length`** 다.
부싯돌은 놓는 블록이 아니지만 손에는 쥔다.

곁가지로 확인한 것 — **키운 나무를 베면 잎이 제대로 진다**(52장 → 0장).
심기 → 자라기 → 베기 → 잎 지기 → 다시 심기 고리가 끊긴 데 없이 돈다.

### v63 곁가지 4 — 묘목이 10배 느리게 자라고 있었다
`growTick(dt)` 를 `if (S.waterTimer > 0.15)` 블록 **안에** 넣어 뒀다.
그 블록은 0.15초에 한 번만 열리는데 넘겨준 건 **프레임 dt(0.016)** 라,
`Q.growTimer` 가 1초에 **0.10초**만 갔다. 평균 12초로 잡은 것이 **110초**가 되어 있었다.

시험이 못 잡은 이유가 정확히 그 모양이다 — 시험이 `growTick(1.0)` 을 **직접** 불렀다.
함수 안쪽은 다 맞았고, 틀린 것은 **누가 얼마나 자주 부르는가**였다.

프레임마다 부르게 옮기고(큐가 비면 즉시 반환하니 공짜다),
**진짜 프레임(`B.step(1/60)`)으로 1초를 돌려 `Q.growTimer` 가 1초 갔는지**를 단언에 넣었다.
고침을 되돌려 그 단언이 실제로 깨지는 것(0.10초)까지 확인했다.

**교훈:** 시간이 걸리는 기능은 **함수를 직접 부르는 시험만으로는 부족하다.**
`step()` 으로 진짜 프레임을 돌려 **호출 빈도까지** 재는 단언을 하나 둔다.
(같은 부류: `snowTimer` · `grassTimer` · `lavaTimer` — 전부 다른 주기에 얹혀 있다)

**같은 부류를 전부 훑었다** — 임시 탐침으로 진짜 프레임 1초를 돌려 타이머마다 얼마나 갔는지 쟀다.

| 타이머 | 1초에 | 판정 |
|---|---|---|
| `playSeconds` | 1.00 | 기준 |
| `snowTimer` | 0.45 주기로 내려감 | 정상 (프레임마다 `-= dt`) |
| `grassTimer` | 0.9 | 정상 (0.15초 블록 안에서 `+ 0.15` — 스스로 앞뒤가 맞는다) |
| `waterTimer` | 0.15 에서 감김 | 정상 |
| `lavaTimer` | 6번 증가(4에서 감겨 2) | 정상 |
| `decayTimer` | 0 | 정상 — 부패 큐가 비어 있으면 아예 안 센다 |
| `growTimer` | **0.10** | **틀렸다** → 고침 |

**잘못 얹힌 것은 `growTick` 하나뿐이었다.** 나머지는 각자 자기 블록의 주기와 앞뒤가 맞았다.
`grassTimer` 처럼 "0.15초 블록 안에서 0.15를 더하는" 모양은 헷갈리지만 스스로 일관된다 —
헷갈리는 것과 틀린 것은 다르다.

## v64 — 묘목 큐의 프레임 비용 (2026-09-07)

`growTick` 을 프레임마다 부르게 바꿨으니(v63) 정말 공짜인지 쟀다.
처음 측정은 **0.028ms → 1.084ms(38배)** 였다. 숲을 심으면 끊긴다는 뜻이라 놀랐는데,
**v57 에서 겪은 그 함정이었다** — 방금 블록 120개를 놓아 놓고 바로 쟀다.
그 1.056ms 는 전부 **청크 재굽기**였다.

400프레임을 돌려 가라앉힌 뒤, **같은 세계에서 큐만 비워** 다시 쟀다:

| | 프레임당 |
|---|---|
| 묘목 60그루가 큐에 | 0.059ms |
| 같은 세계, 큐만 비움 | 0.017ms |
| **차이** | **0.042ms** (예산 16.6ms 의 0.25%) |

공짜가 맞았다. 이 측정 방식을 시험으로 굳혔다 —
**절대 ms 를 박지 않고**(기계마다 다르다) 같은 세계에서 큐만 비워 잰 값과 견준다.

**교훈(두 번째로 같은 것에 걸렸다):** 벤치마크 직전에 세계를 건드렸으면
그 뒷정리(청크 재굽기·조명)가 **첫 측정에 통째로 얹힌다.**
재기 전에 **가라앉히고**, 되도록 **같은 조건에서 한 변수만 바꿔** 견준다.

**또 오염에 걸렸다(다섯 번째).** 이 성능 시험이 단독 3/3 통과, 전체 **10/10 실패**였다 —
앞선 v62 묘목 시험이 **안 자란 묘목 3그루**(돌 위·어두운 방·낮은 천장)를 큐에 남긴다.
`B.Q.growQ.length = 0` 을 첫 줄에 넣어 세는 양을 못 박았다.
**단독 통과 + 전체 실패 = 오염**이라는 서명은 이제 다섯 번 봤다. 바로 큐·개수·전역값부터 본다.

## v65 — 자문 8차 (2026-09-07)

자문이 10건을 냈고, 되돌리기 어려운 3건(양동이·칸별 모양·계단 모서리) 중
**계단 모서리만** 유저가 골랐다. 나머지 7건 + 덤 2건을 먼저 넣었다.

| # | 증상 (자문 실측) | 고침 |
|---|---|---|
| 1 | **크리에이티브인데 돌 하나에 1.25초.** 잘못 놓은 벽 한 줄(20칸)을 헐려면 25초를 붙잡는다. 굳기 표가 **서바이벌 수치**다 | 설정 "캐기 속도 — 보통/빠름(4배)/즉시". 균열 4단계·스윙·"턱-턱" 은 잘 만든 부분이라 살렸다. "즉시" 도 0 이 아니라 **0.08초** — 0 이면 누르는 동안 초당 60칸이 사라진다 |
| 3 | **영역 첫 모서리를 찍어도 화면에 아무것도 안 남는다.** 토스트는 1.6초면 사라져 스무 칸 날아가 감으로 B 를 찍는다 | A 만 있을 때 **노란 한 칸** 상자. 둘 다 찍히면 예전처럼 초록 |
| 4 | **되돌리기가 "되돌리기" 한 마디다.** 32,768칸 채우기도 문 하나도 같아서 세 번 이상 못 누른다 | `endBatch` 가 **이미 저장해 두던 라벨**을 꺼내 쓴다 — "되돌리기 — 채우기 32,768칸" |
| 5 | **목록이 한 칸 채울 때마다 닫힌다.** 핫바 열 칸을 갈아 끼우려면 스무 번을 누른다. 열린 동안 숫자키도 죽어 있다 | 클릭해도 안 닫는다 · 열린 채로 숫자키가 대상 칸을 옮긴다. 닫기는 `E`·`ESC`·바깥 클릭 |
| 6 | **날씨를 고정할 수 없다.** 노을 사진을 찍으려 맑음을 골라도 2분 뒤 비가 온다 | `K` 로 고른 날씨는 **잠긴다**. 시간에는 이미 "하루 길이 0" 짝이 있었는데 날씨에만 없었다 |
| 7 | **날면서 빨리 갈 수가 없다.** Alt+휠로 배율을 올렸다 내렸다 해야 한다 | 비행에도 달리기 배율 2배. `S.sprintTap`·Ctrl 판정이 **이미 다 계산돼 있었다** |
| 8 | **조준한 칸의 밝기를 볼 수가 없다.** `F3` 의 "빛 12/15" 는 서 있는 자리 값이라 저 구석이 어두운지 알 수 없다 | `F3` 에 "· 조준 4" 를 더한다 |
| 덤 | 미니맵 표식 12개가 전부 똑같은 금색 점 | 번호를 옆에 적는다 |
| 덤 | `docs/GAMEPLAY.md` 의 "물 — 캘 수 없음" 이 사실이 아니다 | 진짜 이유(레이캐스트가 액체를 건너뛴다)로 고쳤다 |

### 두 번 데인 자리
**`hit` 은 `step()` 의 지역 변수인데 HUD 갱신은 `animate()` 에 있다.** 조준 밝기를 넣으며
`hit` 을 그대로 썼다가 프레임마다 `hit is not defined` 가 터졌다. `S.aimFace` 로 넘긴다.
그 전에는 `inside`·`lightBlk` 를 import 하지 않아 같은 자리에서 터졌다.
**둘 다 "페이지 오류 없음" 시험이 잡았다** — 단위 시험은 전부 통과하고 있었다.

**교훈:** 이 파일에서 `step()` 과 `animate()` 는 **다른 함수**다.
한쪽에서 계산한 것을 다른 쪽에서 쓰려면 `S` 를 거친다 (`CLAUDE.md` 2.5절 그대로).

## v66 — 계단 모서리 (2026-09-07 · 자문 8차 10위 · 유저 승인)

꺾이는 자리마다 **네모 반 칸이 툭 튀어나와** 나선계단 층계참과 박공지붕 모서리가 뭉툭했다.
"왜 내 지붕은 마크처럼 안 예쁘지" 의 정체였다.

**규칙 셋** (윗층이 덮는 넓이로 세면 명확하다)

| 상황 | 넓이 |
|---|---|
| 외톨이 · 같은 축으로 이어짐 | 1/2 (직선) |
| 높은 쪽 **뒤**에 직각 계단 | 3/4 (안쪽 — 오목한 자리를 메운다) |
| 낮은 쪽 **앞**에 직각 계단 | 1/4 (바깥) |

바깥 모서리에서 **어느 한 칸을 남기느냐**가 이 작업의 핵심이었다.
남기는 칸은 **이웃 계단의 높은 쪽과 맞닿는 칸**이다. 반대로 잡으면 두 계단의 높은 부분이
서로 안 닿아 그 자리에 노치가 남는다 — 고치려던 바로 그 증상이 그대로 남는다.
시험이 그 한 칸의 위치(`ob[0] >= 0.49`)를 직접 못 박는다.

**저장 포맷을 안 건드렸다.** 모서리를 `shape` 에 넣지 않고 `boxesAt()` 이 이웃을 보고 계산한다.
그래서 v5 그대로고, 옆 계단을 캐면 모양이 **저절로 되돌아온다**
(울타리·유리판이 `connectsTo` 로 쓰던 틀을 계단으로 넓힌 셈이다).
`boxesAt()` 이 단일 진입점이라 **메싱·충돌·조준이 한 번에 맞았다** — 이 설계의 값을 오늘 받았다.

한 곳만 따로 손댔다: **조준 상자**(`HL_GEO`)는 모양마다 미리 만들어 둔 것이라
모서리 계단에는 직선 외곽선이 떴다. 조준할 때 실제 상자로 만들고 캐시한다(`dynamicHighlight`).
균열 오버레이(`SHAPE_BOUNDS`)는 확인해 보니 **모든 계단이 통짜 큐브**다
(밑에 깔린 반 칸이 x·z 를 꽉 채우므로 min/max 가 0~1). v66 전후가 같아 손댈 것이 없었다.

### v66 마무리 — 그려지는지와 미리보이는지를 따로 못 박았다
상자 계산이 맞아도 **메시가 그 상자를 안 쓰면 화면은 그대로다**(v21 에서 겪었다).
그래서 `boxesAt` 이 아니라 **구워진 삼각형**의 넓이를 직접 센다 —
이 칸에서 위를 보는 면의 합이 직선 1.5 · 안쪽 1.75 · 바깥 1.25 다
(밟는 바닥 윗면 1.0 은 윗층에 가려도 그대로 구워진다 — 계단 전부가 예전부터 그랬다).

**v66 이 새로 만든 어긋남 하나를 그 자리에서 잡았다** — 놓을 자리 고스트가 늘 직선이었다.
옆에 직각 계단이 있으면 놓는 순간 모서리가 되는데 미리보기는 직선이라,
"놓아 봐야 아는" 물건이 되어 고스트를 넣은 뜻이 없어진다.
모서리는 **그 칸이 아니라 옆 칸이 정하므로 놓기 전에도 읽을 수 있다** —
`updateGhost` 가 `boxesAt` 을 그대로 부른다.

시험을 쓰다 또 걸린 것: 계단 방향은 **시선(`player.yaw`)이 정한다.**
yaw 를 안 세우고 시험을 쓰면 어느 쪽이 높은 쪽인지 모른 채 안/바깥을 뒤바꿔 기대하게 된다.
`currentShape(false) === SH_STAIR_N` 을 **시험대 단언으로** 먼저 못 박았다.

## v67 — 자문 9차 · 폰이 1급이 아니었다 (2026-09-07)

자문이 **실측**을 붙여 왔다. 헤드리스로 6가지 해상도와 힙 메모리를 직접 쟀다.
10건 전부 소~중이라 승인이 필요한 것은 없었다.

### 가장 큰 것 — 회귀 시험 265항목이 전부 데스크톱 폭으로만 돌고 있었다
그래서 **폰에서만 나는 것은 아무도 못 잡았다.** 하네스에 폰 컨텍스트(844×390 · `isMobile`)를
넣고, 회차마다 페이지를 한 번 더 열어 `phoneTest` 만 돌린다. 이번에 그 그물로 셋을 잡았다.

| # | 증상 (자문 실측) | 고침 |
|---|---|---|
| 1 | **폰에서 블록 54칸 중 33칸이 화면 밖**이고 스크롤도 안 된다. 1024×640 에서도 5칸(부싯돌 포함)을 못 누른다. 블록을 넣을수록 나빠진다 | `#picker` 를 `place-items: start center` + `overflow:auto` · 카드에 `max-height` · 목록에 `overflow-y:auto` · `touch-action: pan-y`(body 가 `touch-action:none` 이라 없으면 손가락으로 못 민다) |
| 2 | **폰에서 [플레이]를 누르면 그 세션은 끝까지 갇힌다.** 설정·슬롯·청사진·새 세계가 전부 `ESC` 메뉴 안인데 폰엔 `ESC` 가 없다. 되돌리기도 없다 | 터치 단추 **메뉴·되돌리기** 두 개 |
| 3 | **`H` 도움말이 게임을 안 멈춘다** — 판 뒤에서 시점이 돌고, 왼쪽을 누르면 안 보이는 블록이 캐지고, 휠은 핫바를 넘긴다. 튜토리얼 마지막 줄이 "H 를 누르면…" 인데 | `openPicker` 와 같이 `exitPointerLock` + `S.uiOpen`. 그러자 조기 반환에 걸려 **다시 못 닫는** 문제가 생겨 `helpOpen()` 을 만들고 `H`·`ESC`·바깥 클릭 세 길을 다 뚫었다 |
| 6 | **`"32,768칸 채우는 중…"` 토스트가 한 번도 뜬 적이 없다** — 같은 태스크라 그리기가 안 일어나고 둘째 토스트에 덮인다 | `requestAnimationFrame` 두 겹 뒤에 판다 (6000칸 넘을 때만) |
| 8 | **`Q` 가 같은 블록이면 모양을 안 가져온다** — 손에 온전한 돌을 들고 돌계단을 복사하는, 계단을 잇는 가장 흔한 순간에만 안 된다 | 조기 반환을 "블록도 같고 **모양 갈래도 같을 때**" 로 좁혔다 |

### CSS 에서 두 번 헛짚었다
`max-height` 를 걸었는데 카드가 계속 394px 이었다(화면 390).
① **`dvh` 를 모르는 브라우저는 그 줄을 통째로 버린다** — `vh` 를 먼저 두고 `dvh` 로 덮는다.
② 그래도 394 였다. 이 문서는 **전역 `border-box` 를 안 쓴다** — `max-height:342` 에
패딩 48 + 테두리 4 가 더 붙어 394 가 됐다. `box-sizing: border-box` 한 줄.
**둘 다 "시험대가 안 섰다" 단언이 숫자를 뱉어 줘서 찾았다.**

## v67 이어서 — 자문 9차 나머지 (2026-09-07)

| # | 증상 | 고침 |
|---|---|---|
| 4 | **터치 튜토리얼이 4번째 줄에서 영영 멈춘다.** 3·5·6 단계가 각각 `G` 키·`Ctrl+F`·`H` 키에만 걸려 있어 폰에서는 도달할 방법이 없었다. 뒤 세 줄은 아무도 못 봤다. 끝난 뒤 상시 안내도 **여섯 조작 전부가 폰에 없는 것**이었다 | `advanceTutTouch()` 로 문구가 요구하는 동작에 이어 준다 — 줄 긋기(`place(repeating)`) · 스틱 · 웅크림 단추. `HINT_TOUCH` 추가. **폰 시험이 일곱 줄을 터치만으로 끝까지 간다** |
| 7 | **날씨·잠금·모양이 저장에 없다.** 시각(`t`)·달 위상(`md`)·비행 속도(`fly`)는 실려 있는데 날씨만 빠졌다 | `wt`·`wk`·`sm` 세 선택 필드. **저장 버전은 v5 그대로** — 없으면 기본값이라 예전 저장이 그대로 읽힌다(시험이 그 경로도 확인한다) |
| 5 | 되돌리기가 **칸 수를 안 센다.** `opts.undo` 는 2000까지 가는데 큰 채우기 2000번이면 865MB다 | `HISTORY_CELLS_MAX`(200만 칸) 상한. `fillSelection` 이 `beginBatch(selectionSize())` 로 시작한다 |

### 날씨를 되살릴 때 화면이 안 따라왔다
`S.weather` 를 직접 넣으면 `setWeather` 는 **조기 반환**한다(`w === S.weather`).
빗줄기·눈송이 `visible` 을 켜는 게 그 안이라, 값만 이어지고 화면은 맑음이었다.
시각 처리를 `applyWeather()` 로 떼어 불러오기에서 부른다.
**"이어졌나" 와 "보이나" 는 다른 문제다** — 시험이 `weatherPoints.visible` 까지 본다.

### 자문의 진단을 측정이 일부 뒤집었다
자문은 "큰 채우기 40회에 +87MB, 원인은 되돌리기" 라고 했다. 재현은 됐다(26.9 → 89.7MB).
그런데 **히스토리를 매번 비우며** 같은 40회를 돌려도 **+52MB** 였다.
늘어난 것의 대부분은 되돌리기가 아니라 **메싱 쓰레기**(아직 수거 안 된 것)다.
히스토리 몫은 10~17MB 남짓이다.

그래도 고침은 그대로 값이 있다 — `opts.undo` 2000 × 39,304칸 × 11바이트 = **865MB** 가
막혀 있지 않았다. 상한은 매일 걸리는 것이 아니라 **천장**이다.
**남의 실측도 "무엇이 원인인가" 는 따로 확인한다** — 같은 조건에서 한 변수만 바꿔 본다.

## v67 마무리 — 슬롯 이름과 표식 (자문 9차 #10·#9)

| # | 증상 | 고침 |
|---|---|---|
| 10 | 슬롯이 `SEED 20767 · 45분` 이라 **성을 지은 게 1번인지 2번인지 시드 번호로 외워야** 한다 | 저장에 선택 필드 `nm` · 슬롯 옆 **✎** 로 이름 붙이기. `renameSlot` 은 그 슬롯 JSON 의 `nm` 만 갈아 끼우므로 지금 놀고 있는 슬롯이 아니어도 안전하다 |
| 9 | **표식을 찍어도 돌아갈 수가 없다** — 높이가 없고(갱도 입구와 지상 탑이 같은 점), 좌표를 안 알려 주고(`/tp` 에 넣을 숫자를 모른다), 번호만 있어 사흘 뒤엔 3번이 뭐였는지 모른다 | `[x, y, z, 이름]` 으로 늘리고 `Shift`+`B` 로 이름을 붙인다. 찍을 때 토스트가 **좌표를 말한다**. `/tp 3` · `/tp 채석장` 으로 돌아간다 |

**저장 버전은 v5 그대로다.** `marks` 는 예전 `[x, z]` 와 지금 `[x, y, z, 이름]` 을
**길이로 구분**해 읽는다 — 읽는 곳이 셋(미니맵·`B` 키·`/tp`)이라
`markX/markY/markZ/markName` 네 함수로 모아 두었다. v58 에서 `mk[2]` 를 직접 읽어
NaN 이 되고 캔버스가 조용히 아무것도 안 그린 사고가 바로 이 자리다.
높이를 모르는 예전 표식으로 `/tp` 하면 **그 자리 지표로 올려 준다**(땅에 파묻히지 않게).

사람이 지은 문자열을 마크업에 끼우는 자리가 또 나왔다(슬롯 목록은 `innerHTML` 을 쓴다).
`esc()` 로 막고 시험이 `<b>굵게</b>` 라는 이름으로 확인한다 — v63 청사진에서 배운 그대로다.

### v67 사고 — `tidy-imports` 가 `input.js` 의 import 84개를 지워 게임이 안 켜졌다

**이번 세션에서 가장 값진 사고다.** 커밋하고 push 한 뒤에야 발견했다.

원인은 내가 슬롯 이름에 넣은 한 줄이었다:

```js
.replace(/"/g, "&quot;")        // 정규식 리터럴 안의 큰따옴표
```

`tools/tidy-imports.mjs` 의 `stripCode` 는 문자열을 지울 때 `"..."` 를 정규식으로 짝짓는다.
정규식 안의 그 `"` 하나가 **이후 모든 따옴표 짝을 한 칸씩 밀어**,
본문 49,058자 중 **40,939자(83%)** 가 사라졌다. 남은 것에는 아무 이름도 없으니
"전부 안 쓰인다" 로 읽혀 import 22줄이 7줄로 잘렸다.

**두 겹으로 고쳤다.**
① `esc()` 가 정규식 안에 큰따옴표를 글자로 두지 않는다 (`/[&<>"]/g` + 표).
② **도구가 조용히 코드를 지우지 못하게** 안전장치 둘 —
   본문의 절반 넘게가 제거기에 삼켜지면 그 파일을 건너뛰고,
   한 파일에서 10개 넘게 지우려 하면 `--force` 없이는 건너뛴다. 둘 다 종료 코드 1 로 알린다.
   **사고를 일부러 재현해 안전장치가 실제로 막는 것까지 확인했다.**

**교훈 셋**
- **코드를 고치는 도구는 "많이 고치려 할 때 멈춰야" 한다.** 정리 도구가 84개를 지우는 것은
  정리가 아니라 사고다. 임계값을 넘으면 사람에게 묻는다.
- **정규식으로 소스를 파싱하는 도구는 정규식 리터럴에서 무너진다.** 특히 `/"/`·`/'/`.
  그런 도구를 믿고 자동으로 돌린다면 안전장치가 필수다.
- **`node tests/run.mjs 1` 을 커밋 **전에** 한 번 더 돌린다.** `tidy → codemap → stamp → commit`
  을 한 줄로 이어 붙이는 바람에, 도구가 망가뜨린 것을 검증 없이 그대로 커밋했다.

## v68 — 유저가 "실행이 안 돼" (2026-09-07)

화면은 `세계를 만드는 중…` 에서 막대가 멈춘 채였다.

**먼저 추측하지 않고 배포본을 실제 브라우저로 열어 봤다** — 오류 0, 정상 부팅.
즉 배포된 코드는 멀쩡했고, 사람 브라우저에 **깨진 판이 남아 있던 것**이다
(앞 커밋에서 import 84개가 지워진 판이 잠깐 올라가 있었다).

### 왜 새로고침해도 안 나았나
서비스 워커는 "우리 파일은 네트워크 먼저" 로 잘 짜여 있었다. 그런데 그 `fetch(req)` 가
**브라우저 HTTP 캐시**에서 답을 받아 온다. GitHub Pages 는 `max-age` 를 주므로,
그 사이에 연 사람은 캐시가 만료될 때까지 **계속 깨진 파일**을 받는다.

- `fetch(new Request(req, { cache: "no-cache" }))` — HTTP 캐시까지 건너뛰고 서버에 물어본다
- 껍데기 미리담기도 `cache: "reload"` — 안 그러면 깨진 것을 그대로 다시 담는다
- `VERSION` 이 `blockyard-v1` 고정이라 **`activate` 의 옛 캐시 청소가 한 번도 안 돌았다.**
  `tools/stamp.mjs` 가 배포마다 다시 쓴다

### 진짜 문제는 "말을 안 하는 것" 이었다
모듈이 터지면 `main.js` 자체가 안 도니 `bootDone()` 도 안 불린다 —
**막대가 멈춘 검은 화면만 남고, 사람이 할 수 있는 게 없다.**
모듈 바깥(인라인)에 감시견을 뒀다: 오류를 잡거나 15초가 지나면
**무엇이 잘못됐는지 적고**(`파일을 못 읽었습니다: …/src/main.js`)
**[캐시 비우고 다시 열기]** 단추를 띄운다(서비스 워커 해제 + 캐시 삭제 + 재적재, 세계 저장은 그대로).

**한 번 헛짚었다** — `window.addEventListener("error", fn)` 으로는 안 잡혔다.
**모듈·이미지 같은 자원 로딩 실패는 버블링을 안 한다.** 세 번째 인자 `true`(캡처)가 있어야 한다.
`main.js` 를 일부러 깨뜨려 감시견이 뜨는 것까지 확인했다.

**교훈:** "안 돼요" 를 받으면 **내 코드부터 의심하기 전에 그 사람이 받는 것을 직접 받아 본다.**
그리고 오프라인 캐시를 두는 앱은 **깨진 판을 한 번 올리면 사람을 거기 가둔다** —
빠져나올 길을 화면에 두는 것이 캐시 전략보다 먼저다.

### v68 마무리 — 배포본을 보는 도구를 만들었다
오늘 사고의 핵심은 **시험 275항목이 전부 통과하는데 사람은 게임을 못 켰다**는 것이다.
시험은 저장소를 보고, 사람은 배포본을 본다. 그 사이를 아무도 안 보고 있었다.

`node tools/check-live.mjs [주소]` — 배포 주소를 진짜 브라우저로 열어 켜지는지 보고
0/1 로 끝난다. 부팅 감시견이 떠 있으면 **사람이 보는 그 문구를 그대로** 찍는다.
깨진 주소로 1, 정상 주소로 0 이 나오는 것까지 확인했다.
`CLAUDE.md` 작업 루프에 9단계로 넣었다.

**곁가지 사고:** 그 도구를 시험하려고 `main.js` 를 일부러 깨뜨린 스크립트가
타임아웃으로 백그라운드로 넘어가면서 **`finally` 의 복원이 안 돌았다.**
`main.js` 가 깨진 채 남아 있었다(커밋 전에 `git status` 로 발견).
**저장소 파일을 건드리는 임시 스크립트는 쓰지 않는다** — 꼭 써야 하면
`git stash`/사본으로 하고, 끝나고 `git status` 를 반드시 본다.

### v68 곁가지 — 부팅 대기 조건이 사실은 아무것도 안 기다리고 있었다
시험과 부팅 감시견이 둘 다 `window.__blockyard.booted !== false` 로 "다 열렸나" 를 봤다.
그런데 **훅에 `booted` 가 아예 없었다.** `undefined !== false` 는 늘 참이라,
**훅 객체가 만들어진 순간**(아직 지형도 안 굽고 조명도 안 켠 때) 이미 다 열린 것으로 읽혔다.

`main.js` 가 `S.booted = true` 는 세우는데 그것은 `S` 쪽이고, 훅은 별개 객체였다.
훅에 `booted: false` 를 두고 맨 끝에서 `true` 로 바꾼다. 시험이 그 존재를 못 박는다.

**교훈:** `x !== false` 로 "준비됐나" 를 묻는 조건은 **그 값이 없을 때도 참**이다.
있는지부터 단언한다(`hasOwnProperty`). 오늘 이 한 줄 때문에 275항목이
"부팅이 끝났다" 를 실제로는 한 번도 확인하지 않고 있었다.

임시 확인은 **저장소 파일을 안 건드리는 방법**으로 했다 —
Playwright 의 `ctx.route("**/src/mesh.js", r => r.abort())` 로 모듈 하나를 막아
부팅을 죽이고, 감시견이 뜨는지 봤다(떴다). 파일을 고쳤다 되돌리는 방식은 어제 사고를 냈다.

## v69 — 자문 10차 (2026-09-07)

자문이 **헤드리스로 직접 재현·계측**해 왔다. 10건 다 되돌리기 어려운 것은 없었다.
자문이 물어보라던 두 갈래는 이렇게 정했다 —
불(#1)은 **설정으로 끄기**(더 작고 마크의 `doFireTick` 자리), `BATCH_RELIGHT_ALL`(#3)은 다음으로.

| # | 증상 (자문 실측) | 고침 |
|---|---|---|
| 4 | **되돌리기가 거짓말을 한다.** 허공에 모래 한 칸을 놓으면 떨어져 y=1 에 앉는데, `Ctrl+Z` 는 `되돌리기 — 모래 놓기` 라고 말하고 **모래는 그대로 남는다** | `fallTick` 이 `applyEdit` 을 건너뛰고 `world[]` 를 직접 쓴다(`CLAUDE.md` 6절 첫 줄). 그 편집이 연 묶음(`S.fallOwner`)에 낙하를 같이 담는다 — 이제 되돌리면 떨어진 자리의 모래까지 사라진다 |
| 1 | **불이 집을 139칸 먹는데 한 칸도 못 돌린다.** 12×12 판자 오두막 → 3000틱에 464→325장. 번짐은 `record=false` 라 기록에 없다. **이 게임에서 유일하게 영영 사라지는 경로** | 설정 "불 번짐" 끄기 (마크의 `/gamerule doFireTick false`). 시험이 그 오두막을 그대로 지어 태워 본다 |
| 9 | **`F3` 이 시야거리를 거짓말한다.** 자동 조절이 40m 로 줄여 놨는데 `시야 120m` 이라고 찍는다 — 원인을 보러 연 화면이 원인을 가린다 | `farNow()` 를 찍고, 줄어 있으면 `40m (자동 · 설정 120)` |
| 10 | **성능 화면에 시간이 한 줄도 없다.** 117ms 짜리 프레임을 0.25초 평균이 **58 FPS** 로 뭉갠다 | `최악 117ms (최근 1초)` 한 줄 |
| 7 | **세계에 이름을 붙였는데 게임 안에서 한 번도 안 보인다** — 쓰는 곳이 저장과 접힌 슬롯 목록 둘뿐 | 미니맵 캡션이 이름 우선(없으면 SEED). 겸사겸사 캡션 갱신을 `refreshMinimapCap()` 으로 뺐다 — `animate()` 안에 박혀 있어 시험이 닿을 수가 없었다 |
| 8 | **이어하기 뒤 `Ctrl+Z` 가 "더 없음" 이라고만 한다** — 240단계라고 적어 놓고 껐다 켜면 0인데 그 말을 안 한다 | `undoEmptyWhy` — 이어하기 직후에만 "지난 판의 되돌리기는 남지 않습니다" |
| 6 | **표식 12개를 찍어 놓고 읽을 목록이 없다** — 96px 미니맵의 7px 글자뿐이고 화면 밖 표식은 이름도 안 나온다 | `/marks` — 번호·이름·좌표를 글자로 |
| 5 | **안전망이 20초짜리다.** `pushBackup` 이 `saveGame` 마다 도니 **자동 저장 한 번**에 옛 세계가 덮인다 | 갈아타기 직전용 `.prev` 키를 따로 둔다(자동 저장이 못 덮는다). 토스트도 "옛 세계는 설정 › 직전으로 되돌리기 에" |

### 4번이 오늘 가장 나쁜 것이었다
되돌리기가 **작동을 안 하는 것보다, 했다고 말하면서 안 하는 것**이 나쁘다.
받침돌을 캐면 그 위 자갈이 무너지는 건 갱도 파기의 일상인데, 지금까지는 되돌려도 무너진 채였다.
고침을 되돌려 시험이 실제로 깨지는 것(`모래가 1칸 남았다 (y=1)`)까지 확인했다.

### v69 곁가지 — 같은 임시 스크립트에 **두 번** 당했다
어제(v68) `check-live.mjs` 를 시험하려고 `src/main.js` 를 고쳤다 되돌리는 스크립트를 썼다.

① 그때는 타임아웃으로 백그라운드에 넘어가며 **`finally` 가 안 돌아** 파일이 깨진 채 남았다.
② 오늘 그 스크립트가 **드디어 끝나면서 `finally` 가 돌았는데**, 시작할 때 읽어 둔 사본이
   이미 몇 시간 낡아 있었다. **v67~v69 의 import 가 통째로 날아갔다** — 되돌리는 것 자체가
   되돌리기가 됐다. 알림을 보고 바로 `git status` 를 봐서 잡았고, 배포본은 커밋된 판이라 무사했다.

`CLAUDE.md` 2절에 9번으로 못 박았다 — **임시 스크립트가 `src/` 를 고쳤다 되돌리게 하지 않는다.**
부팅·모듈을 깨뜨려 볼 때는 네트워크를 가로챈다(`ctx.route(..., r => r.abort())`).
그래도 썼다면 **커밋 전에 `git status` 를 본다.**

## v70 — 바닥 한 장에 세계 전체를 다시 굽던 것 (2026-09-07 · 자문 10차 #3)

`settleWorld()` 가 `relightAll(true)` 뒤에 **`markAllDirty()`** 를 얹고 있었다.
그런데 `relightAll(true)` 는 `prevSky`/`prevBlk` 와 견줘 **빛이 실제로 바뀐 청크만** 표시한다
(`src/light.js`). `markAllDirty()` 가 그 정밀도를 통째로 버렸다.

`markAllDirty()` 가 거기 있던 이유는 따로 있었다 — 묶음 편집 중에는 `applyEdit` 이
`touch()` 대신 `S.batchCells++` 만 하므로 **블록이 바뀐 자리가 표시되지 않는다.**
그러니 필요한 건 "전부" 가 아니라 **묶음이 들고 있는 그 칸들**이었다.
`settleWorld(list)` 가 `list` 를 받아 그 칸만 `touch`·`refreshTop` 한다.

**같은 세계에서 한 변수만 바꿔 잰 값**

| | 고침 전 | 고침 후 |
|---|---:|---:|
| 바닥 한 장(23×23=529칸) 다시 굽는 청크 | 144 | **8** |
| 그걸 다 굽는 데 걸린 프레임 | 5 | **1** |
| 32³(32,768칸) 청크 | 144 | **32** |
| 프레임 | 6 | **2** |
| 힙 | 54.7MB | **31.2MB** |

**정확성은 지문으로 못 박았다** — 32³ 채우기 뒤의 `lightSky`·`lightBlk` 전체 해시가
`relightAll(false)`(세계를 처음부터 다시 켠 정답)와 **한 비트도 다르지 않다.**
되돌리기 뒤에도 같다. 조명은 "대충 맞다" 가 없다 — 어긋나면 어두운 얼룩이 남는다.

**교훈:** 이미 정밀한 계산을 해 놓고 그 결과를 **덮어쓰는 한 줄**이 성능 문제의 정체일 때가 있다.
`relightAll` 은 처음부터 바뀐 청크를 알고 있었다. 아무도 그걸 안 쓰고 있었을 뿐이다.

## v71 — 시작 화면이 내 세계를 안 보여 주던 것 (2026-09-08 · 자문 10차 #2)

3일 만에 열어도 **접지 않고 읽히는 것이 버튼 글자 "이어하기" 세 자뿐**이었다.
이름·플레이 시간·"3일 전"·섬 그림·표식이 전부 저장에 있는데
접힌 `설정` 안에만 있어서, 펼치기 전엔 존재를 모른다.

`<h1>` 바로 아래에 **"이어서 짓던 곳"** 한 칸을 뒀다 — 섬 그림 + 이름 + 마지막 시각 +
플레이 시간 + 놓음/캔 + **표식 이름들**. 돌아갈 곳이 곧 "하던 일" 이다.

**돌아온 사람에게는 다섯 줄 소개문을 접는다.** 마크도 월드 목록이 첫 화면이다.
저장이 없으면 반대로 소개문이 보이고 이 칸이 숨는다.

`drawPreview(target)` 이 캔버스를 받게 해 시작 화면과 설정이 같은 그림을 쓴다.

**측정을 두 번 고쳤다.** 처음에는 `card.textContent` 로 "보이는 글자" 를 셌는데,
`textContent` 는 **`hidden` 요소도 읽는다.** 소개문을 접어 놓고도 그대로 나왔다.
`hidden`·`display:none`·접힌 `details` 를 걸러 읽는 함수를 시험에 넣었다 —
**"보인다" 를 재려면 진짜 보이는 것만 세야 한다.**

폰 시험도 같이 넣었다(v67 의 그물) — 폰 폭에서 가로로 안 넘치고 섬 그림이 안 사라지는지.

## v72 — 번져서 탄 집이 되돌아온다 (2026-09-08 · 백로그 마지막 항목)

v69 는 "불 번짐 끄기" 설정으로 막았다(마크의 `doFireTick`). 이건 **켜 둔 채로도** 되돌아가게 한다.

`fireTick` 의 번짐·꺼짐이 `applyEdit(..., false)` 라 기록에 없었다.
v69 의 `S.fallOwner` 와 같은 방식으로 `S.fireOwner` 를 뒀다 —
`ignite` 가 만든 그 묶음에 번짐과 타 없어짐을 같이 담는다(`fireEdit` 한 함수로 모았다).

한 칸의 일생이 `판자 → FIRE → AIR` 인데, 되돌리기는 묶음을 **거꾸로** 되짚으므로
`AIR → FIRE → 판자` 로 제자리에 온다. 12×12 오두막을 3000틱 태워
**48장이 사라진 뒤 `Ctrl+Z` 한 번에 220장 전부** 돌아온다(고침을 되돌려 깨지는 것까지 확인).

불은 오래 타므로 `FIRE_UNDO_MAX = 20000` 상한을 뒀고,
새 편집이 시작되면(`beginBatch`) 지난 주인은 놓는다.

**이제 이 게임에 "영영 사라지는 경로" 가 없다.** TNT·모래 낙하·불이 전부 되돌아온다.

## v73 — 자문 11차 · 손이 지루하던 자리와 귀가 비어 있던 자리 (2026-09-08)

자문이 **소리 발음 횟수**를 직접 세어 왔다(`createOscillator` 를 감싸서).
"소" 여섯 건을 넣었다.

| # | 증상 (자문 실측) | 고침 |
|---|---|---|
| 1 | **폰에서는 반블록·계단에 갈 길이 화면에 0개.** `S.shapeMode` 를 바꾸는 곳이 `G` 키와 픽블록뿐인데 폰엔 둘 다 없다 — 30분을 지어도 나오는 건 네모 상자뿐이었다 | 터치 **모양** 단추. v66 이 계단 모서리에 판을 통째로 썼는데 **폰은 그 계단을 놓아 본 적이 없었다** |
| 3 | **`Alt`+클릭 영역 모서리가 6칸을 못 넘고, 넘으면 코앞 허공을 찍는다.** `aimCell` 이 안 맞으면 눈앞 4칸을 돌려줘 **늘 성공한 것처럼** 보였다 | 영역 찍기만 사거리 64 · 안 맞으면 `null`(`strict`)과 토스트 |
| 2 | **`/fill` 에 조건이 없다.** 다 지은 20×10 벽을 다시 칠하려면 200칸을 하나씩 캐고 놓아 **4분 40초** | `/fill 조약돌 벽돌` — 그 블록인 칸만 바꾼다 |
| 5 | **`/clone` 이 한 번뿐**이라 기둥 열둘에 명령을 열두 번. 게다가 `Ctrl+C` 클립보드를 조용히 덮었다 | 횟수 인자(최대 64) · 클립보드 복원 |
| 6 | **불이 정지 화면이고 무음.** `LIQUID_TILES` 에 불(타일 55)이 없었다 | 타일 애니메이션 + 근접 타닥 소리 |
| 4 | **바다가 완전한 무음.** 6칸 위에서 뛰어들어도 0개, 물가 5초에 0개 (용암 옆은 7개) | 입수 첨벙 + 물가 잔물결. **용암 뽀글의 틀을 `ambientNear` 로 뽑아** 물·불이 같이 쓴다 |
| 7 | **낮이 밤보다 적막했다** — 자연음이 `dayLight < 0.35` **안에만** 있어 낮에는 설계상 무음(20초에 1개 · 밤은 5개) | 낮에는 새소리 |

### 두 번 헛짚었다
① `ambientNear` 의 세로 훑기를 `-3..3` 2칸 간격으로 써서 **0을 건너뛰었다** —
   눈높이에 있는 불이 안 들렸다. 용암 쪽은 `-4..4` 였다.
② 밤 귀뚜라미는 `setTimeout` 으로 세 음을 울린다 — **동기 루프에서는 발음이 안 잡힌다.**
   시험이 `setTimeout` 예약까지 세게 했다. "울리기로 했는가" 도 소리다.

## v74 — 실내 소품: 책장과 카펫 (2026-09-08 · 자문 11차 #10)

놓을 수 있는 53종에 **가구·소품이 0개**였다. 방을 다 짓고 들어가면 텅 빈 상자였고,
양털로 침대 흉내를 내다 나온다. 마크에서도 실내를 채우는 것의 절반은
**텍스처만 다른 통짜 블록**이라, 가장 값싼 둘부터 넣었다.

- **책장(57)** — 통짜. 옆면에 책이 꽂혀 있고 위아래는 판자 나이테.
- **카펫(58)** — **한 겹(1/16)**. 딛고 서되 걸리지 않고, 빛을 막지 않고,
  받칠 바닥이 있어야 놓이고, 발소리가 푹신하다(`SOFT`).

### 얇은 블록을 넣자 낡은 지름길이 드러났다
`surfaceTop()` 이 `boxesAt()` 을 안 거치고 **`SHAPE_BOXES` 를 직접** 읽고 있었다.
그래서 카펫 위에 서면 **한 칸 떠올랐다.** `boxesAt` 으로 돌렸다 —
울타리·유리판·계단 모서리(v66)도 같은 지름길에 걸려 있었을 자리다.

`docs/GAMEPLAY.md` 블록 체크리스트에 그 교훈을 14번으로 적었다:
**모양이 필요한 곳은 반드시 `boxesAt()` 을 거친다.**

v63 에서 만든 "블록이 화면까지 닿는가" 그물이 새 블록 둘을 **자동으로** 검사했다 —
목록 단추·갈래·이름 검색이 다 걸려 있어 따로 시험을 쓸 필요가 없었다.

## v75 — 바다에 볼 것을 두다 (2026-09-08 · 자문 11차 #8)

지표 기둥 9,216칸 중 **바다가 63~80%**(시드 5개 실측)인데, 그 안에 있는 것은 물고기 점 18개뿐이었다.
탑에 올라가면 눈에 들어오는 것의 3/4 가 균일한 파랑 — "다 지었는데 허전하다" 의 정체다.

- **해저를 얼룩덜룩하게** — 노이즈를 따라 자갈·돌 무늬. 시드마다 762~1,000칸.
- **작은 바위섬·모래톱** — 본섬에서 6칸 넘게 떨어진 깊은 물에만, 최대 7개.
  가장자리를 한 칸 낮춰 네모 판이 아니라 섬으로 보이게 한다. 수평선이 끊긴다.

**새 블록을 안 썼다.** 물속 교차 쿼드(해초)는 그 칸이 물이 아니게 되어
둘레 물 면이 갈라져 보인다 — 마크의 waterlogged 가 없으면 값이 싸지 않다.

### 기존 시드가 안 바뀌게 하는 법
`decorateSea` 가 **자기만의 난수**(`makeRng(seed + 90210)`)를 쓴다.
위의 `rng` 를 이어 쓰면 호출 차례가 밀려 **모든 시드의 땅·동굴·나무가 달라진다**(v61 교훈).

그 주장을 시험으로 못 박았다 — `S.noSeaDecor` 로 같은 페이지에서 두 판을 생성해
**뭍 기둥의 높이**와 **지표 아래(동굴·광맥) 해시**를 견준다. 세 시드 모두 동일하다.

**측정에서 두 번 헛짚었다.**
① 새로 생긴 바위섬이 `heightMap > SEA` 라 "뭍" 으로 세어져 비교가 어긋났다.
② 해시를 각자 자기 높이까지 훑어, 섬만큼 더 읽고 "달라졌다" 가 나왔다 —
   **두 판을 같은 자로 재야 한다**(장식 없는 쪽 높이를 자로 썼다).

곁가지: 섬 위에 마른 덤불을 놓았더니 `v9 바이옴` 시험이 "엉뚱한 바이옴의 식물" 로 잡았다.
바이옴 규칙을 깨느니 섬을 맨 바위·모래로 뒀다 — **기존 규칙이 새 기능을 다듬어 준 자리**다.

## v76 — 오두막이 채마다 달라졌다 (2026-09-08 · 자문 11차 #9)

8~10채가 **문·창·횃불 자리까지 전부 같았다.** 두 번째부터 알아보고 세 번째부터는 안 들어간다 —
지어진 것을 보러 갈 이유가 그것뿐인데 복사본이었다.

이제 뽑는다 — **크기**(5~8 × 5~7) · **벽 높이**(3~4) · **재료**(판자·조약돌·벽돌) ·
**무너진 정도**(채마다 다른 확률) · **문이 뚫리는 벽**(네 면 중) · **창 개수와 자리**(1~2개) ·
**지붕**(평지붕·통나무·반블록 처마) · **안에 놓인 것**(책장·카펫·램프) · **횃불 자리**.

`buildHuts` 도 **자기만의 난수**(`seed + 7777`)를 쓴다 — 안 그러면 여기서 뽑는 횟수가 달라져
뒤따르는 풀·꽃·해변이 모든 시드에서 밀린다(v61·v75 교훈).

### 세계 생성이 반블록을 쓸 수가 없었다
처마를 얇게 얹으려다 알았다 — `set(x,y,z,b)` 가 **늘 `shape[i] = SH_FULL` 로 덮고** 있었다.
모양 인자를 받게 했다(`set(x,y,z,b,sh)`). 생성기가 계단·반블록을 쓸 길이 이제 열렸다.

### 시험을 세 번 고쳐 썼다
① 처음엔 "오두막 밖 지형이 한 비트도 같아야 한다" 로 썼는데 계속 깨졌다 —
   오두막이 **고르지 않은 땅을 파고들고**(±2 허용), 그 뒤 **모래 침강 패스가 다시 돈다.**
   "밖" 을 좌표로 깔끔히 가를 수가 없다.
② 그래서 판정을 바꿨다 — **난수 줄기가 밀렸다면 온 세계의 풀꽃이 달라진다.**
   지표 한 겹을 찍어 바뀐 기둥 수를 세고, 그것이 **오두막 발자국 크기(12% 미만)** 인지 본다.
③ 그래도 "반블록 처마가 없다" 가 나왔는데, 훅에 `SH_SLAB` 이 있는지 안 보고
   `undefined` 와 견주고 있었다. **v68 의 19번 교훈이 그대로 재발했다** —
   상수와 견주는 시험은 **그 상수가 있는지부터 단언한다.**

**오염에 또 걸렸다(여섯 번째).** `v76` 이 단독 통과·전체 10/10 실패였다 —
앞선 지형 시험이 `S.terrain` 을 남기면 **평지가 줄어 오두막이 거의 안 선다.**
`S.terrain = 0` 을 첫 줄에서 못 박고, 오두막 수도 **횃불 하나가 아니라
"안에 놓인 것 + 횃불" 합계**로 세게 했다(횃불은 자리가 겹치면 안 놓인다).
"단독 통과 + 전체 실패 = 오염" 서명을 이제 여섯 번 봤다.

## v77 — 세션 인계 체계 (2026-09-08 · 유저가 등재한 SOP)

컨텍스트는 세션과 함께 사라지는데 이 프로젝트는 **세션을 넘어 이어지는 작업**이다.
유저가 두 마디를 등재했다 — **"새세션 준비해줘"** 와 **"새세션 시작"**.

### 만든 것
- `docs/HANDOFF.md` — 색인 계층의 **2층**. `CLAUDE.md`(0) → `docs/INDEX.md`(1) → 여기.
  지금 상태 표 · 마지막 세션이 한 일 · **진행 중이던 것 · 다음 후보 · 이번에 데인 함정** ·
  더 알아야 하면 볼 곳 · 반드시 지킬 것.
- `tools/handoff.mjs` — 그 문서를 **git·시험 파일·백로그에서 읽어** 다시 쓴다.
  `--check` 는 링크·숫자·안 채운 자리를 잡고 문제가 있으면 **종료 1**.
- `CLAUDE.md` 4.6절에 SOP 등재 · `docs/INDEX.md` 에 인계 층 추가.

### 도구가 숫자, 사람이 판단
판 번호·시험 수·모듈 수·백로그 개수는 **손으로 적으면 반드시 어긋난다**(v58 교훈).
도구가 읽어 쓴다. 반대로 "진행 중이던 것 · 다음 후보 · 함정" 은 판단이라 사람이 적고,
`<!-- keep:… -->` 로 감싸 **도구가 안 덮는다**. 안 채우면 `--check` 가 잡는다.

### 검증을 실제로 돌린다
"인계가 됐다" 는 주장은 **새 세션을 흉내 내어** 재야 한다 —
`CLAUDE.md`·`docs/INDEX.md`·`docs/HANDOFF.md` **세 장만** 읽는 서브에이전트에게
여섯 가지를 묻는다(판 번호·시험 수 / 마지막에 한 일 / 진행 중 / 다음 후보 /
절대 하면 안 되는 것 / 시험 돌리는 법). 못 맞힌 항목이 그대로 고칠 자리다.

**함정 절이 이 문서에서 가장 값지다.** 이번 세션에 **실제로 사고가 난** 일곱 자리만 적었다 —
도구가 코드를 지운 것 · 임시 스크립트가 낡은 사본으로 덮은 것 · `x !== false` 의 함정 ·
오염의 서명 · 벤치마크 함정 · 세계 생성 난수 줄기 · "보인다" 를 재는 법.
추상적인 좋은 말은 안 적었다. 다음 세션이 **또 밟을** 자리만 적는 것이 요령이다.

### 검증이 실제로 구멍을 냈다 (v77)
"인계가 됐다" 를 주장으로 두지 않고 **두 번 재검증**했다. 세 장만 읽는 에이전트가
매번 진짜 구멍을 짚었다 — 도구를 만든 사람은 자기 문서가 뭘 빠뜨렸는지 못 본다.

**1차에서 나온 것 넷**
① 시험 수가 **두 값**(도구가 쓴 상태표 281 vs 사람이 쓴 문단 289)으로 갈렸다 —
   사람이 적는 칸에서는 숫자를 빼고 "전 항목 통과" 로만 쓰게 했다.
② `docs/INDEX.md` 의 "현재 상태 한 줄" 이 **`v?`** 로 굳어 있었다.
   `stamp.mjs` 가 **마지막 커밋 하나만** 봐서 "도장 갱신" 위에서 돌면 판 번호를 잃었고,
   게다가 갱신 패턴이 `v\d+` 라 **한 번 `v?` 가 되면 영영 안 고쳐졌다**(`v[\d?]+` 로).
③ **"이미 처리된 것" 목록이 없었다** — 자문을 띄울 때 그게 없으면 같은 제안을 또 받는다.
   커밋 제목에서 판 제목만 뽑아 싣게 했다.
④ 백로그가 7건인데 요약은 3덩어리뿐이었다 — 전체를 표에서 뽑아 싣는다.

**2차에서 나온 것 셋**
⑤ **1순위 후보(지하 구조물)가 백로그 목록에 없었다.** 인용문으로 적어 둬서 표 파서에 안 잡혔다 —
   정식 항목으로 올렸다. **"후보" 와 "백로그" 가 어긋나면 새 세션이 첫 동작에서 막힌다.**
⑥ 자문 프롬프트 **원문 틀**이 없어 매번 새로 짜야 했다 — 틀을 통째로 실었다.
⑦ 시험 **이름 필터**를 뭘 넣어야 할지 몰라 `TESTING.md` 를 열어야 했다 — 앞머리 목록을 실었다.

**교훈:** 인계 문서는 **읽는 쪽에게 시켜 봐야** 뭐가 빠졌는지 안다.
질문을 "알 수 있는가" 가 아니라 **"다른 파일을 안 열고 바로 실행할 수 있는가"** 로 물어야
진짜 구멍이 나온다.

## v78 — 자문 12차 (2026-09-08 · 실측 근거 있음)

고인물 자문 12차가 10건을 냈고, 그중 **9건**을 구현했다.
10번(지하를 깊게 — 모든 시드의 땅이 밀린다)만 유저에게 따로 물어보려고 남겼다.

### 1. 물·용암이 되돌아오지 않았다 (가장 컸다)
지하실 벽 한 장을 잘못 캐서 물 **192칸**이 방에 밀려들었는데 `Ctrl+Z` 는 벽만 돌려놓았다.
좌클릭으로는 물을 못 지우니(레이캐스트가 액체를 건너뛴다) **되돌리기가 유일한 안전망인데
그 안전망만 물·용암을 안 잡고 있었다.** 용암은 한 칸 놓고 되돌리면 12칸이 남았다.
`docs/GAMEPLAY.md` 의 "되돌릴 수 없는 것은 없습니다" 와 v72 의
"이제 영영 사라지는 경로가 없다" 가 사실이 아니었다.

- `S.fluidOwner` 를 뒀다. 모래(`S.fallOwner` · v69)·불(`S.fireOwner` · v72)과 **같은 틀**이다.
  `endBatch` 에서 물·용암 큐 넷 중 하나라도 남아 있으면 그 편집을 주인으로 삼고,
  네 큐가 다 비면 `releaseFluidOwner()` 가 놓는다.
- `waterTick`·`dryTick`·`removeWater`·`lavaFlowTick`·`lavaDryTick` 은 속도 때문에
  `applyEdit` 을 안 거치고 `world[]` 를 직접 쓴다 — 그래서 **쓰기 직전에** `fluidRec` 로 손수 기록한다.
- **묶음에 `toWl`(되돌린 것을 다시 할 때 돌아갈 물 레벨)을 더했다.** 없으면 `redo` 가
  흐르던 물 192칸을 **전부 근원(0)으로** 되살려 무한 물이 생긴다. 시험이 근원 수를 견준다.

### 2. 계단을 걸어 오르면 눈이 한 프레임에 0.5칸 순간이동했다
계단 여섯 단이 딸깍 여섯 번이었다. 웅크릴 때 눈높이는 **이미 부드럽게 내려가는데**
(`S.sneakEye`) 스텝업에만 안 걸려 있었다. `S.stepLift` 를 같은 틀로 두고 카메라에서 뺀다.
물리는 한 줄도 안 건드렸다 — 발은 예전대로 0.5칸씩 오르고 **눈만** 따라 올라온다.

### 3. 폰에서 미니맵의 69%가 터치 단추에 덮여 있었다
단추가 78% 불투명이라 지도가 뒤로 비쳐서 **둘 다 안 읽혔다.** 실측 69%(844×390).
`html.touch` 클래스를 `boot.js` 가 붙이고(CSS 가 `IS_TOUCH` 판정을 그대로 쓴다),
터치에서는 미니맵을 **화면 위 가운데**로 옮겼다 → 겹침 0%.
폰 시험이 "화면 안에 들어오는가" 만 보고 **"서로 겹치는가"** 는 안 봤다. 그 한 줄을 넣었다.

### 4. 번식이 조용히 실패했다
`follow` 22~36초 vs `love` **20초**. 게임이 "아직 따라오고 있다" 고 보여 주는 동안
번식 창은 이미 닫혀 있었다 — **기능이 없는 것보다 거짓 신호가 나쁘다**(v69 #4 와 같은 자리).
`love = follow` 로 맞추고, 사랑이 사는 동안 **1초에 한 번 하트**를 띄운다
(`LOVE_HINT` 는 먹인 순간·태어난 순간 두 번만 쓰이고 지속 표시가 0이었다).

### 5. 폰에는 픽블록도, 핫바 2쪽도 없었다
`pickBlock()` 을 부르는 곳이 Q·휠클릭·패드 셋뿐이었다. **뽑기** 단추를 넣고,
**목록을 길게 누르면 핫바 2쪽**(울타리·문·사다리·유리판·TNT·부싯돌 열 종)이 나오게 했다.
`Tab` 과 같은 길을 타도록 `swapBarPage()` 로 뽑았다.

### 6. 폰의 도움말이 키보드 설명서였다
1,155자에 `<kbd>` 68개, 터치 낱말 2개. 있지도 않은 키 이야기다.
`#help-touch` 한 벌을 따로 쓰고 `html.touch` 로 갈아 끼운다.
**"영역 도구·명령창·시간·날씨·표식은 지금 키보드에서만 됩니다" 를 적어 두었다** —
폰에서 도달할 수 없다는 사실조차 안 알려 주던 것이 가장 나빴다.
곁가지: `helpCols` 가 `querySelector` 하나만 잡고 있어 도전 과제 탭으로 넘어가면
나머지 한 벌이 남는다 — `querySelectorAll` 로.

### 7. 놓기 반복이 문서와 어긋나 있었다
`PLACE_REPEAT` 0.35초 = 실측 1.79칸/초. 마크는 4틱(0.20초) = 5칸/초.
`docs/GAMEPLAY.md` 는 **0.2초라고 적어 두고** 있었다.
0.35 로 늦춘 까닭은 "조준한 칸이 바뀌면 쿨다운을 건너뛰어 한 번 누른 것이 여러 개로
놓이던 것" 인데 — **원인은 건너뛴 것이지 간격이 짧은 것이 아니었다.** 0.20 으로 되돌렸다.
첫 뜸(`PLACE_DELAY` 0.50)은 그대로다.
**`v22` 시험이 `repeat >= 0.3` 을 못 박고 있어 같이 옮겼다** — 이제 상수에서 유도한다.

### 8. 동물이 무리를 안 지어 번식이 성립하지 않았다
5시드 중 4시드에서 **"같은 종이 3칸 안에 있는 쌍" 이 0개**였다(중앙값 8~18칸).
번식 조건이 "같은 종 · 3칸" 이라 **처음 만나는 벽에서 막혔다.**
`seedMobs` 가 낱개로 뿌리던 것을 **같은 종 네 마리 한 덩어리**로 바꿨다.
`placeMob` 을 `placeAround(m, cx, cz, dmin, dmax, tries)` 로 일반화해 우두머리 곁(1.2~3.0칸)에 놓는다.
**총 마릿수는 그대로다** — 시험이 14를 못 박는다.

### 9. 눈이 세계를 거의 안 바꿨다 (자문의 "0칸" 은 과장이었다)
자문은 "눈 5분 뒤 세계 변화 0칸" 이라고 했는데 **직접 재 보니 +31칸이었다.**
코드는 이미 있었고(`loop.js` 의 눈 쌓기), **풀·흙 위에만** 쌓는 것이 문제였다 —
설원(지도의 30%)은 겉이 이미 눈이라 아무 일도 안 일어난다.
`snowSticksTo()` 로 **드러난 통짜 고체면 어디에나** 쌓게 넓히고 표본을 6→10으로 올렸다.
이미 눈이면 건너뛰므로 **스스로 멈춘다**(한 겹 덮고 더 두꺼워지지 않는다).
`isTouched` 는 그대로 — 사람이 놓은 것을 날씨가 개조하지 않는다(v19 의 약속).

> **남의 계측도 확인하고 쓴다.** 자문이 낸 숫자 아홉은 그대로 재현됐지만
> 9번의 "0칸" 은 틀렸다. 고치기 전에 한 번 재 보는 값이 있다.

### 시험
새로 8항목(`v78` 5 + 폰 3), `v22` 1항목을 새 계약으로 옮겼다.
**288 + 폰 9** (러너는 오류 검사 2를 더해 299). 전 항목 ×10회 통과.

## v79 — 지하를 깊게 (2026-09-08 · 유저 승인 "새 세계에만 적용")

자문 12차의 10번. 세계는 64칸인데 **지표에서 기반암까지 13~15칸뿐**이라
"내려갈수록 좋은 것" 이라는 사다리가 설 자리가 없었다 (석탄 중앙값 y=4, 철·금·다이아 y=3~4).
반대로 최고봉 위로는 30~38칸이 **늘 빈 하늘**이었다.

### 한 것 — 지형을 통째로 12칸 위로 밀었다
`WY`(64)도 저장 포맷(v5)도 안 건드렸다. 물속 깊이도 언덕 높이도 그대로고,
늘어난 것은 **기반암 위 돌의 두께**뿐이다.

| | 판 1 (v78 까지) | 판 2 (v79 부터) |
|---|---:|---:|
| 해수면 | 11 | **23** |
| 뭍 평균 높이 | 14.6~16.7 | **26.6~28.7** |
| 동굴 공기 | 10,158~18,035 | **33,998~57,531** |
| 최고봉 위 여유 | 40~44 | 28~32 |
| 전체 메싱 | 34~45ms | 52~65ms |

### 해수면을 세계마다 다르게 — `export var` 라이브 바인딩
`SEA` 는 25곳에서 import 된다. `dims.js` 는 **아무것도 import 하지 않는 뿌리 모듈**이라
값을 바꿀 곳이 거기밖에 없다. `export var SEA` 는 라이브 바인딩이라
**dims.js 안에서 재대입하면 import 한 쪽이 다시 읽지 않아도 새 값을 본다** — `setGen(g)` 하나만 바꾼다.
"읽기는 import, 쓰기는 S" 규칙의 정신을 그대로 지키면서 뿌리 모듈에 순환을 안 만든다.

**모듈 최상위에서 값으로 붙잡은 곳이 두 군데 있었다** — 라이브 바인딩의 함정이다:
- `scene.js` 의 `OUTER_SEA_Y = SEA + 1 - 0.12` → `outerSeaY()` 함수로
- `main.js` 훅의 `SEA: SEA` → `get SEA() { return SEA; }` (게터가 없었으면 시험이 예전 판에 굳는다)

### 저장은 `gn` 한 칸
지형 판만 담고 해수면은 거기서 따라온다. **없으면 1 → 바다 11 → 예전 저장이 그대로 열린다.**
저장 버전은 v5 그대로다(`wt`·`sm`·`nm` 과 같은 선택 필드 갈래).
`liftLegacy` 는 손댈 필요가 없었다 — 세계 데이터 자체는 형식이 안 바뀌었다.
`generate(seed, gen)` 로 판을 지정할 수 있게 열어 두었다. 시험이 두 판을 견준다.

### 광석 — 시도 횟수를 올렸다가 되돌렸다
처음엔 "돌이 두 배니 광맥 시도도 두 배" 로 잡았다. **세계당 다이아가 11~27개에서 65~92개**가 됐다.
깊은 판은 낮은 층이 통째로 단단한 돌이라 **씨앗이 거의 안 걸러진다** —
예전 판은 절반이 동굴·흙에 떨어져 버려졌다. 시도 횟수를 되돌리고, 다이아 몫만 0.030 → 0.018 로 낮췄다.

| 시드 333 | 판 1 | 판 2 |
|---|---:|---:|
| 석탄 | 657 | 1,704 |
| 철 | 247 | 618 |
| 금 | 26 | 97 |
| 다이아 | 27 | 27 |

깊이 사다리는 `oreCeil()` 이 정본이다 — 다이아 7→8 · 금 11→14 · 철 16→20.
**시험이 그 함수를 읽는다.** 숫자를 두 군데 적지 않는다 (v58 교훈).

### 곁가지로 나온 진짜 버그
**카브가 지표를 뚫은 자리에 장식이 허공에 떴다.** 사막의 죽은 덤불·선인장을
`heightMap` 만 믿고 얹은 뒤, **모래 가라앉히기 패스가 나중에 돌면서** 받침을 빼 갔다.
예전 판에도 있던 구멍인데 지하가 깊어져 지표 카브가 잦아지며 드러났다.
가라앉힌 **뒤에** 받침 잃은 것을 걷어내는 패스를 넣었다 (선인장은 통짜 블록이라 따로 챙긴다).

### 시험
- 깨진 것 다섯을 새 계약으로 옮겼다 — `v6 세계`(높이 여유 → 하늘·지하 분배) ·
  `v11 광석`(천장을 `oreCeil()` 에서 읽는다) · `v20 얼음`(좌표를 `SEA` 기준으로) ·
  `v6 풀·꽃`(진짜 버그였다) · `v65 비행`.
- **`v65` 는 여덟 번째 오염이었다.** 단독 통과·전체 실패. 앞선 시험이 남긴 `S.flySpeed` 때문에
  느린 쪽·빠른 쪽이 둘 다 가속 한계에 눌려 2배 차이가 1.5배로 읽혔다. 첫 줄에서 못 박았다.
- 새로 3항목(`v79`). **291 + 폰 9** (러너 302). 전 항목 ×10회 통과.

> **자문의 진단이 맞아도 처방은 재 봐야 한다.** 자문은 "지형 기준 높이만 내려서" 라고 했는데,
> 실제로는 **올려야** 지하가 생긴다. 그리고 "시도 횟수도 같이" 는 재 보니 반대였다.

## v80 — 자문 13차 · v79 가 지상에 남긴 대가를 치렀다 (2026-09-08)

자문 13차가 10건을 냈는데 **여덟이 v79 회귀**였다. 지형을 12칸 올리면서
**해수면을 따라가야 할 것들이 안 따라 올라갔다.** 아홉 건 처리, 열째는 절반.

> **v79 가 잡았다고 적어 둔 그 함정의 세 번째 자리에서 또 데었다.**
> `memory.md` v79 에 "모듈 최상위에서 값으로 붙잡은 곳이 두 군데 있었다" 고 적어 놓고,
> **오브젝트에 한 번 넣고 끝나는 곳**(`m.position.y = outerSeaY()`)은 못 봤다.
> 라이브 바인딩을 쓰면 **읽는 자리를 전부 세어야 한다** — 상수·게터·오브젝트 속성 셋 다.

### 1. 바깥 바다 판이 y=11.88 에 남아 있었다 (수평선이 12칸 아래)
물가에 서면 발밑 물은 23 인데 지평선 물은 11.88 — 바다가 세계 끝에서 절벽처럼 떨어지고
수평선이 활처럼 휘었다. **매 세션 가장 오래 보는 그림**이었다.
`updateOuterSea` 가 **매 프레임 다시 맞춘다.** 구름 두 층도 같이(부동소수 두 번 쓰는 값이다).

### 2. 그리는 삼각형의 89%가 발밑 지하 (프레임 −19%)
시야를 40m 로 줄여도 청크가 하나밖에 안 줄었다 — **96칸 섬은 그 거리 안에 세계가 거의 다 들어온다.**
자동 조절의 유일한 손잡이가 이 비용에 안 먹었다.
청크 기둥(16×16)의 **가장 낮은 지표**보다 통째로 아래인 청크는 지상에서 볼 길이 없다.
34칸 밖의 그런 청크를 안 그린다 — **삼각형 121,486 → 86,368 (−29%) · 봉우리에서 −34%.**
지하로 내려가면 전부 돌아온다. 판 1 에서는 거의 안 걸러진다(얕아서 묻힌 청크가 없다).
`scene.js` 가 `world.js` 를 import 하지 않도록 **바닥 배열을 loop.js 가 재어 넘긴다.**

### 3. 갱도 과제가 y ≤ 8 에서만 열렸다
판 2 에서 지표 아래 30칸을 파도 안 열렸다. 상한을 `SEA - 3` 으로 —
판 1 에서는 그 값이 정확히 예전의 8 이다.
**곁가지 버그:** 횃불을 건 칸이 "파낸 칸" 에서 빠져 200칸을 파도 190으로 세어졌다(판 1 도).

### 4. 시작 화면이 모든 세계를 "산 90%" 라고 불렀다
`y > 24` 를 산으로 셌는데 판 2 의 뭍 평균이 26~32 라 평지도 전부 산이었다.
**시드를 고를 유일한 정보 두 개 중 하나가 상수가 됐다.** `SEA + 13` 으로.

### 5. 동굴 울림이 y < 22 에서만 났다
판 2 에서 굴을 19칸 파고 내려가도 무음, 겨우 들리는 y=20 도 y=4 의 1/9 음량.
**절대 높이가 아니라 "머리 위에 흙이 얼마나 있느냐"** 로 바꿨다(`topMap - pos.y > 5`).
분모도 지하 두께로. v73 이 채운 자리를 v79 가 절반 비웠던 것을 되돌렸다.

### 6. 용암이 y=1~4 에 못 박혀 있었다
지하는 13→26칸이 됐는데 용암 띠는 4칸 그대로. **블록광이 닿는 동굴 공기 비율 34.8% → 17.7%.**
코드 주석이 스스로 용암을 "지하 탐험의 유일한 시각 목표" 라고 적어 뒀는데 그걸 안 늘렸다.
`lavaTop()` 을 두고 판 2 는 12 로 — **34.8% / 33.9% / 33.8% 로 판 1 과 같아졌다.**
(`oreCeil()` 과 같은 갈래다 — 시험이 그 함수를 읽는다.)

### 7·8. 구름이 산에 닿고, 시작 화면 카메라가 봉우리보다 낮았다
구름 바닥 58.2 고정 · 최고봉 43~44 → 하늘 여유 26칸에서 14칸.
시작 화면 카메라는 34 고정이라 **봉우리보다 5~10칸 낮아** 눈앞이 산비탈이었다.
둘 다 `seaLift()` 를 태웠다.

### 9. 새 지하의 위 절반이 텅 빈 회색 돌이었다
석탄의 80% · 철의 82%가 아래 12칸에 몰려 있었다. 원인은 **씨앗을 온 지도에 고르게 뿌린 것** —
바다 기둥(68%)은 얕아서(vTop 7~18) 씨앗이 전부 낮은 층에 떨어진다.
**별도 난수 줄기**(`makeRng(seed + 5150)`)로 지하 위쪽만 따로 채웠다 —
그래야 땅·동굴·나무·오두막·바다 장식이 한 비트도 안 달라진다(v61·v75 규칙).
결과: 석탄이 y0~27 에 고루 퍼지고, 첫 광석까지 깊이가 판 1 의 9칸 대 판 2 의 11~13칸.

### 10. 지하에서 길을 잃는다 — 절반만 했다
- **굴 어귀를 지상 지도에 주황 점으로.** `heightMap - topMap >= 3` — 두 배열이 이미 있어 뺄셈 하나다.
- **지하 단면에서 표식 옆에 `▲n`/`▼n`.** 26층이 되면서 위층 표식과 아래층 표식이 똑같이 그려졌다.
- 남은 것(`SEEN_UNDER` 한 장이 26층을 나눠 쓴다 · 어귀 자체가 적다)은 `docs/BACKLOG.md` 로.

**굴 어귀 문턱은 되돌렸다.** 0.66 → 0.638 로 낮춰 봤지만 어귀가 178→189 로 거의 안 늘었다 —
병목은 문턱이 아니라 `carvedBelow` 조건이었다. **지형을 흔들 값이 아니라 원래대로 뒀다.**

### 시험
`v80` 6항목 추가, `v6 용암`·`v80 그리기` 를 새 계약으로. **297 + 폰 9** (러너 308).
전 항목 ×10회 통과. `v80 그리기` 는 아홉 번째 오염이었다 — 앞선 시험이 남긴 `S.terrain` 이
판 1 의 묻힌 청크 수를 바꿨다. 첫 줄에서 `S.terrain = 0` 을 못 박았다.

> **큰 축을 옮기면 "그 축을 읽는 곳" 을 전부 세어야 한다.**
> v79 는 `SEA` 를 옮기고 import 25곳을 확인했는데, **파생값**(수평선·구름·카메라·과제·소리·
> 용암·미리보기·광맥)은 세지 않았다. 자문이 그것을 여덟 개 찾아냈다.
> 다음에 이런 축을 옮길 때는 `grep` 으로 **그 축이 들어간 계산식**까지 훑는다.

## v81 — 지하에 갈 이유와 돌아올 길 (2026-09-08)

백로그 1·2순위. 자문 13차가 "**길을 잃는 문제가 볼 것이 없는 문제보다 크다**" 며
지도를 먼저 꼽았기에 그 순서로 했다.

### 1. 지하 지도를 세 겹으로
`seenMap` 의 지하 비트가 **한 장**이라, 판 1 에서 13층이 나눠 쓰던 것을 판 2 에서는 26층이 썼다.
위층 굴을 밝혀 놓고 아래층에 내려가면 그 칸들이 "밝혀짐" 인 채 **통돌 회색**으로 그려져,
어느 게 아는 길인지 알 수 없었다 (y20 에서 633칸을 밝힌 뒤 y4 에서 보면 돌 82% · 공기 0%).

- 비트 1 = 지상 · **2·4·8 = 지하 얕은/중간/깊은 층**. 층 두께는 해수면이 정한다(`underBand(y)`).
- 지하 밝힘 반경 6 → **8** (지상 14). 지하가 두 배가 됐는데 반경이 그대로면 두 배로 걸어야 한다.
- 저장은 `mv`(지도 판) 한 칸만 더 실었다. **없으면 예전 저장** → `expandLegacySeen()` 이
  지하 한 장을 세 겹으로 펼친다. 밝혀 둔 것을 잃지 않는다. 저장 버전은 v5 그대로.
  (어느 층에서 봤는지는 원래 없던 정보라 복원할 수 없다 — 전부 봤다고 친다.)

### 2. 버려진 갱도
통로 **3칸 폭** × 3칸 높이. 처음에 2칸으로 만들었더니 **버팀목 기둥 둘이 폭을 통째로 막아
걸어갈 수가 없었다** — 마크의 폐광도 기둥 사이 가운데로 걷는다.
조약돌(또는 판자) 바닥 · 4~6칸마다 울타리 기둥과 원목 들보 · 그 사이 바닥에 횃불 ·
줄기의 60%에 5×5 끝방(책장·램프·카펫).

**지켜야 할 것을 함수 하나로 모았다** — `mineBoxOk()` 를 통로와 끝방이 **같이** 쓴다.
처음엔 통로만 재고 방은 대충 쟀는데, 그러면 방이 물속에 열린다.
- 물·용암·얼음을 뚫지 않는다 (뚫으면 세계가 잠기고 되돌릴 사람이 없다)
- 지표를 뚫지 않는다 — `heightMap` 여유 **5칸**. 생성 중에는 `topMap` 이 아직 낡아서 못 쓴다.
- 얕은 판(바다 11)에는 아예 안 짓는다 — 지하 13칸에 통로를 놓을 자리가 없다.
- **별도 난수 줄기** `makeRng(seed + 31337)` — 시험이 `S.noMines` 로 껐다 켜서
  지형 해시가 같은지 본다. 실측 세계당 기둥 64~100 · 들보 51~78 · 횃불 17~22 · 물 접촉 0.

### 데인 자리 — 받침 검사는 **생성기의 맨 마지막**이어야 한다
갱도를 넣자 v6·v79 시험이 깨졌다. **뒤에 판 갱도가 앞선 갱도 횃불의 바닥을 뚫었다.**
v79 에서 같은 부류를 이미 겪었는데(모래 가라앉히기가 사막 덤불의 모래를 내려앉혔다)
그때는 "가라앉히기 다음" 으로만 옮겼다. 이번에 **맨 끝으로** 옮겼다.
> **생성기에 단계를 더할 때는 "그 앞 단계가 놓은 것의 받침을 빼는가" 를 본다.**
> 검사를 중간에 두면 그 뒤에 오는 것이 검사를 통과하지 않는다.

### 곁가지
- 워크트리 격리(`isolation: "worktree"`)로 자문을 띄웠다 — 자문이 **얼어붙은 사본**을 재는 동안
  본체를 고칠 수 있다. 지난 두 번은 자문을 기다리며 20~30분을 놀렸다.
  `tests/harness.mjs` 가 playwright 를 저장소 밖 절대 경로에서 찾아서 사본에서도 그대로 돈다.
  `.claude/worktrees/` 를 `.gitignore` 에 넣었다.

### 시험
`v81` 2항목 추가, `v60 미니맵` 을 새 계약으로(층을 나눴으니 "그 층만 밝혀진다" 까지 본다).
**299 + 폰 9** (러너 310). 전 항목 ×10회 통과.

## v82 — 백로그 세 건 (2026-09-08)

자문 14차가 두 번 스톨해 결과가 없었다. 기다리지 않고 **백로그를 먼저 비웠다.**

### 1. 굴 어귀를 더 뚫는다
v80 에서 지표 카브 문턱(0.66 → 0.638)을 낮춰 봤지만 178→189 로 거의 안 늘었다 —
**병목은 문턱이 아니라 `carvedBelow`("바로 아래 칸이 이미 뚫려 있어야 한다")** 였다.
그래서 **아래에서 위로** 뚫는 길을 따로 냈다. 위에서 아래로 파면 굴과 안 이어져 어귀가 아니다.
지표 아래 14칸까지 굴을 찾아 그 위를 뚫고, 맨 위 두 칸만 2×2 로 넓혀 입을 만든다.
기둥에 물·용암·얼음이 섞이면 그만둔다. **별도 난수 줄기**(seed + 24680).
뭍 기둥 대비 4.6~8.8% → **7.9~14.5%** (판 1 은 8.9~13%).

### 2. 핫바 칸마다 모양 기억
`S.shapeMode` 가 전역 하나라 3번 칸(계단)에서 2번 칸(유리)으로 바꾸면 유리도 계단으로 나왔다.
`S.shapeBar`·`S.shapeBarAlt`(쪽마다 하나)를 두고, **모양을 바꾸는 길을 `setShapeMode(m)` 하나로 모았다.**
읽는 곳이 8군데였는데 **쓰는 곳을 하나로 모으는 게 요령**이었다 — 안 그러면
어느 한 경로(픽블록·G·터치 단추)에서만 모양이 안 남는 버그가 남는다.
`selectSlot` 이 떠나는 칸에 남기고 새 칸에서 꺼내 오고, `swapBarPage` 가 배열째 맞바꾼다.
저장은 `sb`·`sb2`. **없으면 예전 저장 — 그때 쓰던 모양 하나를 고른 칸에만** 넣는다
(전부에 넣으면 다음 칸이 계단으로 나온다). 저장 버전은 v5 그대로.

### 3. 꼭대기 과제를 해수면 기준으로
`y > 50` 이라 크리에이티브에서는 그냥 날면 열렸고, 반대로 산은 아무리 올라도 안 열렸다
(최고봉이 판 1 은 20~32 · 판 2 는 32~44). 이제 **`!flying && onGround && y > SEA + 9`** 다.
평지 세계에서도 9칸 쌓아 올라서면 열리니 영영 못 따는 일은 없다.

### 곁가지 — 지도의 어귀 점이 바다 밑까지 찍고 있었다
`heightMap - topMap >= 3` 을 **겉면(topMap)이 뭍인지**로 걸렀더니, 어귀는 겉면이 파여
내려간 자리라 **언덕의 어귀가 통째로 빠졌다**(360개 중 57개만 표시). 지형 높이(`heightMap`)로
판단하도록 고치고 문턱을 4로 올렸다. 바다 밑 카브는 걸어서 못 가니 안 찍는다.

> **파생 조건은 "무엇을 재는 값인지" 로 고른다.** "여기가 뭍인가" 는 지형 높이의 성질이지
> 겉면의 성질이 아니다. 겉면으로 재면 **바로 그 겉면을 파낸 자리**가 빠진다.

### 곁가지 — 자문 에이전트를 워크트리에 띄운다
두 번 스톨했지만 격리 자체는 옳았다. `isolation: "worktree"` 로 자문이 **얼어붙은 사본**을 재는
동안 본체를 고칠 수 있다. 지난 두 번은 자문을 기다리며 20~30분을 놀렸다.
`tests/harness.mjs` 가 playwright 를 저장소 밖 절대 경로에서 찾아서 사본에서도 그대로 돈다.
**긴 스크립트 하나보다 짧은 스크립트 여러 번** 을 프롬프트에 적어 두는 편이 스톨을 줄인다.

### 시험
`v82` 3항목 추가. **302 + 폰 9** (러너 313). 전 항목 ×10회 통과.

## v83 — 자문 14차 · 열 건 전부 (2026-09-08)

자문 14차는 **두 번 스톨한 끝에** 세 번째로 나왔다(워크트리 격리). 10건 중 여섯이
v79~v82 가 남긴 자리였고, **가장 큰 것은 하루 전에 내가 넣은 v81 의 부작용**이었다.

### 1. 지하 지도가 5초에 스무 번 깜빡였다 (내가 만든 것)
v81 이 지하를 세 겹으로 나눈 대가로, **굴 하나를 걷는데 바닥이 한 칸 오르내릴 때마다
지도가 통째로 백지**가 됐다 — 실측 15걸음에 한 번, 층 전환 196회, 초당 최대 5번.
고친 문제(위층 굴이 아래층에 통돌로 뜨는 것)보다 **새 문제가 더 자주 눈에 띄었다.**

고침: **다른 층에서 밝힌 자리는 흐리게(45%) 남긴다.** 데이터는 이미 비트 셋에 다 있었다.
경계를 옮기거나 이력(hysteresis)을 주는 것보다 이게 낫다 — 층을 나눈 뜻은 지키면서
"밝힌 것을 기억해 준다" 는 약속도 지킨다.

> **나눠서 고친 것은 "나뉜 경계에서 뭘 잃는지" 를 반드시 본다.**
> 층·판·모드를 나누면 그 경계를 넘나드는 순간이 새 증상이 된다.

### 2. 지하가 통째로 군청색이었다
셰이더의 `tint = mix(uNight, white, sky*1.25)` 가 **하늘빛만** 봤다. 지하는 `sky = 0` 이라
블록광이 15여도 tint 가 통째로 달빛색이었다 — 블록광 11 에서 **파랑이 빨강의 1.57배**.
오래된 코드인데 v79·v81 이 화면의 절반을 지하로 만들면서 드러났다.
`tint` 를 블록광 몫만큼 따뜻한 흰색으로 다시 섞는다 (한 줄).

### 3·4. 폰에는 지도를 쓸 길이 없었다
확대(`[` `]`)도 표식(`B`)도 시작 지점(`V`)도 키보드 전용이었다. 폰 지도는 92×92px 에
한 칸 0.96px — 지하 1,200칸을 밝혀도 **잉크가 찍히는 건 7.9%.**
v80 의 굴 어귀 점과 v81 의 ▲n/▼n 이 전부 **"표식을 찍을 수 있다" 는 없는 전제** 위에 있었다.
고침: **지도 자체를 조작으로.** 탭 = 표식 · 길게 = 확대. `#minimap` 의 `pointer-events` 를
터치 기기에서만 켠다. 키보드와 같은 함수(`toggleMark`·`cycleMinimapZoom`)를 탄다.

### 5. 지하 단면에는 굴 어귀가 안 찍혔다
v80 이 "지하에서 길을 잃는 것" 을 고치려고 넣은 점이 **정작 길을 잃는 화면에만 없었다** —
`mouth` 계산이 지상 갈래 안에만 있었다. 뺄셈 하나를 지하 갈래에도 넣었다.

### 6. "시야거리를 줄였습니다" 가 거짓말이었다
`lim = far + CH*1.8`(28.8)인데 청크 반대각선은 8√3 ≈ 13.9 — **두 배**라 시야를 40m 로 줄여도
한 청크도 안 줄어드는 자리가 있었다. `CH*0.9` 로 조였다(지상 70→28 at far 40).
지하는 그래도 안 줄어서(섬이 통째로 들어온다) **굴 속에서 눈높이 위아래 20칸 넘게 떨어진
청크 층**을 걸렀다. 단, **어귀에 서서 밖을 볼 때는 안 건다** — 먼 산이 사라지면 바로 눈에 띈다
(`DEEP_UNDER` 12칸). 지하 86→64 청크.

### 7. 갱도를 가리키는 것이 게임 안에 없었다
과제 38개 중 **생성된 구조물을 찾는 것이 0개**였다. 오두막(v76)·갱도(v81) 둘 다.
`findMine`·`findHut` 을 넣었다. **`touched` 가 "만들어진 것" 과 "내가 지은 것" 을 가르는
유일한 잣대**다 — 내가 놓은 울타리로는 안 열린다(시험이 그것을 본다).
기존 `mineshaft` 과제는 "내 갱도" 로 이름을 바꿨다.

### 8·9·10 (소)
- 밝혔는데 아래가 빈 칸이 **안 가 본 곳과 같은 색**이었다(밝힌 칸의 15~17%). 흐린 회색으로.
  단면 훑는 깊이도 5칸 고정 → 층 두께로.
- **새가 발밑을 날았다** — `s2.y = 34 + rand*16` 고정. 판 2 봉우리(42)에서 66%가 눈 아래,
  1.4%가 바위 속. v80 이 구름·카메라·용암·광맥에 `seaLift()` 를 태울 때 **새만 빠졌다.**
- **천장에 닿아도 말이 없었다.** 지을 하늘이 45→33칸(−27%)이 됐는데 조용히 아무 일도
  안 일어나 마우스만 계속 누르게 됐다. 토스트 한 줄.

### 시험
`v83` 6항목 + 폰 1항목. **309 + 폰 10** (러너 320). 전 항목 ×10회 통과.
천장 시험은 **날아올라 내려다봐야** 조준이 된다 — 맨 윗 블록의 윗면은 세계 밖이라
서서는 못 겨눈다. 조준이 옆으로 새지 않게 천장을 3×3 으로 깔았다.

## v84 — 실내 소품: 색 카펫 16 · 화분 · 액자 (2026-09-09)

백로그의 마지막 "소" 항목. 방을 다 지어 놓고 놓을 것이 책장·카펫·램프뿐이라
어느 집이나 안이 똑같았다.

- **색 카펫 16색** — 텍스처를 **새로 안 그렸다.** 양털 색(HSL 잡음)과 카펫 짜임(테두리·가운데 무늬)을
  겹쳐 아틀라스 61~76 을 만든다. 테두리 밝기는 바탕 명도로 정해 **어떤 색이든 짜임이 읽힌다.**
- **화분** — 바닥에 놓는 낮은 상자(0.44). **액자** — 벽에 붙는 얇은 판(사다리와 같은 규칙).
- 성질은 `isCarpet(b)` 하나로 묶었다. 색이 늘어도 판정이 한 군데다.
  시험이 **열여섯의 성질을 원래 카펫과 통째로 견준다** — 하나라도 어긋나면 그 색만 다르게 군다.

### 새 시험이 v74 의 버그를 잡았다
`dropCross` 가 카펫을 안 봤다 — **놓을 때는 바닥을 요구하면서(`needsFloor`) 밑을 캐면
허공에 떠 있었다.** v74 에서 카펫을 넣을 때 그 목록에 안 넣은 것이다.
새 블록의 성질을 **기존 블록과 통째로 견주는 시험**을 쓰면 이런 게 잡힌다.

> **"같은 부류를 새로 넣을 때는 기존 것과 성질을 통째로 견준다."**
> 항목을 하나씩 확인하면 원래 것에 있던 구멍을 그대로 복사한다.

### 곁가지 — 시험 하네스가 지난 세션 임시 폴더를 절대 경로로 박고 있었다
`tests/harness.mjs` 가 playwright 를 **다른 세션의 스크래치패드**에서 찾고 있었는데
그 폴더가 청소되면서 **시험이 통째로 안 돌았다.** 후보를 훑어 실제로 불러와지는 첫 번째를
쓰도록 고쳤다 (`BY_SCRATCH` → 최근 세션 폴더들 → 저장소).
> 저장소 밖 절대 경로를 하나만 박아 두면, 그 폴더가 사라지는 날 도구가 통째로 죽는다.

### 곁가지 — 열 번째 오염
`v78 번식` 이 10회 중 1회 터졌다. `mobs[0]` 의 종을 그냥 썼는데 **그 종이 한 마리뿐인 판**이 있었다.
"같은 종이 둘 이상인 종" 을 골라 쓰게 고쳤다.

### 시험
`v84` 4항목 (색 카펫 · 저장 · 소품 · 액자 벽). `v16 목록` 을 새 계약으로(색 갈래 = 양털 + 카펫).
**313 + 폰 10** (러너 324). 전 항목 ×10회 통과.

## v85 — 자문 15차 · 열 건 전부 (2026-09-09)

**여섯이 v81~v84 가 남긴 자리**였고, 가장 큰 것은 **지도 기억을 오염시키고 있었다.**

### 1. 나무 밑에 들어가면 지상 지도가 꺼졌다 (그리고 지하 기억을 더럽혔다)
`S.mmUnder = topMap > pos.y + 2.5` 인데 **`topMap` 은 나뭇잎도 내가 얹은 지붕도 센다.**
잎이 겉면인 기둥의 **51%** 에서 지상 지도가 통째로 꺼졌고, 지상 한 줄(77걸음)에 5번 뒤집혔다.
더 나쁜 것: 그동안 `markSeen(underBand(26)=8)` 이 돌아 **가 본 적 없는 가장 깊은 층의
지도 265칸이 "밝혀짐" 으로 칠해졌다.** 지도 기억이 오염되면 되돌릴 길이 없다.

`naturalRoof(x, z, y)` 로 바꿨다 — 머리 위 첫 **자연 고체**를 찾되
**잎과 `isTouched` 인 칸은 건너뛴다.** 실측 51% → **0%**, 뒤집힘 5 → 0, 깊은 층 오염 265 → 0.

> **"머리 위에 뭐라도 있는가" 와 "돌 밑에 있는가" 는 다른 질문이다.**
> v80 이 동굴 울림에서 이미 같은 답을 냈는데(머리 위 두께), 지도는 그 교훈을 안 받았다.

### 2·4. 폰 — 지도가 시점을 먹고, 과제 팝업이 지도를 덮었다
지도는 시점 영역(오른쪽 58%) **안에 통째로 들어앉아 있다.** 위를 올려다보려고 쓸다
손가락이 지도에 닿으면 **시점이 1도도 안 돌고 표식만 찍혔다.**
→ **8px 넘게 움직이면 탭을 접고 `preventDefault` 를 안 해 시점에 넘긴다.**
그리고 v78 이 "터치 단추가 지도의 69%를 덮는다" 를 고치며 옮긴 자리가 **이미 과제 팝업 자리**였다(48%).
팝업을 지도 아래로 내리고 `pointer-events: none` 을 줬다.
> **세는 것을 한 종류만 세면 옮긴 자리에서 또 겹친다.**

### 3·8. 도움말이 v83 이 넣은 것을 "안 된다" 고 말했다
폰 도움말의 「아직 키보드에서만」 칸에 **"미니맵 표식은 키보드에서만"** 이 그대로 있었다.
v83 이 탭=표식·길게=확대를 넣고 **안내를 한 곳도 안 고쳤다.**
지도 기호(주황·금색·파랑·▲n·흐림)도 게임 안 어디에도 설명이 없었다 — 두 벌 다 적었다.

### 5·6. 지도가 갱도를 안 남기고, 어귀 점은 얼룩이었다
- 갱도 조약돌과 자연 돌의 색차가 **ΔRGB 21**(폰에서 한 칸 0.875px) — 사실상 같은 색.
  단면에서 만난 블록이 갱도 재료(울타리·원목·판자·책장·램프)면 **따뜻한 나무색**을 찍는다.
  `touched` 로 내가 지은 것과 가른다.
- 어귀를 **칸마다** 칠했더니 절반 이상이 한 칸짜리 점이고 협곡 하나가 89~233칸을 통째로 칠했다.
  **덩어리로 묶어 한 어귀당 점 하나**(3칸 미만은 버림), 크기에 따라 반지름을 준다.
  지하 단면에서는 그 기둥 둘레 ±4칸에 실제 공기가 있을 때만 찍는다 —
  층과 무관한 기둥 성질이라 25칸 위의 구멍이 발밑 통돌에 찍히고 있었다.
> **지도는 셀 수 있는 개수여야 읽힌다.**

### 7·9. 배율이 안 남고, 핫바가 제 모양을 안 보여 줬다
`S.mmZoom`·`S.contour` 가 저장 어디에도 없었다 — 폰에서 **매 세션 450ms 길게 누르기부터** 해야 했다.
`opts` 에 넣으니 `loadOpts` 가 기본값의 키를 전부 읽는 구조라 저절로 실렸다.
핫바는 칸마다 모양이 딸렸는데(v82) **핫바를 봐서는 알 수가 없어 첫 한 번은 늘 틀리게 놓았다.**
칸 모서리에 글리프(`▄`·`◱`)를 찍고, 칸을 옮겨 **모양이 실제로 바뀌었을 때만** 알린다.

### 10. 갱도가 곧은 복도 다섯 개였다
모든 갱도가 각자 **단 하나의 높이에 평평하게** 누워 있었다(`y[min] == y[max]`). 20초면 벽.
`carveShaft` 로 **토막 2~4개를 꺾어 잇는다** — 토막마다 축을 바꾸고 45%로 한 칸 오르내린다.
**`mineBoxOk` 를 토막마다 걸고 실패하면 거기서 끊는다** — 꺾이게 만들면서 안전성은 그대로다.
갱도가 지나는 높이 1~5개 → **8~11개**, 물·용암 접촉은 여전히 0.

### 곁가지 — 내 정규식이 import 하나를 지웠다
`main.js` 의 hud import 를 정규식으로 고치다 `refreshMinimapCap` 을 통째로 날려
**게임이 안 켜졌다.** 시험이 30초 타임아웃으로 바로 잡았다.
v67 의 tidy-imports 사고와 같은 부류다 — **import 줄을 정규식으로 건드리면 반드시 시험을 돌린다.**

### 시험
`v85` 4항목 + 폰 1항목, `v83 지도` 를 새 계약으로(어귀 = 덩어리마다 하나).
**317 + 폰 12** (러너 329). 전 항목 ×10회 통과.

## v86 — 양동이 (2026-09-09 · 유저가 고른 백로그)

물·용암은 **놓기는 되는데 좌클릭으로 지울 수가 없는 유일한 블록**이었다.
지우려면 `/fill 공기` 나 영역 비우기까지 가야 해서 결국 물을 안 쓰게 됐다 (자문 8차부터 백로그).

**조준선은 그대로 뒀다.** 액체를 건너뛰는 것은 **물속에서 바닥을 캘 수 있어야** 해서 맞는 동작이다.
대신 `raycast(6, wantLiquid)` 인자를 하나 더해 **액체를 걷어내는 도구만 따로 쏜다.**
`place()` 맨 앞에서 양동이를 가로챈다 — 평소 `hit` 로는 물을 영영 못 집는다.

- 부싯돌과 같은 `ITEMS`. **`ALL_BLOCKS` 에 넣으면 "수집가" 과제가 영영 불가능해진다** (v19 교훈).
- 한 번에 한 칸. 근원을 걷으면 흘러 나간 것은 `dryTick` 이 물린다.
- **되돌리기에 실린다** — v78 의 `S.fluidOwner` 가 뒤따르는 마름까지 그 편집에 담는다.
  시험이 "펐다 → 되돌리면 원래 칸 수로 정확히 돌아온다" 를 본다.
- 핫바 2쪽 기본에 넣었다 (TNT 를 빼고). 도움말 두 벌에 다 적었다 —
  **안내를 한 곳에만 적으면 폰에서는 없는 기능이 된다** (v83 에서 데인 자리).

### 시험
`v86` 1항목. **318 + 폰 12** (러너 330). 전 항목 ×10회 통과.

## v86 (2) — 자문 16차 넷 (2026-09-09)

양동이와 같은 판에 담았다. 자문 16차 10건 중 **넷**을 처리하고 여섯은 백로그로 넘겼다.

### 1. 내가 판 굴에서 지도도 소리도 안 켜졌다 — v85 고침의 **거울상**
`naturalRoof` 가 제 기둥의 `topMap` 에서 아래로 훑는데, **내가 판 기둥은 위가 통째로 공기라
`topMap` 이 발밑까지 내려앉는다** → 지붕 없음. 25칸을 파고 내려가도 지도는 지상 조감도 그대로고,
동굴 울림도 안 나고, **머리 위 흙에 가려 볼 수도 없는 지상 613칸이 "밝혀짐" 으로 칠해졌다.**
v85 는 나무 밑을, v86 은 그 반대쪽을 고쳤다 — **한 축을 좁히면 반대쪽이 열린다.**

고침이 두 번 틀렸다:
1. **이웃의 최댓값** → 벼랑 밑에서도 지하가 됐다 (나무 기둥 249/580 오작동).
2. **네 방향이 모두 덮였는가** → 249 → 11. 갈라 주는 것은 "둘러싸였는가" 다 —
   굴은 네 방향이 다 흙이고, 벼랑은 한두 방향, 노천 채석장은 한 방향도 안 덮인다.
3. 둘러싸임에는 **더 깊은 문턱**(8칸)을 줬다 → 11 → 8 (1.4%, 깊은 웅덩이 속 나무 — 그건 맞다).

**지도와 동굴 울림이 같은 함수(`roofDepth`)를 쓰게 했다** — 원인 하나에 증상 둘이었다.

### 5·3. 어귀가 수직 우물이었다 (그리고 지도에 82~84%가 안 찍혔다)
`openCaveMouths` 가 기둥 **하나**를 수직으로 뚫고 맨 위 두 칸만 넓혔다 —
낙차 중앙값 7~9칸(최대 15), 5칸 이상이 62~81%. **들어가면 사다리 없이 못 나온다.**
그리고 1칸짜리라 지도의 `MOUTH_MIN = 3` 에 전부 걸려 안 찍혔다.

**2칸 폭으로 비스듬히** 올린다 — 두 칸에 한 칸씩 옆으로 밀어 계단처럼. 문턱도 3/2 로 내렸다.
지도 점 8~18 → **24~44**.

> **시험은 "걸어서 나올 수 있는가" 를 직접 잰다** — 어귀 바닥에서 한 걸음 +1칸으로
> 하늘(`lightSky === 15`)까지 너비우선으로 가 본다. 낙차 숫자를 재는 것보다 이게 플레이어의 성질이다.
> (그 시험이 처음엔 33%로 떨어졌는데, **relightAll 을 안 해 낡은 lightSky 를 읽고 있었다** —
>  v81 에서 이미 한 번 데인 자리다.)

### 6. 갱도 지도 자국이 세계마다 13~45% 로 딴판이었다
`isMineMat` 이 `COBBLE` 을 빠뜨려, 바닥이 판자인 줄기(40%)만 지도에 길로 남았다.
`COBBLE`·`TORCH` 를 더했다 — 한 줄이다. 지하 자연 조약돌은 실측 0칸이라 헷갈릴 것이 없다.

### 시험
`v86 어귀` 1항목 추가. `v85 지도`·`v83 지도` 를 새 계약으로. **320 + 폰 12** (러너 331).
전 항목 ×10회 통과.

## v87 — 폰이 시점을 되찾다 · 지하 배율 · 표식 이름 (2026-09-09)

자문 16차의 남은 것 중 셋.

### 폰 시점 제스처가 화면의 38%에서 죽어 있었다
`S.lookId` 는 **캔버스의 `touchstart`** 에서만 잡힌다. 그런데 시점 영역(오른쪽 58%)의
**38% 가 캔버스가 아니다** — 터치 단추 147×276 · 지도 105×123(화면 위 **한가운데**) · 핫바 402×46.
그 위에서 시작한 쓸기는 시점에 **영영 안 갔다.**

> **v85 의 고침이 절반이었다.** "지도에서 8px 넘게 움직이면 탭을 접는다" 까지는 했는데,
> `preventDefault` 를 안 하면 시점으로 넘어갈 거라 여겼다 —
> **`preventDefault` 와 이벤트 라우팅은 무관하다.** target 이 지도면 캔버스 핸들러는 안 돈다.

고침: `window` 의 `touchstart` 가 **모든** 터치의 시작 자리를 기억하고,
`touchmove` 에서 **임자 없는 터치가 8px 넘게 움직이면 시점으로 승격**시킨다(`promoteLook`).
왼쪽(스틱 자리)은 제외한다. 시험이 지도·핫바·캔버스 세 곳에서 실제로 yaw 가 도는지 잰다.

### 지하 지도 배율을 지상과 나눴다
갱도를 통째로 걸어도 ×1 에서는 잉크가 **9~11%**(×4 면 35~65%). 지하는 밝히는 반경이 8칸인데
×1 은 96칸을 다 그린다 — 폰에서 한 칸 0.875px. 한 값을 나눠 쓰니 오르내릴 때마다 다시 맞춰야 했다.
`opts.mmzoom`(지상 ×1) / `opts.mmzoomunder`(지하 ×3) 로 나누고 단계에 ×3 을 넣었다.

### 폰에서 표식에 이름을 붙인다
데스크톱은 `Shift+B`, 폰은 `toggleMark(false)` 고정이라 길이 없었다 —
**코드가 스스로 그 문제를 적어 놓고도**(`hud.js`: "열두 개가 전부 똑같은 금색 점이면…")
폰에서만 안 지켜졌다. **표식 위에서 길게 = 이름 · 빈 자리에서 길게 = 확대** 로 갈랐다.

> **인덱스 0 은 거짓이다.** `if (markHere())` 로 뒀더니 **첫 표식(0번)만 이름이 안 붙었다.**
> `>= 0` 으로 못 박았다. `x !== false` 함정(v68)과 같은 부류다.

### 시험
폰 지도 시험을 새 계약으로 다시 짰고(탭·재탭·빈 자리 길게=확대·표식 위 길게=이름·쓸기),
`지도·핫바·단추 위에서 쓸어도 시점이 돈다` 1항목 추가.
배율이 지상·지하로 갈려 **픽셀을 세계 좌표로 읽는 시험 셋에 배율 1 을 못 박았다.**
**321 + 폰 13** (러너 332). 전 항목 ×10회 통과.

## v87 (2) — 갱도가 갈라지고 끝방이 선다 (2026-09-09)

자문 16차의 마지막 둘. 같은 판에 담았다.

### 갱도가 17~32초짜리였다
줄기 다섯이 **따로따로**고 갈래가 없었다. 통로 총연장 79~146칸.
토막 중간에서 **45% 확률로 갈래를 하나 더** 뻗게 했다 (재귀 1단 — 갈래는 다시 안 갈라진다).
`mineBoxOk` 는 토막마다 그대로 걸리므로 안전성은 안 바뀐다 (물·용암 접촉 여전히 0).
기둥 92~104 → **156~240** · 걸을 수 있는 바닥 → **584~1,060칸**.

### 끝방이 세계당 0~2개였다 (기대 3개)
두 가지가 겹쳐 있었다 —
① 5×5 상자가 통로보다 훨씬 커서 `mineBoxOk` 에서 자주 떨어졌다.
② **`lastLeg` 를 "계획한" 토막 수로 정했는데**, `mineFits` 실패로 줄기가 중간에 끊기면
   마지막 토막이 영영 안 와서 그 줄기는 방을 못 받았다. v85 가 꺾기를 넣으며 끊김이 늘어 더 줄었다.

`endRoom()` 을 따로 빼고 **실제로 놓인 마지막 토막**(`laid`, `lastBx/lastBz`)에 단다.
5×5 가 안 되면 **3×3 으로 물러선다.** 소품도 하나가 아니라 2~3개.
끝방 소품 → 세계당 **3~13개**.

> **"계획한 것" 과 "실제로 된 것" 을 구별한다.** 루프가 중간에 끊길 수 있으면
> 마지막 회차를 `i === n-1` 로 알아내면 안 된다 — 실제로 성공한 마지막을 들고 있어야 한다.

### 시험
`v85 갱도` 에 통로 길이·끝방 소품 단언을 더했다. 갱도가 길어지자
**"견줄 자연 돌 바닥" 을 ±8칸에서 못 찾아** 10/10 회 실패했다 — 찾는 반경을 넓혔다.
**321 + 폰 13** (러너 332). 전 항목 ×10회 통과.

## v87 (3) — 핫바 모양을 색으로도 가른다 (2026-09-09)

자문 16차의 마지막 한 건. `▄`(채운 네모)와 `◱`(빈 네모)가 **폰 9px 에서 둘 다 "작은 주황 네모"**
였다. 그리고 **고른 칸에는 아예 안 그리고 있어** 손에 든 것이 뭔지는 계기판에만 있었다.

`▬`(주황) / `◤`(하늘색) 로 **글자와 색을 같이** 갈랐다.
> **작은 글리프는 형태보다 색이 먼저 읽힌다.** 폰 9~11px 에서 형태로만 가르면 안 갈린다.

시험이 글자뿐 아니라 **`getComputedStyle().color` 도 견준다** — 형태만 보면 이 문제를 못 잡는다.
**321 + 폰 13** (러너 332). 전 항목 ×10회 통과.

## v88 — 스스로 점검한 판이 버그 여섯을 냈다 (2026-09-09)

오늘 아침 네 판(v84~v87)을 빠르게 썼다. **자문을 기다리는 동안 `/code-review` 로
내 코드를 되짚었더니 진짜 버그 여섯이 나왔다** — 시험 332항목이 전부 통과하는 채로.

1. **끝방이 통로 한가운데 생겨 버팀목·횃불을 지웠다.** `endRoom()` 이 토막 길이(`len`)를
   안 받아 `x0 + MINE_W`(시작 언저리)에 방을 팠다. 방 바닥이 통로를 조약돌로 다시 깔면서
   그 자리의 울타리와 횃불이 사라졌다 — **"끝까지 걸어가 볼 이유" 가 통째로 없어졌다.**
   시험은 방 소품 **개수**만 세어 통과했다.
   > **개수를 세는 시험은 자리가 틀린 것을 못 잡는다.**
2. `swapBarPage()` 가 **모양을 되돌리기 전에** `refreshBar()` 를 불러, 새 쪽의 글리프가
   지난 쪽 값으로 굳었다 — 글리프가 고치려던 바로 그 "첫 한 번은 틀리게 놓는다" 가 되살아났다.
3. **물속에서 양동이가 늘 제자리만 펐다.** `raycast` 가 첫 칸(눈이 잠긴 칸)부터 보므로
   `wantLiquid` 면 t=0 에서 바로 맞는다. 웅덩이를 안에서 빼낼 수가 없었다.
   눈이 든 칸을 건너뛴다.
4. **시점 승격이 터치 단추·핫바까지 먹었다.** 놓기를 누른 채 손이 8px 흔들리면
   블록도 놓고 화면도 돌았다. 핫바는 좌우 쓸기로 칸을 바꾸는 제 제스처가 있다 —
   **제 몫의 끌기가 있는 UI 는 승격에서 뺀다**(`ownsDrag`).
5. 폰 도움말이 핫바 2쪽에 **TNT** 가 있다고 했다 — v86 에서 양동이로 바뀌었다.
6. `markHere()` 는 **첫** 표식을, `toggleMark()` 는 **마지막** 표식을 골랐다.
   3칸 안에 둘이면 이름 붙인 것과 지운 것이 달라진다. 둘 다 마지막으로 통일했다.

곁가지: 끝 너머 한 자리만 대 보던 끝방을 **끝 너머 + 옆구리 두 쪽**으로 늘렸다 —
자리가 안 나오는 시드(555)에서 방이 0개였다.

> **빠르게 쓴 판은 스스로 되짚는다.** 시험이 다 통과해도 시험이 안 보는 성질이 있다 —
> 특히 **"자리"**(끝방이 어디에 생기나) 와 **"순서"**(refreshBar 를 언제 부르나) 다.
> 자문을 기다리는 시간이 그 되짚기에 딱 맞는다.

### 시험
`폰 · 지도·핫바·단추` 시험을 새 계약으로 (핫바는 칸이 바뀌고 시점은 안 돈다 ·
단추를 누른 채 흔들려도 안 돈다). **시험이 요소에서 터치를 쏘게** 고쳤다 —
`window` 에 직접 쏘면 그 요소의 제 핸들러가 안 돈다.
**321 + 폰 13** (러너 332). 전 항목 ×10회 통과.

## v89 — 자문 17차 (2026-09-09)

자문이 **v88 이전 사본**을 봐서 #1(핫바 쓸기가 시점도 돌림)은 이미 고쳐져 있었다.
나머지 중 여덟을 처리했다. (양동이로 **붓기**는 중 규모라 백로그로.)

### 양동이 둘레 넷 — 새 기능은 둘레가 다 문제였다
- **문이 안 열렸다.** `place()` 첫 줄에서 양동이로 빠져나가 `tryInteract` 를 영영 안 지났다.
  **같은 도구인 부싯돌은 `tryInteract` 안에 있어 문이 열린다** — 도구 둘의 규칙이 달랐다.
  물을 퍼서 돌아왔는데 제 집 문이 안 열리면 안 된다.
- **회색 상자로 보였다.** `drawIcon`·`makeBlockGeometry` 가 `isCross` 만 납작하게 그리고
  나머지는 6면 큐브라, 핫바·손에서 무엇인지 알 수가 없었다.
  게다가 땅을 겨누면 **놓을 수도 없는 "양동이 블록" 의 고스트**가 떴다(`isItem` 을 안 봤다).
- **되돌리기가 한 칸씩** 쌓였다. 누른 채면 초당 5칸이 따로 기록돼,
  3×3 웅덩이를 지우고 되돌리는 데 `Ctrl+Z` 아홉 번. 이 게임의 자랑이
  "밀려든 물 192칸도 한 번"(v78)인데 **양동이만 그 밖**이었다. 홀드를 한 묶음으로.
- **물속에서 제자리만 펐다.** `raycast` 가 눈이 잠긴 칸부터 본다 — 그 칸을 건너뛴다.

> **도구를 하나 더 넣을 때는 먼저 있던 도구와 규칙을 맞춘다.**
> 부싯돌이 이미 답을 갖고 있었는데 양동이는 다른 길로 갔다.

### 과제 둘 — 내가 옮긴 문턱이 양쪽으로 틀렸다
- **꼭대기**: v82 에서 `y>50` → `SEA+9` 로 옮기며 **판 2 의 뭍 평균 높이(26.6~32.1)를 안 셌다.**
  8시드 중 2개가 **스폰 자리에서 이미 달성** — 게임이 처음 건네는 말이 가만히 서 있는 것에
  대한 상장이었다. `moved` 를 걸고 `SEA+15` 로.
- **지도장이**: 분모가 지도 전체(9,216칸)인데 **뭍은 23~31%뿐**이라,
  섬을 한 칸도 안 남기고 다 걸어도 30%대에서 멈췄다. 설명("섬의 8할")과 어긋났다.
  분모를 뭍 기둥으로.

### 폰 셋
- **스틱을 끝까지 밀면 달린다** — 폰에는 `Ctrl` 도 `W` 더블탭도 없어 96칸 섬을 늘 4.6b/s 로
  걸었고, 비행 2배에도 갈 길이 없었다. 베드락도 조이스틱 테두리까지 밀면 달린다.
- 설정 라벨 **"마우스 감도" → "시점 감도"** (폰에 마우스가 없다) + 도움말에 한 줄.
- **과제 팝업이 조준선 자리**(폰 세로 390px 에서 150px 는 곧 화면 한가운데)였다 → 136px.

### 시험
`v89` 2항목 + 폰 1항목. `v82 과제` 를 새 문턱으로.
**323 + 폰 14** (러너 335). 전 항목 ×10회 통과.
폰 시험에는 `arena` 헬퍼가 안 실린다 — 시험장을 손으로 깔아야 한다.
스틱은 픽셀 반지름에 기대지 말고 `S.stick` 을 직접 넣는다.
